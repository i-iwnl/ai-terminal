# Issue #120 P2: 操作性・当たり判定・E2E ハーネスの穴 - Overview

> **Issue**: [#120 P2: 操作性・当たり判定・E2E ハーネスの穴（11 件を統合）](https://github.com/i-iwnl/ai-terminal/issues/120)
>
> 旧 #116 / #114 / #113 / #112 / #111 / #106 / #90 / #89 / #83 / #57 / #13 を畳んだ Issue。
> **各項目の調査記録と実測値は、統合元の Issue 本文が引き続き正**（クローズしても読める）。
>
> - `architecture.md` - 触る構造・Contract 変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-08-03

---

## 1. ゴール

P1（#119）が「見た目と構造」を直したのに対し、こちらは **操作（キーボード・当たり判定）と、
検査そのものの穴**を埋める。

| カテゴリ | 対象 |
|---|---|
| 対象トラック | main（menu / poller）+ renderer（shortcuts / TabBar / Sidebar）+ e2e（ハーネス） |
| ブランチ | 周1・周2: `feat/issue-120-p2`（マージ済み・削除済み）。周3 以降は main から新規 |
| 関連 PR | #123（周1・周2。**#120 は閉じていない**） |

---

## 2. 着手前に実コードで測り直した結果（2026-08-03）

**Issue 本文の前提が2つ古くなっていた。** loop.md「計画書の現状認識は古くなる」の9件目・10件目。

| Issue 本文の記述 | 実際 |
|---|---|
| 「`README.md` のショートカット表に `Cmd+E` / `Cmd+J` / `Cmd+Shift+J` / `Cmd+Shift+[` / `Cmd+Shift+]` が1つも載っていない」 | **全部載っている**（README の該当表に5件とも実在）。**この作業項目は既に不要** |
| （A-4 の前提）「`Cmd+W` にタブを閉じる手段が無い」 | `Cmd+W` は `close-pane` だが、**README が「タブに残るペインが1枚だけならタブごと閉じる」と明記**しており、意味変更は意図的に文書化済み。**問題は「分割中のタブを1手で閉じられない」ことだけ**に絞られる |

依然として正しい前提:

- `menu.ts` の `role: 'resetZoom' / 'zoomIn' / 'zoomOut'` は実在し、**`registerAccelerator` が true**（`actionItem()` を通らないため）
- **zoom 3種は README に1つも載っていない**
- `close-tab` は `accelerator: undefined`
- `Cmd+E` は `last-active-tab`（`shortcuts.ts`）

---

## 3. 完成条件

### 周1: キーボードの決着（A-1 / A-2 / A-4）— **完了 2026-08-03**（PR #123 でマージ済み）

- [x] `Cmd+E` を macOS 標準へ返すか、`last-active-tab` のまま残すかを決め、**理由を Issue に書き戻した**
- [x] フォントサイズについて (a) `AppAction` に足す / (b) Electron の zoom をそのまま認める のどちらかを決めた。**両方をキーに載せていない**
- [x] (a) を採るなら `menu.ts` の zoom 3種の `role` の扱いを**同じ周で**決めた（放置すると同じキーが2系統から発火する）
- [x] 分割中のタブを1手で閉じる手段を用意するか決めた
- [x] `README.md` のショートカット表が実装と一致している（**zoom 3種の扱いを含む**）
- [x] `test/unit/` の `matchShortcut` のケースを更新した

### 周2: サイドバーのパネル切替（A-3）— **完了 2026-08-03**（PR #123 でマージ済み）

- [x] `role="tab"` + `aria-selected`（または `aria-current`）が付いた。**`<button>` 要素そのものは変えていない**（E2E 28ファイル・44箇所が `.sidebar__tabs button` を使う）
- [x] `AppAction` にパネル切替の語彙を足した
- [x] `menu.ts` の「表示」に載せた（現在のパネルにチェックが付く形が望ましい）
- [x] キーを足した。**`Cmd+Shift+1/2/3` は使わない**（`Cmd+Shift+3/4/5` は macOS のスクリーンショットにシステムが先に奪う）
- [x] `e2e/` にキーボードだけで切り替えるシナリオを追加し、**同じ PR で** `scenarios.yml` に追記した

### 周3: 当たり判定（B-2）+ 改名の結論（B-3）— **完了 2026-08-03**

- [x] タブの閉じるボタンの張り出しについて判断した。**3案はいずれも採らず**、(a)(b) は実測から却下、(c) より良い手（**ボタンの箱を左へ非対称に広げる。見た目 0px 変更で食い込み 0**）を採った。**`design-rules.md` に「当たり判定を広げるときは、広げた先が誰のクリック対象かを向きごとに見る」として一般化した**
- [x] `S44-target-size.spec.ts` に「当たり判定がタブ選択の側へ 1px も食い込まない」を追加した（`elementFromPoint` の 1px 刻み）。**修正を戻して決定的に赤くなることまで確認した**
- [x] **B-3（サイドバータブの改名）は e（改名しない）で決着。** P1 の周3 で入れた `.panel-scope` が I-4 の代替を担っていることを実コードで確認し、結論を #120 に書き戻した

### 周4: 不具合（C-1 / C-2）— **完了 2026-08-03**

- [x] C-1: **仮説を実際に観測した**（`performance.now()` の計装 + CPU 16本のビジーループ）。仮説は正しかったが**遅延源が違った** — 「xterm がマウント中」ではなく **React 18 の passive effect フラッシュの遅延**。`createFocusEchoGate()` でプログラム的 `focus()` のこだまを落とすようにした
- [x] C-1: **テストを待ちで硬くするだけで済ませていない**（S61 は既に `is-active` を待っており、その後に競合していた）。判定を純粋関数へ切り出し `test/unit/focus-echo.test.ts` 6件で固定し、**対照実験で 180 起動 × 2 アーム、10 対 0（p ≈ 0.002）**まで確認した
- [x] C-2: 無言で終わらなくなった。**ただし C-2 は #120 の本文に存在しなかった**（`issue-24/known-issues.md` 2 が原典 → #119 のレビューで再発見 → #120 へ回されたが**転記が漏れていた**）。#121 の C-2 とは別物

> **C-2 の空振りは「他アプリ起動」限定ではない。** tmux 永続化でタブだけ閉じた場合と、アプリ再起動後（`ownedSessionIds` はプロセス内 Set）にも同じ経路に落ちる。

### 周5: テスト基盤（D-1 / D-2）

- [ ] D-1: `make e2e-lint` に「`e2e/screenshots.spec.ts` が参照するセレクタが実装に存在するか」の check を足した（または `make e2e` に撮影レーンを含めた）。**決めた方針を `.claude/skills/e2e/` に書いた**
- [ ] D-2: 偽 CLI に「呼び出し回数に応じて内容が変わる `agents.json`」のモードを足した。**既存シナリオの挙動を1つも変えていない**
- [ ] D-2: `S12-task-list.spec.ts` で `busy -> idle` の遷移を作り、「待たせています」が出ることを検証した
- [ ] D-2: `docs/images/S12-task-list.png` を撮り直した
- [ ] **`.claude/workspace/issue-119/known-issues.md` の 8（撮影レーンの非決定性）のうち、経過時間の分がこの周で消えること**

### 周6: 手動確認と判断の記録（D-3 / E-1）+ 締め

- [ ] D-3: Finder からの実ドラッグ4項目を人手で確認し、結果を記録した
- [ ] E-1: safeStorage の着手条件が今も満たされていないことを確認し、結論を書き残した
- [ ] `known-issues.md` の 7（S40 の未較正4件）を、`.tab-bar__tab--*` を触る周があれば較正した

### 全周共通

- [ ] `make check` が通る
- [ ] **`make e2e` は P2 の最後に1回**。`make e2e-lint` は毎周
- [ ] `README.md` を更新した

---

## 4. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 周5・周6 は未着手（周1〜周4 は完了） |
| 実装 | **周1・周2 完了**（PR #123 で main へマージ済み）/ **周3・周4 完了**（`feat/issue-120-p2-r3`。未 commit・未 PR）/ 周5・周6 が残り |
| 検証 | 周4 の時点で `make check` **450 PASS** / `make e2e-lint` PASS=581 FAIL=0 / S61・S63・S57・S60・S56・S42・S25・S07・S44・S69 green / C-1 は**対照実験で 180 起動 × 2 アーム、10 対 0**（p ≈ 0.002） |

`make css-substitution-check` は周3 で**意図的に落としている**（幾何を実際に変えたため。
CLAUDE.md「値を意図的に変えるときだけ落ちてよい」）。

---

## 5. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | 周5（D-1: `e2e-lint` の check / D-2: 動的フィクスチャ） | D-2 は `.claude/workspace/issue-119/known-issues.md` の 8（撮影レーンの非決定性）のうち経過時間の分を消す。**周3 の撮影で、8枚すべての差分がこの経過時間だったことを再確認済み** |
| P1 | 周6（D-3: D&D の手動確認 / E-1: safeStorage の判断）+ 締めの `make e2e` | `known-issues.md` の 1（タブの 38% が死角）もここか #121 で扱う。**2（S69 の一度きりの flaky）はフル実行で再確認する** |

**ブランチ**: 周1・周2 は `feat/issue-120-p2` で作業し PR #123 としてマージ済み（ブランチは削除済み）。
**周3・周4 は `feat/issue-120-p2-r3`**（main から新規。**まだ commit も PR もしていない**。
ユーザー指示で「周4 まで進めてから」commit する方針。周3 と周4 で1本の PR にする）。
main から生やした枝が1本なのでスタックにはならない。
