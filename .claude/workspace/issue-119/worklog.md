# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-08-03 - Issue の統合と、P1 の計画（5ペルソナレビュー実施）

### 実施内容

- **オープン Issue 22 件を P ごとに3件へ統合した。** #119（P1）/ #120（P2）/ #121（P3）。統合元は全部クローズ済みだが、**調査記録と実測値は統合元の本文が引き続き正**（クローズしても読める）
- #20 の Phase/PR 表を実コードで測り直し、**3件が古かったことを確認した**（後述）
- ユーザー判断で、#120 の B-1（履歴行の 96px 予約）を **P1 の前提として #119 に引き上げた**（#120 からは外した）
- **P1 の案を書き、5ペルソナのデザインレビューを回した。** 案の前提が5つ壊れた（`architecture.md` の 4.1）
- レビューの裁定と周の分割を `architecture.md` に記録した

### 設計判断

裁定の全文は `architecture.md` の 4.2 にある。要点だけ:

- **E-4（`selectionBackground` を上げる）は却下。** 3人が独立に「選択範囲の塗りは WCAG 1.4.11 の対象ではない」と結論した。拘束するのは**塗りの上のテキストの 1.4.3** で、`#2f5d8f` にすると既定前景が 5.73 → 4.60 に悪化し、ANSI 16 色のうち 3:1 未満が 9 → 11 色に増える。**改善ではなく退行だった**
- **G（選択中タブ）は白の下辺 2px。** アクセントも `--border-control` も `prefers-contrast: more` の面（#525252）で 3:1 を割る。白だけが 7.81 を保ち、しかも1〜3型で比が動かない
- **A-3（メモ/編集の常時表示）はしない。かつ `visibility: hidden` も採らない。** `visibility: hidden` の要素はフォーカスを受けられないので `:focus-within` が発火できず、キーボードからメモへの唯一の入口が消える。**この点は2人のレビュアーが両方とも見落としていた**
- **F-3（ウィンドウ状態）は `config.json` ではなく `window-state.json`。** `setConfig` → `broadcastConfig` → 全ペインの `term.options.theme` 再代入という経路が実在し、ウィンドウを掴んで動かすたびにこれが走る
- **周1 として「値も見た目も1つも変えず、壊れたときに赤くなる検査を置く周」を先頭に足した。** 現在の 70 spec は、この Issue が変えようとしている4項目を1本も見ていない

### 教訓

- **#20 の Phase/PR 表は 2026-07-31 時点で止まっていた。** PR 15（サイドバー折りたたみ）は #118 で完了、設定の節切り直しは #115 で完了、履歴の範囲見出しは #117 で完了していた。**loop.md が「計画書の現状認識は古くなる」と警告しているとおりで、8件目の実例**
- **「PR #108 と同じ手が使える」という類推が誤りだった。** #108 は `padding-left` で幅を予約したまま当たり判定だけ直したもので、**1px も幅を回収していない**。回収するかしないかで、見た目が変わるかどうかが真逆になる。**前例を引くときは「何をしたか」ではなく「何を維持したか」を見る**
- **`opacity: 0` は3つの性質を持ち、混同しやすい。** (1) レイアウトから外れない、(2) アクセシビリティツリーに残る、(3) **ポインタイベントを止めない**。`.tab-bar__close` は (3) を `pointer-events: none` で潰した実績があるのに、`.history-item__action` には入っていない。**同じ規則が片方にしか適用されていない箇所は、他にもある可能性がある**
- **紙のコントラスト表の誤りが4件目。** design-rules が3件記録している型（「間違っていたのは何と比べるか」）がそのまま再発した。案の 2.03 / 2.53 は実際には 1.96 / 2.45
- **インラインスタイルは詳細度でクラス規則に必ず勝つ。** `style={{width}}` を常時付けると `.sidebar.is-collapsed { width: 0 }` が死に、#118 で入れたばかりの折りたたみが黙って壊れる。**`--sidebar-width` カスタムプロパティ経由にすれば後勝ちの順序が保てる**

### 次に再開するとき最初に読むべきこと

1. **`architecture.md` の 4.1（壊れた前提）と 4.2（裁定）を必ず読む。** 案の初版をそのまま実装すると、当たり判定・折りたたみ・選択色の3箇所を壊す
2. **次のアクションは周1（関門を先に置く）。** `overview.md` の「周1」の完了条件が正。**値も見た目も1つも変えないこと。`make css-substitution-check` PASS が受け入れ条件**
3. **周1 の一番重要な成果物は「履歴行のタイトル右端をクリックすると `.history-item__row` に当たる」assert が、現状の実装で赤くなることを確認すること。** 赤くならないなら、その検査は何も守っていない（loop.md の「赤くなるか確かめたが空振りする5つの形」の1つ目）
4. レビューで見つかった Issue 未登録のバグのうち、**通知の無言 no-op と `Cmd+W` の割り当ては #120 へ回した**。`pointer-events` と `chromeSafeToApply` の取り残しは、それぞれ周2 / 周6 の前提としてこの Issue で直す
5. **`.tab-bar__state-slot` の配線は周5。** 6px のスロットは全タブで確保済みかつ空で、判定関数 `findNextYourTurnTab` も既にある。**唯一の利用者が `Cmd+J` だけ**という状態

---

## 2026-08-03 - 周1: 関門を先に置く（値も見た目も1つも変えない）

### 実施内容

- `--bar-height: 36px` を宣言し、`.tab-bar { height }` と `.notice-list { top }` の
  **リテラルの 36 の二重管理**を解消した。`make css-substitution-check` PASS
- **S73**（ウィンドウ上端の2本の帯の高さ）と **S74**（履歴行のタイトルの当たり判定）を追加
- `test/unit/selection-contrast.test.ts` を新設
- `test/unit/css-tokens.test.ts` に `--bar-height` の参照検査と、
  サイドバーの見出し2種の宣言の一致検査を追加
- `e2e/scenarios.yml` を同じ周で更新（`make e2e-lint` PASS=518 / FAIL=0）
- **4つの関門すべてを実際に壊して、赤くなることを確認した**

### 設計判断

- **`.panel-heading` への畳み込みは周3 へ移した。** `verify-css-substitution.mjs` は
  「トークンを展開したテキストが一致するか」を行単位で見るので、**セレクタを
  まとめると宣言の出現回数が減って必ず FAIL する**。周1 の受け入れ条件
  （PASS）と両立しない。代わりに**畳める前提（2つの宣言が本当に同一であること）**を
  単体テストで固定した。これは畳んだあとも意味を持ち続ける検査ではないが、
  畳む判断の根拠としては十分
- **`selectionBackground` を S40 に足すのはやめた。** 選択範囲は xterm が canvas
  （WebglAddon）に描くので DOM に存在せず、`e2e/fixtures/contrast.ts` の
  `getComputedStyle` ベースの実装では**原理的に測れない**。`src/shared/theme.ts` の
  `contrastRatio()`（S40 と同じ式）を使う単体テストに置いた
- **S74 の主 assert は「現状の不具合の記録」ではなく「周2 の退行の検出」**にした。
  現状は `.history-item__actions` がフロー内にあるのでタイトルとは重ならず、
  9点すべてが resume に届く（green）。**周2 で絶対配置にしたときに、
  `pointer-events: none` を忘れると赤くなる**という形

### 教訓

- **実測すると、レビューの指摘も外れる。** 保守担当レビュアーは
  「`.history-item__action::before`（24px）の当たり判定が meta の行に上下 3.5px ずつ
  はみ出している = 今すでにバグ」と報告したが、**成立していなかった**。
  ボタンの実寸は 14px ではなく **21px** で、`::before` の下端は y=200.5、
  `.history-item__meta` の上端は y=201.5。**1px の余裕がある。**
  design-rules の「要素の実寸を紙で計算しない」が、レビュアーの側でも起きた。
  ただし**余裕が 1px しかない**ことは事実なので、S74 でそこも見るようにした
- **「関門を先に置く」の受け入れ条件を、置こうとしている検査の性質に合わせて選び直す必要があった。**
  `css-substitution-check` PASS を周1 全体の条件にすると、構造のリファクタ
  （`.panel-heading`）が入らない。**同じ周に「値を変えない置換」と
  「構造を変えるリファクタ」を混ぜたのが誤りで、後者を周3 に移した**
- **`#3a6ea5`（選択範囲の塗りを 3:1 に届かせる値）に上げると、単体テスト3件が同時に落ちた。**
  塗りの 3:1 と文字の 4.5:1 が両立しないことが、机上の計算ではなく
  **落ちるテスト**として残った。次に同じ提案が出たときは、この赤が答えになる

### 次に再開するとき最初に読むべきこと

1. **次のアクションは周2（A: 履歴行の幅回復）。** `overview.md` の「周2」の完了条件が正
2. **周2 では `pointer-events: none` を必ずセットで入れる。** 入れ忘れると S74 が
   「9点中5点が resume に届かない」で赤くなる（実測で確認済み）。
   `.history-item__action` に `pointer-events: none`、
   `.history-item:hover .history-item__action` と `.history-item__action:focus` に `auto`
3. **周2 は「見た目を変えない」と名乗ってはいけない。** `.history-item__title` は
   `text-overflow: ellipsis` を持つので、実効幅が 139 -> 235 になれば省略位置が動く。
   S16 / S18 / S19 の撮り直しが作業の一部
4. **S74 の characterization（`rowWidth` 139 / `actionsWidth` 88）は消さずに更新する。**
   その diff がレビュー資料になる
5. `.history-item__meta` は `.history-item__row` の**内側**にある（実測で確認）。
   幅を回復すれば meta も一緒に広がるので、B-4（フォルダ名を出す）の前提は
   周2 だけで揃う

---

## 2026-08-03 - 周2: 履歴行の幅を回復する

### 実施内容

- `.history-item__actions` を `position: absolute` にしてフレックスの行から外した。
  **`.history-item__row` の実効幅が 139px -> 235px に回復**（S74 で実測）
- `.history-item__action` の `opacity` / `pointer-events` を**入れ物側へ移した**
  （`.history-item__actions` に `opacity: 0; pointer-events: none`、
  `.history-item:hover` と `:focus-within` で `1` / `auto`）
- `.history-item__meta` に `min-width: 0`、子の `<span>` に
  `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` を入れた。
  1つ目（相対時刻）だけ `flex-shrink: 0`
- `.history-item:focus-within` にも行の塗りを当てた
- `.history-item__actions::before` に 16px のフェードを足した
- S74 の characterization を 139/88 -> 235/96 に更新
- `docs/images/` の S16 / S18 / S19 を撮り直した

### 設計判断

- **`opacity` を個々のボタンではなく入れ物に持たせた。** ボタンは絶対配置で
  タイトルの上に重なるので、`background: transparent` のままだと下の文字が
  透けて読めなくなる。入れ物に `--surface-2`（行のホバーと同じ塗り）を敷いたが、
  **opacity を個々のボタンに置いたままだと、隠れているあいだも入れ物の塗りだけが
  行の上に残る。** 入れ物側へ移すことで塗りも一緒にフェードする
- **`visibility: hidden` ではなく `opacity: 0` + `pointer-events: none`。**
  `visibility: hidden` の要素はフォーカスを受けられないので `:focus-within` が
  発火できず、キーボードからメモ機能への唯一の入口が消える（レビュアー2人が
  この点を見落としていた。`architecture.md` の 4.2）
- **`.history-item:focus-within` にも行の塗りを当てた。** ホバーせずに
  キーボードだけで到達したとき、入れ物の塗りだけが行と違う色になって
  継ぎ目が見えるのを防ぐ
- **左端に 16px のフェードを足した。** agent-browser で実機を見たところ、
  長いタイトルが**省略記号も無しに不透明な矩形で断ち切られ**、
  「タイトルがそこで終わっている」ように読めた（`text-overflow` は 235px の
  位置で効くので、その手前を覆っているあいだは省略記号が出ない）。
  フェードにすると「文字がボタンの下へ続いている」と読める

### 教訓

- **撮り直した13枚のうち、今回の変更が理由なのは3枚だけだった。** 残り8枚は
  撮影レーンの非決定性（経過時間・ランダム UUID・空状態が写る）。
  `known-issues.md` の 8 に記録した。**RGB で画素差を測って1枚ずつ判定し、
  説明できないものは `git checkout HEAD --` で戻す**運用で守った
  （`.convert('RGBA')` を挟むと Pillow の `getbbox()` が誤判定するので必ず RGB）
- **agent-browser で実機を見て初めて分かったことがある。** E2E と
  スクリーンショットはどちらもホバーしていない状態しか撮らないので、
  「ボタンが出ているあいだタイトルがどう見えるか」を1本も見ていなかった。
  断ち切られた見た目は自動検査では絶対に見つからない
- **S74 が予測どおりに機能した。** 周1 で「`pointer-events` を省くと9点中5点が
  resume に届かなくなる」ことを確認してあったので、周2 では入れ忘れがないことを
  green で確認するだけで済んだ

### 次に再開するとき最初に読むべきこと

1. **次のアクションは周3（B: スコープ行 + C の結論）。** `overview.md` の「周3」が正
2. **周3 では最初に `.panel-heading` への畳み込みをやる**（周1 から移した）。
   `test/unit/css-tokens.test.ts` の「サイドバーの見出し2種の宣言が完全に一致している」が
   畳める前提を保証している。**`make css-substitution-check` はこの変更では
   必ず FAIL する**（セレクタをまとめると宣言の出現回数が減るため）。
   落ちた出力を根拠として残すこと
3. **`.memo-panel__heading` は畳む対象に含めない**（`color` と `margin` が別物。
   単体テストが固定している）
4. 周3 の B-4（履歴 meta にフォルダ名）の前提は周2 で揃った。
   `.history-item__meta > span` は既に ellipsis を持ち、1つ目（相対時刻）だけ
   `flex-shrink: 0` なので、**フォルダ名を足すならその後ろに置けば削られるのは
   フォルダ名とブランチ名になる**
5. **画像は毎回 RGB で1枚ずつ判定すること**（`known-issues.md` の 8）

---

## 2026-08-03 - 周3: 3パネルにスコープ行を常設し、見出しの階層を作る

### 実施内容

- `.panel-scope` を新設し、**タスク / 履歴 / メモの3パネル最上部に「いまどの範囲か」を常設**した
  - タスク: `このマシン全体の Claude` / `このフォルダの Claude`（`scopeAgentsToCwd` に連動）
  - 履歴: `このフォルダの Claude` / `すべてのフォルダの Gemini` など（範囲 + プロバイダ）
  - メモ: `このアプリのメモ`
- 見出しの階層を作った。`<h1 class="visually-hidden">ai-terminal</h1>` を足し、
  パネル内の区切り（`task-group__heading` / `memo-panel__heading` / `panel-empty__heading`）を `<h3>` へ落とした
- 履歴の `history-list__scope-note` から「すべてのフォルダを表示中」を落とした
  （すぐ上のスコープ行と**同じことを縦に2回言っていた**）
- **B-4**: `allFolders` のとき履歴行の meta に `basename(entry.cwd)` を出す
- 全体メモの placeholder に「どのフォルダ・どのセッションにも属さない」を明記
- S75 を追加。S65 / S71 の characterization を更新
- `test/unit/css-tokens.test.ts` の「見出し2種の宣言が一致」を、
  **「範囲」と「区切り」が同一ではないこと**を見る検査に差し替えた
- `design-rules.md` に耐久性のある規約を7件反映した

### 設計判断

- **`.panel-heading` へ畳む案はやめた。** 履歴の見出しは「範囲」、タスクの
  グループ見出しは「区切り」で、**同じ体裁にすると3つ並んだときに1つ目だけ
  意味が違う**状態になる（IA / a11y / 保守担当の3人が独立に指摘）。畳むのではなく
  `.panel-scope` として**別物にした**
- **区別の手がかりは下線。** `text-transform: uppercase` は日本語に効かないので、
  大文字化の有無は英字が混じるときしか差にならない
- **履歴の見出しにプロバイダを入れた。** すぐ下のツールバーに Claude / Gemini の
  トグルが見えているのに、見出しは範囲しか言っていなかった
- **メモにもスコープ行を置いた（(b) 案ではなく (a) 案）。** IA は「範囲が自明なので
  足さない」を推したが、**1枚だけ空けると読み手に「範囲が無いのか書き忘れか」を
  推測させる**（macOS レビュアー）。メモの本当の曖昧さ（全体メモがフォルダを
  またぐこと）は placeholder の修正で別に解いた
- **S01 の注釈はスコープ行を覆ったままにした。** `right` は他の注釈と重なり、
  `above` は番号バッジがキャプションの下に隠れる。**S12（README のタスク一覧の画像）で
  遮られずに写っている**ので、そちらに任せる

### 教訓

- **`make css-substitution-check` は構造のリファクタを表現できない。** あれは
  「トークンを展開したテキストが行単位で一致するか」を見るので、セレクタを
  まとめたり分けたりすると必ず FAIL する。**周1 の受け入れ条件（PASS）と
  両立しない作業を周1 に入れていたのが誤り**で、周3 に移して正解だった。
  FAIL したときは出力の全行を説明できるかで判定する（今回、削除4件はすべて
  役目が別セレクタへ移ったもの、**表に無い `+` 行は無し**）
- **注釈が新しい UI を覆った。** スコープ行を足したら、S01 の
  「サイドバー: タスクと履歴を切り替え」がちょうどその行の上に乗った。
  **README が「実装には有るのに画像には無い」状態を配布しかけた**（#91 の実例）。
  E2E は緑のままで、RGB の画素差を1枚ずつ目で追って初めて気づいた
- **撮影レーンの非決定性がもう1種類見つかった。** 注釈の配置そのものが
  衝突回避の結果で反転することがある（S31 で実測。UI は同一なのに画像が変わる）。
  `known-issues.md` の 8 に追記した
- **`--retries` を付けないと13枚が揃わない。** フル実行のたびに1件ほど起動 flake が出る

### 次に再開するとき最初に読むべきこと

1. **次のアクションは周4（D: サイドバー幅）。** `overview.md` の「周4」が正
2. **周4 の実装方針は `architecture.md` の 4.2 が正。** 要点は3つ:
   - **幅は `--sidebar-width` カスタムプロパティ経由**。`style={{width}}` は
     `.sidebar.is-collapsed { width: 0 }` に詳細度で勝って **#118 を壊す**
   - **ハンドルは `PaneSplitterHandle.tsx` と同じゴースト方式**（`position: fixed` の
     線だけ動かし、`mouseup` で1回コミット）。`fit()` の間引きは要らなくなる
   - **キーボード代替は「表示」メニューの3項目**（`accelerator` 無し）。
     `menu.ts` の `分割比を広げる / 狭める / 50%に戻す` が前例
3. **`clampSidebarWidth()` の下限は信号機の実測右端（x=76）より十分上に置く。**
   下回ると #20 が却下した「44px のレール」の構図が再現する
4. **`DEFAULT_CONFIG.sidebarWidth` を `src/shared/defaults.ts` に必ず足す**
   （`e2e/fixtures/harness.ts` が `{ ...DEFAULT_CONFIG, ...options.config }` を書き出す）
5. 周4 は画像0枚の見込み（既定幅を変えないため）。変わったら理由を追跡すること

---

## 2026-08-03 - 周4: サイドバーのドラッグリサイズと幅の永続化

### 実施内容

- `src/renderer/src/sidebar/sidebarWidth.ts` を新設（`clampSidebarWidth` /
  `sidebarWidthFromPointerDelta` / `stepSidebarWidth`）+ 単体テスト12件
- `SidebarResizeHandle.tsx` を新設。**ゴースト方式**（`PaneSplitterHandle.tsx` と同型）
- `.sidebar` の `width` を **`--sidebar-width` カスタムプロパティ経由**にし、
  `min-width` / `max-width` を CSS から外した
- `AppConfig.sidebarWidth`（既定 260）を `ipc.ts` -> `defaults.ts` -> `coerceConfig` の順に追加
- `AppAction` に `adjust-sidebar-width` を追加し、「表示」メニューに3項目
  （`accelerator` は持たせない）
- **S76**（ドラッグ + `pty:resize` 0 回 + 幅を変えても畳める）と
  **S77**（メニュー代替）を追加
- README にドラッグ・メニュー・下限の理由・スコープ行の説明を追記

### 設計判断

- **範囲は 220〜420px。** 下限 220 は現行の `min-width` 据え置きで、**信号機ボタンの
  実測右端（x=76）より 144px 上**にある（単体テストが `>= 100px` の余裕を固定）。
  下げると design-rules が却下した「44px のレール」の構図が再現する。
  **幅0はドラッグではなく折りたたみ（`Opt+Cmd+S`）の担当。**
  上限は 340 -> 420 に広げた（340 は掴めなかった頃の値で、実際に動かせると窮屈）
- **`min-width` / `max-width` を CSS から外した。** 範囲の正は `clampSidebarWidth`
  1箇所。CSS にも書くと二重管理になるうえ、**インラインの幅が `min-width` に負けて
  ハンドルが無反応に見える**（範囲外を掴んだときに「壊れている」と読まれる）
- **ドラッグ中は `configSet` を呼ばない。** `setConfig` -> `broadcastConfig` ->
  `coerceConfig` が毎回新しい `theme` を組み立てる -> `App.tsx` の
  `useEffect([config.theme])` -> 全ペインの `term.options.theme` 再代入、という
  経路が実在する。`mouseup` の1回だけにした
- **幅は永続化するが、折りたたみ状態は永続化しない。** 幅は「自分が決めた作業環境」で
  開き直すたびにやり直しになると困る。畳むのは「一時的にどける」操作で、
  次回起動で畳まれていると**サイドバーの存在自体に気づけなくなる**（既存の判断）

### 教訓

- **`--sidebar-width` にした判断が実測で裏付けられた。** インラインの
  `style={{ width }}` に差し替えて再ビルドしたところ、**S72 と S76 が両方落ちた**
  （畳んでも幅が 260 のまま）。レビュアー2人が独立に予測したとおりで、
  実装前に潰せていなければ #118 を黙って壊していた
- **`pty:resize` の関門は空振りしない形にした。** 同じ実行の中で
  「ドラッグ中0回 / mouseup 後1回以上」を両方見るので、カウンタが死んでいて
  0 が出ているという `revert しても green` の罠に落ちない
- **台帳は 1 spec = 1 シナリオ。** 1ファイルに `test()` を2つ書いたら
  `make e2e-lint` の check7 が落ちた。S76 / S77 に分けた
- **`make e2e-screenshots` は 9 枚に画素差を出したが、周4 の変更は1枚も無かった。**
  差分の位置は経過時間（x145-210）・セッション UUID・注釈の反転だけで、
  **ハンドルの位置（x≈256-264）には1px も無い**（`background: transparent` なので
  当然だが、確かめて初めて言える）。全部戻した
- **dev と安定版でデータディレクトリが分かれている**（`~/.ai-terminal-dev` /
  `~/.ai-terminal`）。実機確認で「既定 260 のはずが 220」と食い違ったのは、
  dev 側の config.json に古い値が残っていたため。**実機の値を疑う前に
  どちらのディレクトリを見ているかを確認する**

### 実機確認（agent-browser）

| 確認したこと | 結果 |
|---|---|
| 設定にキーが無いときの既定 | 260px |
| ドラッグ中の幅とゴースト | 幅は 260 のまま / `.sidebar__resize-ghost` が出る |
| mouseup 後 | 340px |
| `config.json` への保存 | `sidebarWidth: 340` |
| **アプリ再起動後** | **340px（永続化できている）** |

### 次に再開するとき最初に読むべきこと

1. **次のアクションは周5（F: ウィンドウ + G: 選択中タブ + state slot）。**
   `overview.md` の「周5」が正
2. **周5 の先頭で vibrancy の生死を実機確認する**（`known-issues.md` の 3）。
   `body { background: var(--surface-1) }` が不透明 + `backgroundColor` 明示で、
   **一度も見えていない疑い**がある。`document.body.style.background='transparent'` に
   して左上が変わるかを見れば1行で決着する。**F-1（帯の高さ）の判断材料**
3. **帯は 36px + `trafficLightPosition: { x: 16, y: 11 }`。** `(36-14)/2 = 11` で
   信号機の中心・帯の中心・タブバーのテキスト中心が初めて 18 で揃う。
   **44px 案は「22 x 2」という誤った算術の産物**（信号機の中心は 22 ではなく 23）
4. **G（選択中タブ）は白（`--focus-ring`）の下辺 2px `box-shadow: inset`。**
   `border-bottom` ではないのは `.tab-bar__tabs` の `overflow-x: auto` が
   外向きの影を切るため。**4辺にするとフォーカスリングと区別できなくなる**
5. **周5 は画像12枚を撮り直す。** F と G で PR は分けるが撮影は1回にまとめる。
   S40 の未較正4件（`known-issues.md` の 7）もこの周で実測に置き換える
6. `.tab-bar__state-slot` の配線もこの周（判定関数 `findNextYourTurnTab` は既にあり、
   **唯一の利用者が `Cmd+J` だけ**）
