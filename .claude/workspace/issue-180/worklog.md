# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-08-05 - ワークスペース作成と、#179 からの引き継ぎ2周の受け入れ

### 実施内容

- `.claude/workspace/issue-180/` を作成（overview / architecture / worklog / known-issues）
- **#179 の design-review で出た周5-b / 周7 を #180 のループへ引き取った**（`known-issues.md` 1番）
- Issue 本文の対象17件の open / closed を実測

### 実測（2026-08-05・`gh issue view`）

**17件のうち2件は既に closed。残り15件。**

| 状態 | Issue |
|---|---|
| **CLOSED** | #140（状態語「終了」）/ #168（潰れたタブのバッジ） |
| OPEN（15件） | #139 #141 #145 #146 #149 #150 #152 #153 #155 #157 #171 #172 #173 #176 #177 |

⚠ **Issue 本文の周2 は #140 だけを対象にしており、その #140 が既に closed。**
周2 に入る前に `grep -rn "'終了'" src/renderer/src/` で実コードの残存を確認し、
**残っていなければ周ごと畳む**（本文の周の表は起票時点の認識で、実コードのほうが新しい）。

⚠ **Issue 本文の周8 は #168 を含むが、これも closed。** 周8 の対象は実質4件（#172 / #171 / #173 / #176）。

### 設計判断

- **#179 を reopen せず #180 に寄せた**: 「open な Issue = エージェントが進められるもの」を保つため。
  #179 は対象7件を全 close して閉じており、追跡だけのために open へ戻すと open 件数の意味が濁る
- **材料は #179 の `known-issues.md` 11番へリンクするだけにした**（転記しない）。
  二重化すると、片方だけ直したときに嘘が残る
- **周番号の出自を必ず書くことにした**: 引き継ぎの「周5-b / 周7」は #179 の番号で、
  #180 本体にも「周7」がある。**混ざると再開時に事故る**ので `#179 由来の周7` / `#180 周7'` と書き分ける

### 教訓（該当する場合）

- **束ね Issue を閉じるとき、その `known-issues.md` の未対処項目が宙に浮く。**
  掟（周の中で起票しない）は正しいが、**閉じるときの引き渡し動線が無い**と、
  実測済みの材料がワークスペースの中で腐る。#180 を閉じるときは同じことをする（`known-issues.md` 1番の対処方針に明記した）

### 次に再開するとき最初に読むべきこと

- **最初に読むのは `.claude/workspace/issue-179/known-issues.md` の 11番**（周5-b / 周7 の材料。実測済みなので再計算しない）
- **次のアクションは引き継ぎ周5-b の計画を `/design-review` に通すこと。** `styles.css` を触るので起動条件に該当する。
  ⛔ 5体には「既に実測済みの数値（1.08 / `#3a3a3a` = 1.30・9.11 / `#404040` = 1.42・8.31 / `--surface-tab-active` = 1.09）」を渡し、**再計算させない**
- **`make e2e` はもう赤くない**（#179 周4-0 で環境要因と切り分け済み）。次の周で赤が出たら自分が壊したものとして扱ってよい
- 環境の罠は `.claude/workspace/issue-179/known-issues.md` 12番（⛔ 実機確認で `npx electron .` を使わない）

---

## 2026-08-05 - 引き継ぎ周5-b: design-review で案の中核が7箇所で覆り、PR を3本に割った（PR 1 = 関門）

### 実施内容

- 初版の案を書き、**`/design-review` を計画確定前に5ペルソナで通した**
- **1周目で3体が API エラーで途中終了**したので、返ってきた2体（macOS / IA）の確定事実を
  `confirmed-facts-5b.md` に書き出してから3体を回し直した（`run-review.md` の「2周目」の作法）
- 改訂版を別ファイルに書いた（初版は残す）
- **PR 1（関門・製品コード0行）を実装**: `contrast.ts` の alpha ガード / `S40` に2件 / `S64` に3件

### 案の中核が覆った7箇所（**指摘した人数つき**）

| 初版 | 実際 | 人数 |
|---|---|---|
| 「ターゲットサイズが 24 を割っているかもしれない」 | **158 x 34 / 30 / 30。2.5.8 は既に充足。** 真の欠陥は**高さが不揃い**（`font: inherit` が `line-height: normal` を持ち込み、日本語ラベルだけ Hiragino Sans で 1.5em になる） | **5人** |
| 「開いた直後を測れば hover と focus を1本で固定できる」 | **成立しない。** 別状態。しかも `contrast.ts` が**前景の alpha を見ない**ので透明を測ると**黒として 1.42** が返る（初版が `#404040` に割り当てた数と同値 = 台帳に焼くと必ず誤読される） | **4人** |
| 提案 B（`:focus-visible` -> `:focus`）だけでよい | **A と B を同時に入れると2行が同時に光る**（実測で再現）。いま1行なのは `:focus-visible` が発火せず**偶然**そうなっているだけ | **4人** |
| 提案 C「⛔ トリガーへフォーカスを戻さない」 | **逆。戻さないと `<body>` に落ちる**（2.4.3）。かつ `Tab` は「閉じない」のではなく**メニューの中を歩く**（3項目とも `tabIndex === 0`） | **4人** |
| 「既存の四隅 `elementFromPoint` の型に合わせる」 | `S44` の `measureHitArea()` は **`::before` 専用**。メニュー項目では `{0,0,false}` = 無条件 fail | **3人** |
| 「アクセント色でハイライトしない（却下1.70）」 | **帰属が誤り。** 1.70 は**フォーカスリング**の話。正しい理由は `--accent` の上の文字が **2.20 / 白でも 2.75** で 1.4.3 を割ること | macOS |
| 非目標「ネイティブ `Menu.popup()` は観測できない」 | **偽。** `scenarios.yml:1374` に「誤りだった」と明記があり `S91` が `Menu.prototype.popup` を差し替えて検証している | macOS |

**初版に無かった軸が2つ出た:**

- **`@media (prefers-contrast: more)` に `--surface-2` の上書きが無い** = 「コントラストを上げる」を入れた人のメニューは1ミリも良くならない（3人）
- ⭐ **メニューの枠 `--border-control` 対 選択行の塗り**（a11y。**決定打**）。項目は `width: 100%` で枠に直接接するので、
  **全面ベタ塗りのまま明るくして許される段差の上限は対メニュー面 1.133 = 実質ゼロ**（`#3a3a3a` で 2.65 / `#717171` で 1.14）

### 設計判断

- **選択面は IA 案（`#3a3a3a` + 行頭に白 2px `inset`）を採る**（a11y / 保守 / ヘビーの3人が支持）。macOS 案（`#717171` + 白文字 = 3.02）を却下した理由:
  - **閾値からの余裕が 0.02。** `--surface-3` を将来1階調動かすと**無関係に見えるメニューの関門が落ちる**
  - **`--text-primary` を保ったまま塗りだけで 3:1 に届くグレーは存在しない**（`#676767` <= 塗り <= `#717171` が空区間。a11y が総当たりで実証）
  - **高コントラストで行き止まり**（白文字前提の窓は6値しかない）。**A-10 を解けるのは IA 案だけ**
  - 面の段が6段目になる。**1日 0〜2回 x 0.5秒しか出ない部品**に段を1つ使うのは配分が逆（ヘビー）
- **インセットは採るが `margin` ではなくメニュー側の `padding` で。** macOS が提案した `margin: 0 var(--sp-1)` は
  項目が `width: 100%` の flex アイテムなので**マージンボックスが 100%+8px になり右へはみ出す**（保守）
- **マウス追随は `onMouseEnter` ではなく `onMouseMove`。** `onMouseEnter` は**レイアウト変化後の合成 mousemove でも発火**し、
  メニューは `right: 0` でタブ数に応じて x が動くので、**エージェントの出力でタブタイトルが伸びた瞬間に選択が飛ぶ**（ヘビー）
- **`Tab` / ウィンドウ blur / グローバルショートカットの3経路は `focusout` 1本に畳む**（ヘビーが3つ目を発見:
  `App.tsx:798` が capture で聞いているので、**メニューを開いたまま `Cmd+T` を押すとメニューだけ端末に貼り付いて残る**）
- **PR を3本に割った**（2本ではなく）。**画素が動く PR を1本だけにする**ため。
  混ぜると `S06-new-tab.png` が変わった理由を「色か / 行間か / 挙動か」で切り分けられない

### 関門が赤くなることの確認（PR 1。**壊し方を分けて4回**）

| 壊し方 | 何が赤くなったか |
|---|---|
| `.tab-bar__new-menu-item` の塗りを `#3a3a3a` へ + `:focus-visible` -> `:focus` | **`メニュー項目の選択面の塗り` が 1.08 -> 1.2961**（記録から動いた） |
| 同上（期待値を 1.30 に合わせて先へ通す） | **`メニューの枠（対 選択面の塗り）` が 3.71 -> 2.6499**。**塗りを枠から離さずに明るくすると自動で赤くなる**ことを実証 |
| 同上（S64 の (1) を外して先へ通す） | **`光っている行はちょうど1つ` が 1 -> 2**（フォーカスとホバーが別の行で両方光った） |
| `role="menuitem"` に `tabIndex={-1}` を足す | **`Tab` 1回目が項目1へ行かなくなった**（現状の「メニューの中を歩く」を固定できている） |

**壊した実装はすべて戻し、戻したあと green に返ることまで確認した。**

### ⚠ 書いた関門のうち1本が、書いた直後に恒真だと分かった（**外した**）

初版の S64 には「**外側クリックのあとフォーカスが `<body>` に落ちている**」も入れていた。
是正側（`mousedown` のハンドラで `newButtonRef.focus()`）を入れて赤くなるか確かめたところ、**green のままだった。**

**`mousedown` の既定動作があとからフォーカスを上書きする**ので、ハンドラ内で `focus()` を呼んでも `activeElement` は `body` のまま。
`requestAnimationFrame` で遅らせると戻るが、**戻る時刻がフレーム境界に依存する**ので、「落ちている」という否定形の assert は安定しない
（実際、rAF 版では**この行は通ったのに次の行が落ちた**）。

**否定形の characterization は、是正の実装方式によって恒真になりうる。**
是正側（フォーカスがトリガーに戻っている）を `expect.poll` で待つ形にして **PR 3 で足す**ことにし、理由を spec のコメントに残した。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 41 ファイル / **599 テスト green** |
| `S40` / `S64` 単体（`npm run build` 前置） | **green**。新規2件は予測どおり **1.08 / 3.71** |
| `make e2e-lint` | PASS=767 / **FAIL=0**（spec の追加が無いので増減なし） |
| `make e2e` | （このエントリの下で実施） |
| 撮影 | **不要**（PR 1 は**製品コード0行**。UI・CSS・DOM 構造を1つも触っていない） |
| 実機確認 | **不要**（同上。画面に出るものを1つも変えていない） |

### 次に再開するとき最初に読むべきこと

- **改訂版の案は `/private/tmp/.../scratchpad/proposal-5b-rev2.md`。** ⚠ scratchpad はセッション固有なので、
  **PR 2 に入る前に、採用した値と却下理由を `design-rules.md` へ移すこと**（Issue 固有の内容は移さない）
- **次は PR 2（見た目）。** 入れるもの:
  `line-height: var(--lh-normal)` / `:focus` 1本 + `onMouseMove` -> `focus()` / `--surface-menu-active: #3a3a3a`（**`--surface-menu-hover` ではない**。
  `:hover` の宣言が1つも残らないので名前が腐る。保守の指摘） / `box-shadow: inset 2px 0 0 var(--focus-ring)` /
  **メニュー側 `padding: var(--sp-1)` + 項目の左右 padding を `--sp-2` へ**（枠と塗りを接触させない。4+8=12 で文字の x は不変のはず = **実測で確かめる**） /
  `@media` 側に `--surface-menu-active: #525252`（⛔ `var(--surface-tab-active)` とエイリアスしない。**分母が違う**）
- **PR 2 で更新する関門**: `S40` の2件（塗り 1.30 / 枠は 3:1 を維持）、`S41` に3点（⚠ **`S41:125-127` は `toBeGreaterThan`（厳密大なり）なので、
  `@media` を変えずに項目だけ足すと必ず赤**）、`S44` に「**3項目の高さが等しい**」（⛔ `measureHitArea` は使えない。`getBoundingClientRect` の別関数が要る）
- **`docs/images/S06-new-tab.png` は必ず変わる。** 理由は3つあり、**うち1つは色ではない**:
  (a) `TabBar.tsx:249` の `focus()` は**クリック経路でも走る**ので `:focus` に変えた瞬間に先頭項目が光る
  (b) `line-height` でメニュー高さ 104 -> 112 (c) `screenshots.spec.ts:628-633` の吹き出しが `.tab-bar__new-menu` 基準なので一緒に動く
- **PR 3（挙動・画素ゼロ）**: `focusout` で閉じる + `tabIndex={-1}` + 外側クリックのフォーカス復帰（**`expect.poll` で是正側を待つ**）
- **`known-issues.md` の 2番に、この周に入れなかったもの（キー等価の併記ほか）を全部書いた。** ⛔ 起票しない

---

## 2026-08-05 - 引き継ぎ周5-b PR 2: 選ばれている項目が見えるようにした

### 実施内容

| 変えたもの | 内容 |
|---|---|
| `--surface-menu-active` を新設 | 既定 `#3a3a3a`（対メニュー面 1.30）/ `@media (prefers-contrast: more)` で `#525252`（1.89） |
| `:focus-visible` -> **`:focus` 1本** | `:hover` は**消した**。roving focus では DOM フォーカスが選択カーソルそのもの |
| `box-shadow: inset 2px 0 0 var(--focus-ring)` | **選択状態を担うのはこの白線**（対選択面 11.37）。塗りは冗長な補強 |
| メニュー側 `padding: var(--sp-1) 0` -> `var(--sp-1)` / 項目 `var(--sp-2) var(--sp-3)` -> `var(--sp-2)` | 塗りを枠から離す。**4+8=12 なので文字の x 座標は不変** |
| `line-height: var(--lh-normal)` | 3項目の高さを揃える（34 / 30 / 30 -> **34 / 34 / 34**） |
| `border-radius: var(--radius-md)` | Big Sur 以降の NSMenu のハイライト形 |
| `TabBar.tsx` に `handleNewMenuItemMouseMove` | ポインタが乗った項目へ**選択そのものを移す**。⛔ `onMouseEnter` にしない |
| 関門 | `S40` を 1.30 / 3.43 へ、`S41` に1件、`S44` に「3項目の高さが等しい」、`S64` に白線と幾何の検査 |

### ⚠ PR 1 で作った関門のうち1本が、**測る対象そのものを間違えていた**

PR 1 の `メニューの枠（対 選択面の塗り）= 3.71` は、**塗りを枠から離す是正を入れても 2.65 のまま**だった。
`measureContrast` は**色だけを比べる**ので、**2つが実際に接しているかを1ミリも見ていない**。

`spec-writing-traps.md` の「測る対象そのものが違う」そのもの。**主語を2つに割った**:

| 見るもの | 置き場 |
|---|---|
| 枠が、**いま枠に接している面**に対して見えるか（3.43） | `S40`（色） |
| 現在項目の塗りが**枠に接していない**こと | **`S64`（幾何）**。項目の矩形がメニューの内側に 4px 入っていることを測る |

**色の関門だけでは「離れたこと」を証明できない。** 幾何を見る検査が要る。

### 関門が赤くなることの確認（**壊し方を6通りに分けた**）

| 壊し方 | 赤くなった関門 |
|---|---|
| `line-height` を消す | **S44「3項目の高さが揃っていない」**（1種類 -> 2種類） |
| メニューの水平 padding を戻す | **S64 の幾何**（inset 4 -> 0） |
| `@media` のトークンを消す | **S41「高コントラストで上がっていない」**（1.2961 -> 1.2961） |
| 白線を消す | **S64 の `boxShadow`**（`... inset` -> `none`） |
| `:hover` の併記を戻す + `onMouseMove` を外す | **S64「光っている行が1つ」**（1 -> 2） |
| `onMouseMove` だけ外す | **S64「選択が乗り移っている」**（true -> false） |

⚠ **`:hover` を戻すだけでは赤くならない**（`onMouseMove` が同じ行へフォーカスを移すので2状態が重なる）。
**2つの assert の主語が違うので、順序を入れ替えると片方が到達不能になる。** spec にそう書いた。

### ⚠ 実機確認で「復元したのに再ビルドしていない」を踏んだ

関門の壊し確認のあと製品コードを戻したが、**`npm run build` を挟まずに Electron を起動した**ので、
実機で見ていたのは**壊し方Fのビルド**（`onMouseMove` を外したもの）だった。
「実機では選択が乗り移らない」という**存在しない不具合を10分追いかけた**。

`spec-writing-traps.md` の「壊した実装がビルドに入っていない」の**裏返し**。
**壊すときだけでなく、戻すときも `npm run build` が要る。**

### 実機確認（agent-browser + CDP。**本番と同じ WebGL レンダラ**）

| 測ったもの | 結果 |
|---|---|
| 3項目の高さ | **34 / 34 / 34**（是正前は 34 / 30 / 30） |
| 項目の幅とインセット | 150px / 左右とも枠から **4px** 内側 |
| メニュー | 160 x 112（是正前 160 x 104）/ 面 `rgb(40,40,40)` / 枠 `rgb(122,122,122)` |
| 先頭項目（マウスで開いた直後） | 塗り `rgb(58,58,58)` + `rgb(255,255,255) 2px 0px 0px 0px inset`。**是正前は塗りが1つも当たっていなかった** |
| 光っている行 | **1つ** |
| ポインタを別の項目へ | 選択がそこへ移り、光る行は1つのまま |

⚠ **`npx agent-browser mouse move` では `onMouseMove` が発火しなかった**（`:hover` は付くのに React のハンドラに届かない）。
Playwright の `hover()` では発火する。**実機での確認は `dispatchEvent(new MouseEvent('mousemove', {bubbles: true}))` で行った。**
`known-issues.md` に記録した。

### 画像（**13枚中、画素が変わったのは S06 の1枚だけ**）

| 画像 | 判定 |
|---|---|
| `S06-new-tab.png` | **差分 bbox=(398, 39, 616, 218)**。メニューの矩形そのもの。先頭項目に塗りと白線が出た + 高さが 104 -> 112 になり注釈の吹き出しが下がった = **この周の成果物そのもの** |
| 残り12枚 | **画素差ゼロ**（バイト差のみ）なので戻した。`.tab-bar__new-menu*` は `{newMenuOpen && ...}` でしか DOM に無く、**メニューが写らない画面には波及しようがない** |

比較は **RGB に落として** `ImageChops.difference(...).getbbox()`（⛔ RGBA だと Pillow 9.5+ が誤判定する）。
**S06 が差分を出したことで、レシピが「差が無い」側に倒れていないことを較正できている。**

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 41 ファイル / **599 テスト green** |
| `S40` / `S41` / `S44` / `S64` 単体 | **green**（1.30 / 1.30 -> 1.89 / 3.43） |
| `make css-substitution-check` | **意図どおり FAIL**。出た行は**トークンの追加と、この周で変えた宣言だけ** |
| `make e2e-screenshots` | **13枚中 画素差は S06 の1枚のみ**（残り12枚は戻した） |
| 実機確認 | 実施（上表） |
| `make e2e` | （このエントリの下で実施） |

⚠ `make e2e-screenshots-check` は**回していない**。撮った直後は比較元が古い（`issue-179/known-issues.md` 2番）ので、
**上の RGB 画素比較のほうが直接的**。

### 次に再開するとき最初に読むべきこと

- **次は PR 3（挙動・画素ゼロ）。** `focusout` で閉じる（`Tab` / ウィンドウ blur / **グローバルショートカット**の3経路が1本で塞がる）
  + `role="menuitem"` に `tabIndex={-1}` + 外側クリックでトリガーへフォーカスを戻す
- **PR 3 で S64 の characterization（3）を「直った」側へひっくり返す。** いま「`Tab` 1回目が項目1へ進み、
  3回目で設定へ抜けても `aria-expanded="true"` のまま」を固定している
- ⛔ **外側クリックのフォーカス復帰は、否定形ではなく `expect.poll` で是正側を待つ形で書く**
  （`mousedown` のハンドラ内の `focus()` は既定動作に上書きされる。PR 1 で実測済み）
- **`--fs-md` 化はこの周に入れていない**（画像が落ちたときの主語を増やさないため）。`known-issues.md` 2番

---

## 2026-08-05 - 引き継ぎ周5-b PR 3: メニューが開きっぱなしになる経路を塞いだ

### 実施内容

- `focusout` を1本購読し、**フォーカスがメニューの外へ出たら閉じる**
- `role="menuitem"` 3つに **`tabIndex={-1}`**（APG の roving tabindex）
- 外側クリックで閉じたあと、**フォーカスが `<body>` に落ちていたらトリガーへ戻す**（2.4.3）
- `S64` の characterization 3件を「直った」側へひっくり返し、経路ごとの assert に書き直した

### 設計判断

- **`keydown` で `Tab` を捕まえる形にしなかった。** 閉じる（React の unmount）とブラウザが
  次のフォーカス先を決めるのが競合し、フォーカスが `<body>` に落ちうる。
  **`focusout` はフォーカスが移ったあとに発火する**ので、その競合が原理的に起きない
- **`focusout` 1本で2経路が塞がる**（`Tab` / グローバルショートカット）。
  ⚠ ヘビーユーザーが見つけた3つ目の入口（`App.tsx:798` が capture で聞いているので、
  **メニューを開いたまま `Cmd+T` を押すとメニューだけ端末の上に残る**）も同じ1本で消える
- **外側クリックの復帰は `requestAnimationFrame` で1フレーム待つ。**
  `mousedown` のハンドラ内で `focus()` を呼んでも**既定動作があとから上書きする**（PR 1 で実測）。
  待ってから、**まだ `body` に居るときだけ**戻す（端末など別の要素がフォーカスを取っていたら奪わない）

### ⛔ ウィンドウの非アクティブ化は**意図的に見送った**（検証手段が無い）

a11y は「この周でやらないと嘘になる」と判定していたが、**関門を1本も書けないので入れなかった。**

| 試したこと | 結果 |
|---|---|
| `focusout` で塞がるか | **塞がらない**。OS のフォーカスが外れても DOM のフォーカスは項目に残る（実機で Finder をアクティブにして確認） |
| `window` の `blur` を購読 -> E2E で `BrowserWindow.blur()` | **Renderer に届かない**。`isFocused()` が最初から false（`isVisible()` も false）= **一度も OS フォーカスを持たないので外れようがない** |
| CDP で起動した実機 | `document.hasFocus()` が **false のまま**。`osascript` で前面に出すのは **Apple Events の権限が無い**（-1743） |

⛔ **`dispatchEvent(new Event('blur'))` で済ませない。** 自分で作った偽物なので、
**Chromium が本当に blur を投げるか**を1ミリも確かめない = 恒真の関門。

**正攻法は Main の `win.on('blur')` を IPC で渡すこと**（Main 側なら `app.evaluate` から観測できる）。
Contract の変更なので `/electron-ipc` を通す別の周にする。`known-issues.md` 4番。

### 関門が赤くなることの確認（**4通り。経路ごとに1回ずつ**）

| 壊し方 | 出た赤 |
|---|---|
| `focusout` の購読を外す（`tabIndex` は残す） | **`Tab` でメニューが閉じない**（`toHaveCount(0)` が 1） |
| `tabIndex={-1}` を外す（`focusout` は残す） | **同上**（`Tab` がメニューの中を歩くので `relatedTarget` がメニュー内 = 閉じない） |
| 外側クリックのフォーカス復帰を外す | **`expect.poll` が `""`（body）のまま** |
| （PR 2 で書いた `window blur` の関門を、購読を外して試した） | 赤くはなったが、**そもそも本物の blur を作れていない**と分かったので関門ごと外した |

### 実機確認（agent-browser + CDP）

| 経路 | 結果 |
|---|---|
| `Tab` 1回 | **メニューが閉じ、`aria-expanded="false"`、フォーカスは「設定を開く」** |
| 開いたまま `Cmd+T` | **タブが 1 -> 2 に増え、メニューは閉じた** |
| 外側クリック | ⚠ **確認できない**（下記） |

⚠ **`agent-browser` のマウス操作はページに DOM イベントを1つも届けない。**
プローブで `document` の `mousedown`（capture）が **0回**だった。`:hover` は付くのに、である。
**周5-b で2回この罠に落ちた**（1回目は `onMouseMove`）。`known-issues.md` 3番に一般化して記録した。
**外側クリック経路の担保は E2E（Playwright のクリックは届く）だけ。**

### `make e2e` が1回赤くなった（S79）。切り分けて負荷由来と判定した

| 測ったこと | 結果 |
|---|---|
| フル `make e2e`（1回目） | **S79 が failed**（exit 1）。100 passed / 6 flaky |
| S79 単体 | 1回目は **`electronApplication.firstWindow` の 15s タイムアウト**、**リトライで green** |
| **同じコミットでフル `make e2e`（2回目）** | **102 passed / 5 flaky（exit 0）。S79 は緑** |

**#179 周4-0 が S79 に入れた「OS 側 / アプリ側」の主語を分ける assert には到達していない**
（落ちたのはその手前の起動そのもの）。**アプリの回帰なら別のメッセージが出る**ので、
`launchApp` の起動タイムアウト = マシン負荷、と読める。
周4-0 の教訓どおり「`main` でも落ちる」で止めず、**同じコミットが緑になるところまで**見た。

### 教訓

- **⚠ 「実機で動かない」と言う前に、道具がイベントを届けているかを確かめる。** 2回とも道具のほうだった
- **⚠ 検証できない是正は入れない。** a11y が必須と判定した項目でも、
  **関門を書けないなら「見送った理由」を残すほうが誠実**（入れると「直した」という記録だけが残る）
- **⚠ 壊し確認のあとは、戻したあとにも `npm run build` が要る**（PR 2 で踏んだので今回は毎回入れた）

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 41 ファイル / **599 テスト green** |
| `S40` / `S41` / `S44` / `S64` | **4本とも green** |
| `make e2e` | **1回目は赤**（S79 が failed / exit 1）。**同じコミットで再実行して 102 passed / 5 flaky（exit 0）** |
| `make e2e-lint` | PASS=767 / **FAIL=0** |
| 撮影 | **不要**（画素は動かない。`focusout` / `tabIndex` / フォーカス復帰はどれも見た目を変えない） |
| 実機確認 | 実施（上表。**外側クリックだけ道具の制約で確認できず**） |

### 次に再開するとき最初に読むべきこと

- **引き継ぎ周5-b はこれで完了**（PR 1 = #202 / PR 2 = #203 / PR 3）。**次は引き継ぎ周7**
- **周7 の材料は `.claude/workspace/issue-179/known-issues.md` の 11番。** 要点:
  - `scrollIntoView` / `scrollLeft` は `src/renderer/` に **0件**
  - **キーボード経路は既に自動スクロールしている**（`focus()` の副作用）。壊れているのは
    **DOM フォーカスを動かさない経路** = `Cmd+J` / `Cmd+E` / `Cmd+1-9` / `Cmd+Shift+]`
  - ⚠ **`S54` が12枚で緑なのは、この不具合が無いことの根拠にならない**
  - ⛔ **`block: 'nearest'` を必ず付ける**（付けないと #170 と同じ「タブが上に押し出される」を再生産する）
  - 「タブバーがスクロールできることが画面に出ていない」も同時に扱う。⛔ **白は使わない**（`--border-control` が適切）
- **周7 は `styles.css` を触るなら `/design-review` が要る**（スクロールバーの見た目を足すなら該当する）
- **`known-issues.md` 4番（ウィンドウ非アクティブ化）は、周を増やすときの第一候補**

---

## 2026-08-05 - 引き継ぎ周7（前半）: 選ばれたタブが可視域へスクロールインする

### 計画ゲートで測り直した前提（材料は `issue-179/known-issues.md` 11番）

| 材料の記述 | 実測 |
|---|---|
| `scrollIntoView` / `scrollLeft` は `src/renderer/` に 0件 | **そのとおり**（scrollbar 指定も 0件） |
| `tabButtonRefs` は既存 | **そのとおり**（`TabBar.tsx:219` の `Map<string, HTMLButtonElement>`） |
| 壊れているのは DOM フォーカスを動かさない経路 | **そのとおり**。`App.tsx` の `setActiveTabId` を呼ぶ経路が4つ（`Cmd+1-9` / `Cmd+Shift+]`・`[` / `Cmd+J` / 通知クリック）で、**どれも `focus()` を呼ばない** |
| ⚠ `Cmd+1-9` | **タブの選択**（サイドバーのパネル切替は別の修飾）。`S54` が `Meta+1` で1枚目を選んでいるので確認できる |

### 実施内容

- **E2E S97 を新設**（3経路 + 前提の固定）
- `TabBar.tsx` に `useEffect` 1本（`activeTabId` が変わったら `scrollIntoView({ block: 'nearest', inline: 'nearest' })`）

### 設計判断

- ⛔ **`focus()` で代用しない。** 選択とフォーカスは別物で、`Cmd+J` で飛んだ先では
  **端末にフォーカスが要る**（飛んだ直後に打てることがこのショートカットの価値）
- ⛔ **`block: 'nearest'` を省かない。** 既定は `'start'` で、縦にもスクロールしようとする

### 関門が赤くなることの確認

| 壊し方 | 出た赤 |
|---|---|
| `useEffect` の中身を消す | **「Cmd+1 で選んだタブが画面外に着地している」**（`viewport ratio 0`） |
| 同上 + (1) の assert を外して先へ通す | **「Cmd+Shift+] で送った先のタブが画面外に着地している」** |
| 同上 + (1)(2) を外して先へ通す | **「新しく開いたタブが画面外にある」** |
| **`block: 'nearest'` を外す**（既定の `'start'` に戻す） | ⚠ **赤くならなかった**（下記） |

### ⚠ 恒真だった検査が2つ。1つは直し、1つは承知で残した

**(a) 直した: 「Cmd+Shift+] で送った先が見えている」が恒真だった。**
初版は (1) の直後に測っていたので、**スクロール位置が右端のままで12枚目は最初から見えていた**。
実装を丸ごと消しても緑になる。**測る前に `scrollLeft = 0` へ戻し、
「12枚目が見えていない」ことを先に固定する**形に直した（`spec-writing-traps.md` の
「検査は正しいが、その条件を踏んでいない」）。

**(b) 承知で残した: 「タブバーが縦に押し出されていない」。**
`block: 'nearest'` を外しても**赤くならないことを実測した**。いまの DOM では縦に送れる余地が無い
（`body { overflow: hidden }` でページはスクロールせず、`.tab-bar__tabs` の中身の高さは
コンテナと同じ）。**Issue #170 の幾何がもう一度生まれた日にだけ意味を持つカナリア**として残し、
**恒真であることを spec と `scenarios.yml` の両方に明記した**（黙って持たない）。

### 実機確認（agent-browser + CDP）

| 操作 | `scrollLeft` | 結果 |
|---|---|---|
| タブを12枚開く（`scrollWidth` 1080 / `clientWidth` 855 = 溢れている） | **212** | **新しいタブが見えている**（是正前は 0 のまま） |
| `Cmd+1` | 212 -> **26** | 1枚目が可視域に入る |
| `Cmd+Shift+]` を11回 | 26 -> **212** | 12枚目が可視域に入る |
| どの時点でも `.tab-bar` の `top` | **0 のまま** | 縦に押し出されていない |

⚠ **実機の CDP セッションは、アプリを起動し直したら `agent-browser connect` もやり直す。**
古いターゲットに繋がったまま `Meta+t` を送り、**「タブバーが無い」と誤読した**。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 41 ファイル / **599 テスト green** |
| `S97` / `S54` | **両方 green**（S54 = キーボード経路が壊れていないこと） |
| `make e2e-lint` | PASS=775 / **FAIL=0**（S97 のぶん 767 -> 775） |
| `make e2e` | （このエントリの下で実施） |
| 撮影 | **不要**（既定の1枚では溢れないのでスクロール位置は動かない。`docs/images/` は全13枚がタブ 1〜3枚） |
| 実機確認 | 実施（上表） |

### 次に再開するとき最初に読むべきこと

- **周7 の後半（「タブバーがスクロールできることが画面に出ていない」）が残っている。**
  `::-webkit-scrollbar` / `scrollbar-*` は `src/renderer/` に **0件**。
  ⚠ **`styles.css` を触るので `/design-review` の起動条件に該当する**（計画確定前に通すこと）
  - ⛔ **白は使わない**（`--focus-ring` の白 = 選択中、という唯一のルールが崩れる）。`--border-control` が適切
  - ⚠ #170 で「+」がスクロール域の外へ出たので、**唯一の手がかり（+ が見えたら右端）が消えている**
- そのあとは **#180 本体の周1**（#139 / #146。**値・振る舞いを1つも変えない周**）

---

## 2026-08-05 - 引き継ぎ周7（後半）: design-review で「何もしない」に決まった

### 実施内容

- 「タブバーがスクロールできることを画面に出す」案（A = 端のフェード / B = 常時スクロールバー / C = 何もしない）を書き、
  **`/design-review` を3ペルソナ（macOS / ヘビー / a11y）に回した**
- **3人とも独立に C を出した**ので採用。**コードは1行も書いていない**
- 判断を根拠ごと `design-rules.md` へ記録し、拾えた指摘を `known-issues.md` 5番へ

### 3人が別々の軸で C に到達した

| ペルソナ | 却下の根拠 |
|---|---|
| **macOS** | **AppKit の `NSTabBar` を実測**（`hiddenInset` と同じ styleMask のウィンドウを N 枚タブ化してビュー木を読む）。**`_NSInvisibleScroller` の frame が `y=28 h=0`** = **Apple は自分のタブバーに 1px も割いていない**。溢れの手がかりは**trailing 端で痩せていくタブ**（12枚で 121 -> 64 -> 33 -> 28）。⚠ **AppKit も「まず縮める -> 床に当たったらスクロール」で、床は 120pt。このアプリの床は 90px で、12 x 90 = 1080 = 前半で実測した `scrollWidth` と完全に一致する** = 構造としては既に macOS 側に居る |
| **ヘビー** | **削減 0手/日。** 到達経路を全部数えたら4重に冗長化されていた（`Cmd+1-9` / **折り返す** `Cmd+Shift+[` / `Cmd+J` / 通知クリック）。⚠ **12枚時のタブは `min-width: 90px` に張り付いていて、タイトルに残るのは約 30px（4文字）**。16px のフェードはその **53%** を滲ませる |
| **a11y** | **どの SC にも当たらない**（唯一当たっていた 2.4.11 は PR #205 とブラウザの `focus()` の副作用で既に充足）。B は **2.5.8 を新たに割る唯一の案**（`::-webkit-scrollbar` に寸法を書いた時点で「User agent control」免除が外れる）。A は **1.4.11 を構造的に満たせない**（最濃部が背景そのもの = **1.00**）うえ、**既に成立している 1.4.3 / 2.4.7 を削る**（α 0.26 で本文が 4.5:1、α 0.67 で白リングが 3:1 を割る） |

### 案の実装そのものが成立しなかった（2人が独立に指摘）

`.tab-bar__tabs` は `overflow-x: auto` のスクロールコンテナなので、
**そこへ絶対配置した `::before` / `::after` はコンテンツと一緒に流れて画面外へ出る**
（a11y の実測: `scrollLeft = 768` で `left = -184`）。`right: 0` は**スクロール内容の右端**を指す。

⚠ **案が前例に挙げた `.history-item__actions::before` は移植できない**（`.history-item` はスクロールコンテナではない）。
**#170 の「`overflow-x: auto` は `overflow-y` も `auto` にする」と同じ属性が原因の、2つ目の罠。**

### ⚠ レビュアーの主張を1つ実測で覆した

a11y は「**2.4.11 を守っているのはブラウザの副作用だけで、フォーカスの経路は誰も検査していない。
S97 に10行足すべき最優先項目**」と書いたが、**誤り**だった。

`tabButtonRefs.current.get(next.id)?.focus()` を `focus({ preventScroll: true })` に変えたところ、
**`S54` が赤くなった**（`viewport ratio 0`）。**関門は既にある。**

**専門領域の指摘でも、関門の有無は自分で壊して確かめる。**

### 設計判断

- **C を採ったが「何もしない」で終わらせない。** macOS が出した第4案（**「ウィンドウ」メニューに
  タブ一覧を出し、`Cmd+1-9` をそこに載せる**）が本命で、**36px を1px も使わない**。
  ⚠ ただし `menu.ts` は **#180 周3（#152）と同じ面**なので、順序を決めてから着手する -> `known-issues.md` 5番
- ⛔ **この判断を固定する関門を書かない。** 「手がかりが出ない」を固定する検査は**永久に赤くならない**
  （3人とも独立にこれを指摘した）。書くならメニュー側に置く
- **`scroll-padding-inline` は見送った。** a11y の推奨だが**WCAG の要求ではなく**
  （リングは `outline-offset: -2px` の内側描画なので切れない）、「何もしない」の判断と揃えた

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | （このエントリの下で実施） |
| `make e2e` | **省略可**（下記の判定コマンドで確認） |
| 撮影 | **不要**（画素が動く変更が1つも無い） |
| 実機確認 | **不要**（同上） |

**触ったのは `.claude/**` だけ**なので `make e2e` を省いた。判定は主観ではなくコマンド:
`git diff --name-only origin/main...HEAD | command grep -vE '^(\.claude/|docs/|README\.md$|CLAUDE\.md$)'` が**空**。
⛔ `make check` は省いていない（`lint-skills.sh` が `.claude/**` を読んでいる）。

### 次に再開するとき最初に読むべきこと

- **引き継ぎ周5-b / 周7 はどちらも完了。** ここから **#180 本体**へ入る
- **次は #180 周1（#139 / #146）**。⚠ **値・振る舞いを1つも変えない周**:
  - `#139` = `S40` に `.pane-header` の2エントリ（`mainTargets` と `expected` の**両方**に足す。
    `Object.keys` の突き合わせがある。背景を測るなら `against: '.terminal-stack'` が必須）
  - `#146` = E2E ハーネスに PTY の spawn を失敗させるモードを足し、`describeSpawnError` 経路を `S55` で検証する
- ⚠ **Issue 本文の周2（#140）と周8 の一部（#168）は、対象 Issue が既に closed。**
  周2 は `grep -rn "'終了'" src/renderer/src/` で実コードの残存を確認し、**残っていなければ周ごと畳む**
- **周を増やす候補は `known-issues.md` の 2番・4番・5番**（キー等価の併記 / ウィンドウ非アクティブ化 /
  ウィンドウメニューのタブ一覧 / タブの並べ替え / cwd の2秒古さ）

---

## 2026-08-05 - #180 周1: 検査を足す（#139 / #146）。**値・振る舞いを1つも変えない周**

### 計画ゲートで測り直した前提

| Issue 本文の記述 | 実測 |
|---|---|
| #139「`.pane-header` を S40 の測定対象に入れる」 | **そのとおり未登録**。`S40` に `pane-header` は0件。`.pane-header` は `background: var(--surface-2)` / `color: var(--text-secondary)` |
| #146「**E2E ハーネスに PTY の spawn を失敗させるモードを足し**」 | ⚠ **ハーネスに口は既にあった**（`LaunchOptions.config` = `config.json` の上書き）。だが**その口を使っても spawn は失敗しない**（下記） |

### #139: ペインヘッダの2箇所を S40 に入れた（**一度も測っていなかった**）

| 項目 | 実測 | 札 |
|---|---|---|
| ペインヘッダの文字（対ヘッダの塗り） | **6.69** | `pass` |
| ペインヘッダの塗り（対ターミナル背景） | **1.05** | `fail` |

**塗り 1.05 は承知で `fail` のまま固定した。** `--surface-2`(#222222) をターミナル背景(#1e1e1e)の上に
置いた値で、**帯としてはほぼ見えていない**。ただし「選択中タブの塗り 1.23」と同じ型で、
**帯の境界は必要な情報を運んでいない**（分割中かは2枚のペインが見えることで、種別と cwd は**文字**が担う）。
塗りを明るくすると**ターミナルの最初の行の隣に明るい帯が走る**ことになり、
#56 で「ヘッダがターミナルの1行目を隠した」を直した判断と衝突する。

**関門が赤くなることの確認（3通り）:**

| 壊し方 | 出た赤 |
|---|---|
| ヘッダの文字色を `--text-tertiary` へ | **文字 6.69 -> 5.2448** |
| ヘッダの塗りを `--surface-3` へ | **文字の側が動く**（塗りが変われば文字の実効背景も変わる） |
| ⭐ **`against: '.terminal-stack'` を外す** | **塗りが 1.05 -> 1.00**。`effectiveBackground` が自分自身の不透明な塗りを拾う = **`against` が効いていることの証明** |

### #146: `describeSpawnError` は E2E から踏めない。**実測で確定した**

| 試したこと | 結果 |
|---|---|
| `config.shell` を存在しないパスに | **投げない。**「definitely-not-a-shell が終了しました（コード 1）」= **終了通知**の経路 |
| `config.shell` をディレクトリ（`/tmp`）/ 非実行ファイル（`/etc/hosts`）に | 同上 |
| cwd を消してから `Cmd+T` | 同上（**タブは生まれ**、シェルがコード 1 で終了する） |
| preload の `api.pty.spawn` を差し替える | **`contextBridge` が凍結。`Cannot redefine property: spawn`** |
| Main が `AI_TERMINAL_E2E_*` を読んでいるか | **1つも読んでいない**（`src/main/` `src/shared/` `src/preload/` に0件） |

**原因は node-pty の性質**で、exec の失敗は**子プロセス側で非同期に起きる**
（`S11-cli-missing.spec.ts` が同じことを実測して記録していた）。
Main が同期的に投げるのは「不正な pty:spawn リクエスト」と forkpty そのものの失敗だけで、
**どちらもハーネスからは作れない。**

**ユーザーの判断で「純粋関数へ切り出して unit で固定」を採った。**
`src/renderer/src/tabs/spawnErrorCopy.ts` へ切り出し、`test/unit/spawn-error-copy.test.ts`（4本）で固定。

- ⛔ **Main に E2E 専用の env フラグを足す案は採らなかった**（製品コードに test-only の分岐を入れる前例を新設することになる）
- ⛔ **`dispatchEvent` 的な細工で「到達できたことにする」形も採らない**
- **このリポジトリが8回採ってきた形に合わせた**（`shouldSendResize` / `computeYourTurnSince` /
  `rovingTabindex` / `passesModifierGate` / `paneTree` / `paneHeader` / `paneSplitter` /
  `chromeTextRemainsReadable`）。**繰り返し必要になるということは、これが既定の作法**

**unit が赤くなることの確認（3通り）:**

| 壊し方 | 落ちたテスト |
|---|---|
| `kind` の出し分けを消す | 「claude / gemini が見つからないときは、次の行動になる文言を出す」 |
| シェルにも PATH の文言を出す | 「シェルは『見つからない』でも PATH の話にしない」 |
| 正規表現の `i` を外す | 「claude / gemini が…」（`SPAWN GEMINI Enoent` で落ちる） |

### 設計判断

- **文言を `kind` で分ける理由をコードに書いた。** シェルは決定順が
  `config.shell -> $SHELL -> /bin/zsh` なので「見つからない」は PATH の話にならない。
  **PATH を確認させると外れた指示になる**
- **`Error` 以外で reject されたときの挙動もテストで固定した。** オブジェクトは `String()` で
  `[object Object]` になり **ENOENT を含まないので CLI 不在の分岐に入らない**。
  いまの Main は必ず `Error` にして投げるので実害は無いが、**そこを変えたらこのテストが落ちる**

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 42 ファイル / **603 テスト green**（599 -> 603） |
| `S40` | **green**（新規2件は 6.69 / 1.05） |
| `make e2e` | （このエントリの下で実施） |
| 撮影 | **不要**（**値・振る舞いを1つも変えていない**。`useTabs.ts` の差分は関数の移動のみ） |
| 実機確認 | **不要**（同上） |

### 次に再開するとき最初に読むべきこと

- **次は #180 周2（#140 = 状態語「終了」の一元化）。** ⚠ **対象 Issue は既に closed。**
  着手前に `grep -rn "'終了'" src/renderer/src/` で実コードの残存を確認し、
  **残っていなければ周ごと畳む**（Issue 本文の周の表は起票時点の認識で、実コードのほうが新しい）
- **#146 は「E2E で検証する」を満たしていない。** close するときは
  **「node-pty は exec 失敗を非同期に返すので、この経路は E2E から到達できない。判定は unit で固定した」**を
  Issue に書き戻すこと（黙って閉じない）

---

## 2026-08-05 - #180 周2 を畳み、周3（#152 / #145）を2本の PR で回した

### 周2（#140 = 状態語「終了」の一元化）は**周ごと畳んだ**

前提を実コードで測り直したところ、**既に解決済み**だった（#140 は 2026-08-05 に COMPLETED で closed）。

| #140 の完了条件 | 実コード |
|---|---|
| 「終了」の語が1箇所で定義されている | `src/renderer/src/tabs/tabExitCopy.ts` の `exitWord` / `exitDetail` |
| 3つの利用箇所がそれを参照している | `TabBar.tsx:85`（`tabExitCopy`）/ `paneHeader.ts:35`（`exitDetail`）。**裸のリテラルは0件** |
| unit で検証されている | `test/unit/tab-exit-copy.test.ts`（13本。「可視の語が detail の先頭に含まれる」まで固定） |

`grep` に残る `終了しました（コード N）` は `useTerminal.ts` のスクロールバック文と
`App.tsx` の通知バナー文で、**#140 の対象3箇所ではない**（状態語ではなく文）。

### 周3 の前提も2つとも覆った（**計画ゲートで実測**）

| Issue 本文 | 実測 |
|---|---|
| #152「E2E を1本足す。設定ウィンドウを開いた状態で…本体のタブ数が増えないこと」 | ⛔ **原理的に書けない**（下記） |
| #145「`limitations.md` に手動確認の方針が書かれている」 | ⚠ **既に書かれていた**（#144 の周が #145 を参照しつつ書いていた）。残り2つだけが未達 |

### ⭐ E2E は OS のフォーカスを**構造的に**持てない（今回いちばん大きい発見）

`getFocusedWindow()` を見る判定は、**E2E の中で前提条件を作れない。**

| 測ったこと | 結果 |
|---|---|
| 設定ウィンドウを開いた状態の `getFocusedWindow()` | **`null`** |
| `settingsWin.focus()` のあと | **`null`** |
| `app.focus({ steal: true })` を挟んだあと | **`null`** |
| 全ウィンドウの `isFocused()` / `isVisible()` | **すべて `false`** |
| 原因 | `harness.ts:571-589` が **`BrowserWindow.prototype.focus` / `show` / `showInactive` / `moveTop` を noop へ差し替え**、全ウィンドウを `hide()` し `dock.hide()` している |

**ハーネスを直す道は採らない。** 非表示化は「開発者のエディタからフォーカスを奪わない」ための
意図的な設計で、外すと**テストの本数だけウィンドウが前に出る**。

⚠ **`known-issues.md` 4番（ウィンドウ非アクティブ化）と同じ壁だが、別の面。**
4番は「blur が届かない」、こちらは「focus が誰にも無い」。**同じ原因（ハーネスの非表示化）から出ている。**

**通常起動の Electron では正しく追従することは別途実測した**（スクラッチの2ウィンドウ・`parent` 付き。
子が生成時にフォーカスを取り、`親.focus()` で親へ戻る = 設定ウィンドウそのものの状況）。
**この確認をしないと「本番でも `null` かもしれない = 修正が無意味」を見落とす。**

### 設計判断

- **判定を `src/main/menu-action-routing.ts` の純粋関数へ切り出した**（このリポジトリで **10 例目**）。
  周1 の `describeSpawnError` とまったく同じ形
- **`focusedWindowId === null` は「送る」に倒した。** 誰もフォーカスを持たないのは
  (a) E2E ハーネス (b) アプリが非アクティブ、のどちらかで**「設定からの誤操作」ではない**。
  ⛔ ここを `focus-main` に倒すと **S60 / S61 / S77 / S85 がまとめて死ぬ**（実際に機能を殺す）
- **本体を前に出すが、送らない**（ユーザーの判断）。「前に出してから送る」は
  **見えるようになるだけで破壊的な操作が止まらない**
- **S36 をパッケージ版スモークに足さない。** `isDev()` の入力は `ELECTRON_RENDERER_URL` の有無だけで、
  通常レーンが既に false 側（= 本番のメニュー）を見ている。メニュー構築は asar / 署名 / preload 解決の
  どれにも触れない。スモーク4本は「**パッケージングでしか壊れない層**」を狙って選ばれている
- **元 Issue に無い穴を1つ足した**: `isDev()` の **true 側は機械が一度も通らない**。
  壊しても全レーンが green のまま `make dev` で初めて気づく

### 関門が赤くなることの確認（**unit 4通り + E2E 1通り**）

| 壊し方 | 落ちたもの |
|---|---|
| `null` を `focus-main` に倒す | unit「誰もフォーカスを持たないときは送る」 |
| 常に `send` にする | unit「設定ウィンドウ…前に出す」「id の一致だけで決める」 |
| 破棄済みガードを外す | unit「本体ウィンドウが既に無いときは何もしない」 |
| 比較を反転する | unit 3本 |
| ⭐ **`menu.ts` で `null` を別ウィンドウ扱いにする** | **E2E の `S60` / `S77`**（「フォーカス不在なら従来どおり送る」を守っているのは既存シナリオだと実証） |

**戻したあと `npm run build` を挟んで green に返ることまで確認した**（周1 の教訓）。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **608 テスト green**（603 -> 608） |
| `make e2e`（PR 1） | **104 passed / 0 failed**（flaky 4。すべて `firstWindow` 15s） |
| `make e2e`（PR 2） | **101 passed / 0 failed**（flaky 7。6本が `firstWindow` 15s、1本は S56 のプロンプト描画待ち） |
| `make e2e-lint` | **PASS=775 FAIL=0** |
| `lint-skills.sh` | **PASS=93 FAIL=0** |
| 撮影 | **不要**（`docs/images/` に差分なし。製品コードの見た目は1px も動かない） |
| 実機確認 | 起動・⌘T・設定ウィンドウの開閉が正常。**この変更自体は実機でも踏めない**（ネイティブメニューのマウス操作が要る） |

### 記録した先

- **PR #208**（#152 / close 済み）・**PR #209**（#145 / close 済み）
- `#195` に **A-6**（設定にフォーカスがある状態でメニューをマウスで選ぶ）を追記
- `.claude/skills/e2e/reference/limitations.md` に節を2つ足した
  （S36 を packaged に足さない判断 / OS フォーカスに依存する判定は E2E から踏めない）

### 次に再開するとき最初に読むべきこと

- **次は #180 周4（#153 / #149 = 設定ウィンドウ）。** ⚠ `known-issues.md` 6番を先に読む
  （**設定ウィンドウは実機でもフォーカスを持てない**ので、位置・高さの復元をどう確かめるかを計画で決める）
- `#149`（VoiceOver 検知の実効表示）は **`architecture.md` 2節の「Contract 変更が要るか」を周の入口で判定する**。
  要るなら `/electron-ipc` を通す
- 周4 は **UI の状態表現を変える**（#149 は「実効で有効になっている」の見せ方）ので、
  **計画確定前に `/design-review`**。⛔ 5体には実測済みの数値を渡し、再計算させない
- **`menu.ts` の面は空いた。** `known-issues.md` 5番の「ウィンドウメニューにタブ一覧」を
  周として起こすなら、いま衝突相手がいない

---

## 2026-08-05 - #180 周4-a: 設定ウィンドウの位置と高さを復元する（#153 / PR #211）

### 計画ゲートで測り直した前提

| Issue 本文 | 実測 |
|---|---|
| 「保存・復元の経路が無い」 | **そのとおり**。`settings-window.ts` は `window-state` を import すらしていない |
| 「保存すべきは x / y / height の3つ」 | **そのとおり**（`minWidth === maxWidth === 520`） |
| 「`{ main: ..., settings: ... }` かキーを分けるか別ファイルか判断が要る」 | **根の直下を本体のまま残し、`settings` キーを足す**形を採った。移行処理が要らない |

### ⭐ この周は E2E で本当に検証できた（周1・周3 と違う）

⛔ **「Main の永続化は E2E から見えない」と決めつけない。** `window-state.test.ts` の冒頭は
「`BrowserWindow` の実挙動（保存・復元）は E2E でもプロセスを跨げないので検証できない」と
書いていたが、**設定ウィンドウには当てはまらない**。

| 対象 | 復元を見るのに要るもの | E2E |
|---|---|---|
| 本体ウィンドウ | **アプリの再起動** | 見えない |
| 設定ウィンドウ | **開き直すだけ**（毎回 `new BrowserWindow`） | **見える** |

実測で確かめたこと（探りの spec を書いて捨てた）:

- 隠したウィンドウでも `setBounds()` は効き、`getBounds()` / `getNormalBounds()` に反映される
- **`resize` は飛ぶが `move` は飛ばない**（0回）。保存経路は `resize` 側で通る
- 開き直した設定ウィンドウは**別の `BrowserWindow`**（id が変わる）で、直す前は `{ x: 460, y: 130, height: 640 }` に戻っていた = **不具合が観測可能**

### ⚠ 関門の初版が、片方向しか見ていなかった（**5つ目の空振りの型**）

消し合いは**双方向**（本体が設定を消す / 設定が本体を消す）なのに、初版の `S98` は
「本体を動かす -> 設定を動かす -> 開き直す」の順で、**「本体の保存が設定を消す」を壊しても緑**だった。
**設定ウィンドウを閉じるときの保存が、本体の保存で消えたキーをあとから書き戻す**ため。

**順序を組み替えた**: 設定を閉じた直後に**ファイルを見る** -> そのあと本体を動かす -> 開き直す。

**教訓: 「A が B を壊す」と「B が A を壊す」は別の検査。** 片方だけ書いて
「消し合いを見た」と言わない。**どちらの向きも、壊して赤くなるまで確かめる。**

### 関門が赤くなることの確認（E2E 5通り + unit 3通り）

| 壊し方 | 出た赤 |
|---|---|
| 復元しない（`settingsWindowStateOptions()` を外す） | `x` 317 -> **460** |
| 保存しない（`trackSettingsWindowState()` を外す） | `x` 317 -> **460** |
| 設定の保存が本体を消す | 閉じた直後のファイルで `x` 120 -> **undefined** |
| ⭐ 本体の保存が設定を消す | `x` 317 -> **460**（**初版では緑だった**） |
| 画面外判定を外す | 9000 に置いたウィンドウが**そのまま 9000 で開く**（**OS はクランプしない**と実測） |
| `minHeight` クランプを外す / `width` も取り込む / 非オブジェクトを既定に落とさない | unit がそれぞれ別に落ちる |

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **613 テスト green**（608 -> 613） |
| `make e2e` | **103 passed / 0 failed**（flaky 6。**すべて `firstWindow` 15s**） |
| `make e2e-lint` | **PASS=783 FAIL=0** |
| 撮影 | **不要**（設定ウィンドウの中身は変えていない。`docs/images/` に差分なし） |
| **実機確認** | **実施できた。** 実バイナリを新しいデータディレクトリで起動 -> 設定を開いて Esc で閉じる -> `window-state.json` に**本体のキーを保ったまま `settings` が書かれた** |

⚠ 実機で設定ウィンドウを**動かす**ことはできない（`agent-browser` は OS ウィンドウを動かせない）。
確認できたのは「配線が動く」ところまで。

### 次に再開するとき最初に読むべきこと

- **次は #180 周4-b（#149 = VoiceOver 検知の実効表示）。⛔ 実装の前に `/design-review` を通す**
  （UI の状態表現を変える周。起動条件に該当）
- #149 で先に測ること（Issue 本文の指摘どおり、**設定は独立した BrowserWindow**）:
  - `SettingsPanelProps` は `{ config, onChange, onClose }` の3つだけ。`accessibilitySupport` は渡っていない
  - `src/main/accessibility.ts` の `accessibility-support-changed` は**本体ウィンドウ1枚にしか送っていない**
  - **分割案が Issue 本文にある**（「開いた時点の値を出す」と「開いたまま VoiceOver を起動/終了しても追従する」は独立）。
    **前者だけでも症状は解消する**
- ⚠ **`S31` の撮影画像（設定ウィンドウ）が動く可能性がある。** 変わったら「この画面にこの変更が
  波及するはずがあるか」を言えるまでコミットしない
- 周4-a で `window-state.json` の形が変わった（`settings` キー）。**周4-b で設定ウィンドウを
  触るときは、この永続化を壊していないか `S98` で見る**

---

## 2026-08-05 - #180 周4-b: 支援技術の検知を設定に出す（#149）。**design-review で案の中核が5箇所覆った**

PR #213（判定の `@shared` 化）/ #214（注記本体）/ #215（全ウィンドウ配信）。**#149 は close 済み。**

### 計画ゲートで測り直した前提

| 事前の想定 | 実測 |
|---|---|
| 「検知した状態は E2E で作れないかもしれない」 | ⭐ **作れる。** `app.accessibilitySupportEnabled = true` を `app.evaluate` から代入でき、**`accessibility-support-changed` も実際に発火する**。設定ウィンドウの `invoke` も `true` を返した。**VoiceOver は要らない** |
| Issue 本文「A/B と C は独立。前者だけでも症状は解消する」 | **コードの依存としては正しいが、設計としては独立していない**（後述） |

### ⭐ design-review（5ペルソナ）で覆った5箇所

| 初版 | 実際 | 人数 |
|---|---|---|
| 文言「VoiceOver を検知したため有効です」（**Issue の完了条件の文言**） | **事実誤認。** `app.accessibilitySupportEnabled` は支援技術全般で立つ（音声コントロール / スイッチコントロール / AX API に繋ぐ自動化ツール） | **5人** |
| 置き場「チェックボックスの直後」 | **`<label>` の外に出さないと壊れる。** アクセシブル名が汚染されて 2.5.3 を割り、`.settings__row` の2列グリッドの2行目に落ちる。⚠ **同じ罠が `SettingsPanel.tsx:259-261` に既に注記されていた**（`= 3.0 秒` の換算表示で一度踏んでいる） | **5人** |
| `aria-describedby` は「要否を聞く」 | **必須。** 無いとチェックボックスに来た人は「オフ」としか聞けない = **この機能の対象者にだけ届かない** | **5人** |
| 提案 B（`disabled`） | **却下。** 支援技術利用者だけが永続設定を編集できなくなる（VoiceOver を切ったあとの挙動を決められない） | **5人** |
| `role="status"` を条件マウント | **鳴らない。** live region は中身が変わる前から DOM に無いといけない。常時マウントして中身だけ差し替える（前例 `App.tsx:1098`） | **4人** |

### ⛔ 自分の PR 分割が CLAUDE.md 違反だった（保守が指摘）

初版は **「PR 1 = 関門だけ（赤いまま）」** としていた。これは **「赤いまま push しない」に正面から反する。**
**赤くするのは PR の中の手順**であって、赤い状態を main に入れる根拠ではない。

差し替えた3本: **PR 1 = 判定の `@shared` 化（値を1つも変えない置換）/ PR 2 = 注記 + 関門 / PR 3 = 全ウィンドウ配信 + 関門。**
CSS のトークン化で確立した「置換と値の変更を混ぜない」を、TS にも適用した形。

### ⭐ 提案 C は「追従」ではなく、既存バグの修正だった（ヘビー / 保守が独立に発見）

`registerAccessibilityHandlers(win)` が登録時のウィンドウを閉包で掴んでいた。

- `index.ts` の `app.on('activate')` が本体ウィンドウを作り直すと、掴んでいるのは**最初のウィンドウ**
- 以後 `isDestroyed()` で**永久に early return**。しかも `ipcMain.handle` は二重登録で throw するので**張り直せない**
- = **全ウィンドウを閉じて Dock から復帰すると、支援技術の起動・終了が二度と届かなかった**

**「表示の追従のため」だと思っていたものが、実は独立した不具合の修正だった。**

### 関門が赤くなることの確認（E2E 5+2通り + unit）

| spec | 壊し方 | 出た赤 |
|---|---|---|
| S99 | 常に出す | **検知していない状態で `toHaveText('')` が落ちる**（この行が無いと常時表示でも green） |
| S99 | 一度も出さない / `aria-describedby` を外す / 勝手に `checked` にする | それぞれ別の行 |
| S99 | ⭐ **注記を `<label>` の中へ移す** | **チェックボックスが名前で引けなくなる**（`element(s) not found`）= アクセシブル名の汚染を検出 |
| S100 | 1枚だけに送る（直す前の形） | `.xterm-accessibility` が 1 -> **0**（このとき `getAllWindows()[0]` は**設定ウィンドウ側**で、本体が取り残された） |
| S100 | イベントを一切送らない | 設定の注記が空のまま |

**S99 と S100 は向きが違う**（開く前に立てる = マウント時の `invoke` / 開いたまま立てる = イベント）。
**片方だけだと、もう片方の経路を壊しても緑のまま。**

### 設計判断

- 表示条件は **`accessibilitySupport && !config.screenReaderMode`**（食い違っているときだけ）。
  自分でチェックを入れると消える = それ自体がフィードバックになる
- 文言は **状態が先頭**（`aria-describedby` から「…チェックボックス、**オフ**」の直後に読まれるので、
  矛盾を最短で打ち消す語順）。**括弧とコロンを使わない**（VoiceOver が「かっこ」「コロン」を発話しうる）
- **`.settings__status` を `S40` に足した。** それまで Webhook のテスト送信直後にしか出ず
  **一度も測られていなかった**（2人が指摘）。実測 **6.20**
- **CSS 追加ゼロ・新しい色トークンもゼロ**

### ⚠ レビュアーの主張のうち、実測で確かめたもの

| 主張 | 実測 |
|---|---|
| 「`make dev` は DevTools を自動で開くので、開発中ずっと注記が出続けるかもしれない」（IA / 保守が独立に指摘） | ⚠ **未確認のまま**。`--remote-debugging-port` で起動した実機では **`accessibilitySupport` は false** だった（CDP 接続だけでは立たない）。**DevTools を開いた `make dev` は測っていない** -> `known-issues.md` 7番 |
| 「S31 の画像は変わらない見込み」 | **正しかった。** `make e2e-screenshots-check` が **画素差 0**（検知していないと注記は空。かつアクセシビリティ節は 520x640 の画角の外） |

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **624 テスト green**（613 -> 624） |
| `make e2e` | 3本とも **0 failed**（flaky はすべて `firstWindow` 15s） |
| `make e2e-lint` | **PASS=799 FAIL=0**（S99 / S100 を追加） |
| `make e2e-screenshots-check` | **PASS=39 FAIL=0**（画素差 0） |
| 実機確認 | **実施。** 実バイナリで設定を開き、注記の要素が**存在して中身が空**・`aria-describedby` が張られていることを確認 |

### 次に再開するとき最初に読むべきこと

- **周4 は完了（#153 / #149 とも close）。次は #180 周5（#150 = xterm の入力用 textarea のラベル）**
- ⚠ **周5 も UI の文言なので `/design-review` の起動条件に該当する。**
  ただし #150 は**可視の文言ではなくアクセシブル名**なので、5体を回す前に
  「見た目・文言・状態表現のどれを変えるか」を1行で言えるか確かめること
- **周4-b で `known-issues.md` に 7番〜9番を足した。** 周を増やす候補はそこから選ぶ
  （設定の取得失敗が無告知 / `activate` で張り直されていない他のハンドラ / アクセシビリティ節が画角の外）
- ⭐ **この束ねで確立した作法が1つ増えた**: **「A が B を壊す」と「B が A を壊す」は別の検査**（周4-a）、
  **「開く前に立てる」と「開いたまま立てる」も別の検査**（周4-b）。
  **経路が2つあるものは、必ず両方向を壊して確かめる**

---

## 2026-08-06 - #180 周5: 端末の入力欄がどのペインのものか名乗る（#150 / PR #217）

### 計画ゲートで測り直した前提（**探りの spec を書いて捨てた**）

| Issue 本文 | 実測 |
|---|---|
| 「`.xterm-helper-textarea` は既定の英語ラベルのまま」 | **そのとおり。** `Terminal input`。**分割すると2枚とも同じ** |
| 「`Terminal.strings` は static」 | **そのとおり**（だから (b) = textarea へ直接張る、を採った） |
| 「`fullLabel` を回すのが最も安い」 | **そのとおり。** `paneAccessibleLabel` が既に `zsh・demo-project、シェル` を作っている |

⚠ **同時に分かったこと**: 分割直後の2枚は**ペイン自身の名前も同一**（同じ種別・同じ cwd なので当然）。
**つまり「名前を回す」だけでは2枚の区別は付かない。** 区別は利用者がペインに名前を付けて作る
（#130 で入れた経路）。**この spec はそこまで見る**（リネーム後に別々になること）。

### design-review を回さなかった判断

起動条件（`styles.css` を触る / **画面に出る**文言・ラベル・空状態 / 構造 / 状態の見せ方）の
**どれにも当たらない**。これはアクセシブル名だけの変更で、**画素は1つも動かない**
（`e2e-screenshots-check` が画素差 0 で裏付けた）。

⛔ **「a11y だから回す」にしない。** skill 側が「該当しない周では起動しない」と定めており、
**#149 で確定した a11y の判断（コロンを使わない / 役割の語を名前に入れない）はそのまま適用できる。**

### ⛔ Issue の完了条件の字面から1点だけ外した

完了条件は「**「ターミナル: <名前>」**の形になる」だが、**区切りを `、` にした。**

1. **VoiceOver は句読点の読み上げ設定によって「コロン」を発話しうる**（#149 の design-review で確定）
2. このアプリのアクセシブル名は `tabAccessibleLabel` / `paneAccessibleLabel` とも **`、` で繋いでいる**

**理由をコードのコメントと unit の両方に残した**（次に読む人が「Issue と違う」と気づいたとき、
そこで根拠に辿り着けるように）。

### 関門が赤くなることの確認（**5通り**）

| 壊し方 | 出た赤 |
|---|---|
| 名前を張らない | `Terminal input` のまま |
| 全ペインに同じ固定名（`'ターミナル'`） | `/^ターミナル、/` に一致しない |
| ⭐ **固定名だが区切りは正しい**（`'ターミナル、端末'`） | **ペイン自身の名前を含まない** |
| コロンに戻す | パターン不一致 |
| ⭐ **リネームに追従しない**（依存配列を空に） | **リネーム後も2枚の名前が同じ** |

⚠ **3つ目と5つ目が本体。** 「名前が付いた」だけを見る spec だと、
**全ペインに同じ日本語名を付ける実装でも通る**。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **629 テスト green**（624 -> 629） |
| `make e2e` | **106 passed / 0 failed**（flaky 6。**すべて `firstWindow` 15s**） |
| `make e2e-lint` | **PASS=807 FAIL=0** |
| `make e2e-screenshots-check` | **PASS=39 FAIL=0**（画素差 0） |
| **実機確認** | **実施。** `ターミナル、zsh・ai-terminal、シェル`（**cwd に追従している**）。⌘D で分割して2枚とも同じ名前になることも確認 |

### ⚠ 環境の罠を1つ踏んだ（**次に実機確認するとき効く**）

**`--remote-debugging-port=9222` を掴んだままの Electron が残ると、次の起動が
`bind() failed: Address already in use` で CDP を開けない。** しかもアプリ自体は起動するので、
`agent-browser` は**古いターゲットに繋がって「要素が無い」と嘘をつく**（実際に2回誤読した）。

```bash
lsof -ti :9222            # 残っていれば pid が出る
pkill -f "Electron.app/Contents/MacOS/Electron"
```

⛔ **`pkill -f "remote-debugging-port=9222"` では取り逃す**（プロセス名に引数が出ないことがある）。

### 次に再開するとき最初に読むべきこと

- **次は #180 周6（#155 = gemini に `--session-id` を渡す）。** ⚠ **Issue 本文が「CLI 側の制約が
  解消した」と主張しているので、着手前に実際の `gemini` の `--help` を実測すること**
  （このループで「Issue 本文の前提が実コード / 実環境とずれていた」を**7回**踏んでいる）
- ⚠ 周6 は `/ai-cli` の起動引数を触るので、**skill 側の記述も同時に見る**
- 残り: 周7'（#157 / #177）/ 周8（#172 / #171 / #173 / #176。**design-review 必須**）/ 周9（#141。単独 PR）
- `known-issues.md` の 7〜10番は**まだ誰も着手していない**（8番 = 設定の取得失敗が無告知、が P2 で最も重い）

---

## 2026-08-06 - #180 周6: #155 は**実装せずに閉じた**。事前実測で CLI 側の破壊的な副作用が出た

PR #220（記録の是正のみ）。**#155 は not planned ではなく「調査の結論」として close。**

### 計画ゲートで測り直した前提（**Issue 本文の主張は半分だけ正しかった**）

Issue #155 は「CLI 側の制約が解消したので claude と対称にできる」と主張していた。
**フラグの存在は正しいが、結論は成立しない。**

| Issue 本文 / 事前確認項目 | 実測（gemini 0.53.0） |
|---|---|
| `--session-id <UUID>` がある | ✅ **ある。** 渡した UUID がそのまま `--list-sessions` 行末の `[UUID]` に出る（`GEMINI_LINE_RE` がそのまま拾える）。ファイル名も `session-<日時>-<UUID先頭8>.jsonl` |
| 既存 UUID を `--session-id` に渡すとどうなるか | ❌ `Session ID "…" already exists. Use --resume` で**起動しない** -> resume 経路では絶対に渡せない |
| （Issue に無い）`--resume <UUID>` は使えるか | ⛔ **使ってはいけない。** 英字始まりの UUID は偶然動くが、**数字始まり（全体の約 62%）は index として解釈され、別セッションを作ったうえで既存のセッションファイルを失う**（2回再現） |
| ⭐ （Issue に無い）走行中の gemini がいる状態での `--list-sessions` | ⛔ **走行中セッションは一覧に出ず、しかもその JSONL が削除される。** 会話済みでも消え、以後そのプロセスが書いても復活しない（2回再現）。**終了済みセッションは無事** |

### ⭐ 最後の1行が結論を決めた

tmux で生かした gemini は**走行中**なので `--list-sessions` に1行も出ない。
つまり **`--session-id` で tmux セッション名を安定させても、閉じたあとに選び直す導線が CLI 側に無い。**
安定名だけ付けると「回収できる」という**嘘の状態表現**を生むだけになる。

**Issue 本文自身が「事前確認で否定されたら実装しないで閉じてよい（測定結果と誤記の是正は残す）」と
書いていた**ので、その分岐に入った。ユーザーに案1〜案3（実装しない / `--session-id` だけ / 復帰導線ごと作る）
を提示し、**案1 を選択**。

### ⛔ 「Issue 本文の前提が実コード / 実環境とずれていた」は**これで8回目**

しかも今回は**ずれ方が新しい**。これまでの7件は「行番号」「〜が存在しない」「〜できない」のように
**古くなった記述**だったが、今回は **Issue 本文の記述が全部正しいのに、本文が測っていない側で結論が覆った**。

Issue の「事前に測ること」は3項目（UUID の一致 / 既存 ID の挙動 / CLI バージョンの記録）で、
**そのすべてが肯定的だった。** 否定材料は**そのリストに無い4項目目**（走行中セッションの扱い）から出た。

⭐ **教訓: 事前確認リストは「実装が成立する条件」を尽くしていない。**
リストを埋めたら着手してよい、ではなく、**「この機能が成立するために真であってほしいこと」を
自分で1つずつ言い直してから測る**。今回は「閉じたあとに履歴から選べる」が
リストに無い前提で、そこが唯一の破綻点だった。

### 是正した記述（**7箇所。挙動は1行も変えていない**）

| 場所 | 直した内容 |
|---|---|
| `src/main/pty/tmux.ts` 冒頭 + `buildTmuxSessionName` の doc | 「gemini は ID を採番できない」-> 「採番はできるが選び直す導線が無い」。⛔ `--resume` に UUID を渡すなを追記 |
| `README.md` の tmux 節 | 同上を利用者向けに。**引用ブロックで理由を明示** |
| `README.md` のトラブルシューティング | ⚠ **新規**: 「gemini を走らせたまま履歴で gemini を選ぶと記録が消える」+ 当面の回避策 |
| `.claude/skills/terminal/reference/pty-pitfalls.md` | 同上。⛔ 書いてはいけない理由づけを明記 |
| `.claude/skills/ai-cli/reference/cli-flags.md` | `--session-id` を表に追加 + **実測4行の表**（採用しなかった理由） |
| `src/main/history/reader.ts` | 「v0.37.0 で確認」-> 0.53.0 で再実測。⛔ `[UUID]` を `--resume` に渡すな / ⚠ 破壊的副作用 |
| `src/renderer/src/tabs/closeTabCopy.ts` 冒頭 / `test/unit/pty-plan.test.ts` / `e2e/specs/S90-…` | 「結論は覆っていないが、理由が違う」ことを明記。⛔ テストを「反転させれば #155 が終わる」と読まないよう釘を刺した |

### ⚠ 実測の手順（次に gemini の対話挙動を測るときに効く）

- **`script -q /dev/null gemini … < fifo` は使えない**（`tcgetattr/ioctl: Operation not supported on socket`）。
  `python3` の `pty.fork()` + `TIOCSWINSZ` でサイズを与える形にする（サイズを与えないと TUI の入力欄が出ない）
- 破壊の**主体を切り分ける**こと。「30秒放置しても消えない」->「`--list-sessions` を叩いた直後に消える」の
  2段で測って初めて、`--list-sessions` が主体だと言える（最初は「gemini が勝手にローテーションした」と誤読した）
- プローブは `~/.gemini/tmp/<ディレクトリ名>/` に残るので**後片付けする**（`rm -rf ~/.gemini/tmp/<名前>`）

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **629 テスト green**（周5 と同数。テストは1件も増減していない = 挙動を変えていないことの裏付け） |
| `make e2e` | **109 passed / 0 failed**（flaky 3。うち S56 は `demo-project %` のプロンプト待ちで、`firstWindow` 以外の型） |
| `make e2e-lint` | **PASS=807 FAIL=0** |
| 撮影レーン | `make e2e` に含まれる第2 project が回り、**`docs/images/` は1枚も変わらなかった**（`git status` が空） |
| `bash .claude/scripts/lint-skills.sh` | **PASS=93 FAIL=0** |
| 実機確認 | **実施しない。** 画素も挙動も1つも変えていない（変えたのはコメントと文書のみ） |

⛔ **`make e2e` を省かなかった判定根拠**（主観で決めない）:
`git status` の対象に `e2e/specs/S90-…` / `src/main/history/reader.ts` / `src/main/pty/tmux.ts` /
`src/renderer/src/tabs/closeTabCopy.ts` / `test/unit/pty-plan.test.ts` が入る = **必須側**。
中身がコメントだけでも判定はパスで行う。

### 次に再開するとき最初に読むべきこと

- **周6 は完了（#155 は「実装しない」で close）。次の候補は2つあり、優先度は周10 のほうが高い**
  - **周10（周6 で増やした / `known-issues.md` 12番 / P1）**: `gemini --list-sessions` が
    走行中セッションを破壊する。**利用者のデータが黙って失われる**。対処案は3つ書いたが
    **どれも未検証**なので、計画ゲートで実測してから決める
  - 周7'（#157 / #177 = 文書の追従と再発の仕組み）
- 残り: 周7'（#157 / #177）/ 周8（#172 / #171 / #173 / #176。**design-review 必須**）/
  周9（#141。単独 PR）/ **周10**（新設）。**#180 の対象で open は 7件**
- ⭐ **この周で確立した作法**: **事前確認リストを埋めても着手の根拠にはならない。**
  「この機能が成立するために真であってほしいこと」を自分で言い直してから測る
  （リストに無い4項目目で否定された。詳細は上）

---

## 2026-08-06 - #180 周6 の訂正: **結論が誤っていた。#155 を再オープンした**

PR #221（訂正のみ）。**周6 で出した「#155 は実装できない」は誤り。**

### 何を間違えたか

周6 の決定打は「**走行中セッションは `gemini --list-sessions` に出ず、しかも消される**」だった。
これは**空のセッションでの観測を一般化したもの**で、事実ではない。

| 条件 | 一覧に出るか | 他の gemini 起動で消えるか |
|---|---|---|
| 会話0往復（初期コンテキストのみ / 2行） | ❌ 出ない | ⚠ 消える |
| **会話1往復以上（6行〜）** | ✅ **出る（走行中でも）** | ✅ **消えない** |

`-i <prompt>`（プロンプトを実行してから対話モードに留まる）で確実に会話を成立させて測り直したところ、
**走行中のセッションが `--list-sessions` の1番目に正しく出た。**

```
  1. Reply with exactly: KEEPME (13 minutes ago) [aaaa3333-…]   <- 走行中のまま出る
```

**つまり #155 は実装可能。** tmux で生き残った gemini は会話済みなら履歴に出るので、
`--session-id` で tmux 名を安定させれば履歴から選んで `-A` でアタッチし直せる。

### ⛔ 誤りの作り方（**繰り返さないために一番重要**）

**`pty` 経由でテキストを書いても、gemini の TUI に Enter が届かない。**
`\r` でも `\n` でも、テキストと同じ write に混ぜても、write を分けて 3 秒空けても送信されなかった。
**入力欄に文字は乗る**（`> Reply with exactly: REVIVE` が表示される）ので、
**画面をダンプするまで「会話できていない」ことに気づけない。**

- 「2回再現した」と書いたが、**同じ誤りを2回踏んだだけ**だった。⭐ **再現回数は正しさの根拠にならない**
- 行数を見れば即座に分かった（**2行 = 空 / 6行 = 会話済み**）。
  ⭐ **前提を作る操作そのものを assert してから測る**。
  loop.md の「検査が意味を持つ条件を同じ spec の中で assert する」と同じ話が、実測にも要る
- この誤りで **#155 を一度 close して再オープンする手戻り**を出した（PR #220 -> #221）

### 訂正した範囲（PR #220 で入れた記述をすべて差し替え）

`src/main/pty/tmux.ts` / `src/main/history/reader.ts` / `src/renderer/src/tabs/closeTabCopy.ts` /
`test/unit/pty-plan.test.ts` / `e2e/specs/S90-…` / `README.md`（2箇所）/
`.claude/skills/terminal/reference/pty-pitfalls.md` / `.claude/skills/ai-cli/reference/cli-flags.md` /
`known-issues.md` 12番（**全面書き直し**）。

**訂正後の言い方に統一した**: 「gemini が拾い直せないのは **CLI 側の制約ではなく未実装だから**」。
`pty-plan.test.ts` は「反転させるな」から「**#155 を実装するときはこう書き換える**」へ向きを変えた。

### ⭐ skill に落とした知見

`.claude/skills/ai-cli/reference/cli-flags.md` に「gemini の対話挙動を測るときの罠」を新設。
**`-i <prompt>` を使え / Enter は届かない / `script` + fifo は使えない / `.project_root` にフルパス**。

### known-issues 12番の残り

「会話0往復のセッションが消える」自体は**実在する**（P2 に降格）。
⚠ **消されたあと会話を始めたら復活するかは未確認。** 走行中プロセスはセッションファイルの
fd を**保持していない**（`lsof`）ので復活する可能性が高く、**復活するなら記録のみに降格できる**。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **629 テスト green**（増減なし = 挙動を変えていない裏付け） |
| `make e2e` | **106 passed / 0 failed**（flaky 6。いつもの 2〜6 件の範囲） |
| `make e2e-lint` | **PASS=807 FAIL=0** |
| 撮影レーン | `docs/images/` は**1枚も変わらなかった** |
| `lint-skills.sh` | **PASS=93 FAIL=0** |
| 実機確認 | **実施しない。** 画素も挙動も1つも変えていない（コメントと文書のみ） |

### 次に再開するとき最初に読むべきこと

- **#155 は open に戻した。周 6' として実装し直す**（ユーザー判断）。実装の設計は Issue 本文の
  「分割案 (2) 実装」と `architecture.md` の Contract 変更の作法に従う。
  ⛔ **`--resume` に UUID を渡さない**（数字始まりの UUID は index として解釈され、
  既存のセッションファイルを失う）。resume の引数は index のまま、UUID は tmux 名にだけ使う
- **周10（`known-issues.md` 12番）は、まず「復活するか」を測ってから優先度を確定する。**
  P1 ではなく P2。復活するなら記録のみに降格
- ⭐ **この束ねで一番高くついた教訓**: **前提を作る操作そのものを検証する。**
  「会話済みのセッションを作った」は作ったつもりで、行数を見れば1秒で分かった

---

## 2026-08-06 - #180 周 6': #155 を実装した（PR #222 / #223 / #224）。**design-review で案の中核が6箇所覆った**

**#155 は close。** 一度「実装しない」で閉じて再オープンした経緯は1つ前のエントリ。

### 計画ゲートで測り直した前提

| 事前の想定 | 実測 |
|---|---|
| `--resume <index>` した後も UUID は変わらないか（**未測定だった**） | ✅ **変わらない**（同じファイルに追記、一覧の UUID も不変）。**これが偽なら案は成立しなかった** |
| 偽 gemini は起動引数を無視する | ⭐ **`ARGS: $*` を出す**。ただし**そのまま使ってはいけない**（後述） |
| `stableId` が取れない縮退がある | ❌ **実コード経路には無い**（`GEMINI_LINE_RE` が `[UUID]` を必須にしている）。ただし**型は optional なので到達する** |

### ⭐ design-review（5ペルソナ）で覆った6箇所

| 初版 | 実際 | 人数 |
|---|---|---|
| 提案 F: `resumeNote` からプロバイダ名を外す | ⛔ **却下。** 履歴パネルは provider トグルで分かれ**既定は Claude**。タブの帯は**2型色覚で 1.04 = 区別不能**なので、**残る手がかりは語だけ**。外すのではなく**行き先を具体化し、内訳を数える** | **3人反対** |
| 提案 E: 確認が消えるのを受け入れる | ⛔ **削除ではなく降格。** 「閉じても走り続けている」を伝える唯一の面。消すと**支援技術利用者だけが一方的に失う**（視覚利用者は履歴を眺めて発見できる） | **4人** |
| PR 1 = 分類の置換（等価） | ❌ **等価でない。** `close-tab-copy.test.ts` の `leaf()` が `agentSessionId` を持たないので claude のケースが反転する。**PR 1 を「Main だけ」に組み替えた** | 2人 |
| 提案 D の理由（到達不能だが防御的） | ⚠ **到達する。** `stableId` は optional。**縮退経路として実際に踏む**（鉄則5の系） | 3人 |
| known-issues 12番は非目標 | ⛔ **外せない。** 会話0往復の gemini は `agentSessionId` を持つので **resumable と分類されるが実際には戻れない**。**嘘の向きが逆になっただけ** | 3人 |
| `docs/images/` の撮り直し「⚠ 要確認」 | ✅ **不要と確定**（13枚にダイアログも gemini も無い） | 4人 |

### ⭐ レビューでしか出なかった実害が2つ

1. **バージョン非互換**（ヘビーユーザー）。`--session-id` を無条件に渡すと **0.53.0 未満で新規タブが全滅**する。
   実測すると未知フラグは `Unknown arguments:` + usage を出して**即終了** = tmux ラップ下では
   **「開いた瞬間に終了したペイン」**にしか見えず、`describeSpawnError()` は ENOENT しか説明できない
   -> `gemini --version` を1回だけ見る。⛔ **版が読めなければ「非対応」に倒す**（外し方が非対称）
2. **偽 gemini の `ARGS:` に生 UUID が出る**（保守）。`bin/claude` は
   「**非決定性は撮影時に隠すのではなく発生源で断つ**」として既にマスクしている。
   **初版の関門4行目（`ARGS:` を見る）は採らなかった**

### PR 3本の構成（保守の対案を採用）

| PR | 内容 | 既存テスト |
|---|---|---|
| #222 | Main + 配管だけ（`--session-id` / Contract / 版判定 / 偽 CLI のマスク / S102・S103） | **全部緑のまま** |
| #223 | 分類を `agentSessionId` の有無へ + 内訳 + 告知への降格 + S90 作り直し | `close-tab-copy.test.ts` 14件 |
| #224 | README / `aria-pressed` / known-issues 12番の手当て / skill | — |

**PR 1 が緑のまま入るので、PR 2 の diff が純粋に「分類」、PR 3 が「文書」になった。**

### 関門が赤くなることの確認（**PR 1 で6通り / PR 2 で5通り**）

| PR | 壊し方 | 出た赤 |
|---|---|---|
| 1 | 新規で `--session-id` を渡さない / resume で `agentSessionId` を返さない / `--resume` に UUID を渡す / **tmux 名を ptyId 由来に戻す（形は合う）** / `App.tsx` が渡さない | S102 |
| 1 | 版判定を無視して常に渡す | S103 |
| 2 | 分類を `ptyKind` に戻す / **確認の機構ごと消す** / 告知を1文に戻す / 内訳を落とす / `announce` を素の `useState<string>` に戻す | S90 |

⭐ **PR 1 の4つ目が要点。** 形（`aiterm-<UUID っぽい何か>`）だけを見る検査だと ptyId 由来のままでも通る。
**採番された実際の値と文字列一致**で比べている。

### ⛔ S90 で一度空振りした（**新しい型**）

分割を `.terminal-pane` の**総数**で確かめていたが、**この spec の時点でタブが2枚あるので、
分割しなくても総数は2になる**。アクティブなタブの x ボタンのラベル（`タブを閉じる（2 ペイン）`）で
見る形に直した。⭐ **「数える対象のスコープがテストの前提とずれる」型**は loop.md の表に無い。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | **653 green**（629 -> 653） |
| `make e2e` | **110 passed / 0 failed** |
| `make e2e-lint` | **PASS=823 FAIL=0**（S102 / S103 を追加、S90 を作り直し） |
| 撮影レーン | `docs/images/` は**1枚も変わらず** |
| `lint-skills.sh` | **PASS=95 FAIL=0** |

### skill へ落としたもの

- `design-review/reference/design-rules.md`: 確認を外すときは**降格**にする / 行き先が分かれている操作は
  文言で言い切る（**プロバイダ名は経路情報**。色でも位置でも代替できない）
- `design-review/reference/implementation-traps.md`: **live region は同じ文字列を連続で渡すと鳴らない**
- ⚠ `design-rules.md` が 500 行上限に達したので、**`## 4. 触ると波及するもの`（315行）を
  `ripple-effects.md` へ切り出した**（内容は1行も変えていない）

### 次に再開するとき最初に読むべきこと

- **#155 は close。周 6' は完了。** 残り: 周7'（#157 / #177）/ 周8（#172 / #171 / #173 / #176。
  **design-review 必須**）/ 周9（#141。単独 PR）/ **周10**（`known-issues.md` 12番）
- ⭐ **周10 の優先度が変わった。** #155 の実装後、**会話0往復の gemini が
  「回収できる」と分類されるのに実際には戻れない**唯一の経路になった（`known-issues.md` 12番に追記済み）。
  まず「消されたあと会話を始めたらファイルが復活するか」を測る（`lsof` では fd を保持していない）
- ⚠ **design-review が「別の周へ」と送ったものが7件ある**（`proposal-155-rev2.md` の 5. に列挙。
  `needsCloseConfirmation` が `exit` 済みも数える / 分割タブの `Cmd+W` が判定を通らない /
  確認ボタンの赤 / `Cmd+Shift+T` / フォーカストラップ / `S44` に `.confirm-dialog__button` /
  履歴の provider 選択を記憶しない）。**⛔ 新規 Issue は立てず、`known-issues.md` へ**

---

<!-- 以降、作業のたびにセクションを追記 -->
