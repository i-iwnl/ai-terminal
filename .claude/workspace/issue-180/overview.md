# Issue #180 P3: 検査の追加・語の統一・設定ウィンドウ・文書の追従 - Overview

> **Issue**: [#180 P3: 検査の追加・語の統一・設定ウィンドウ・文書の追従（17件を統合・新規起票を止めるループ）](https://github.com/i-iwnl/ai-terminal/issues/180)
>
> #159 / #160 の棚卸しで残った P3 と、誤って closed になった #161（P3 10件）の引き継ぎを束ねたループ Issue。
> **周の構成・順序の制約・据え置きの条件は Issue 本文が唯一の正**（ここには転記しない）。
> **完了条件は「対象がすべて close されている」こと**で、「対処方針が決まった」では閉じない。
>
> 詳細は以下の3ドキュメントに分割:
>
> - `architecture.md` - 触る構造・Contract変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-08-06（**周6 の結論を訂正し #155 を再オープン**。次は周 6' = #155 の実装）

---

## 1. ゴール

Issue 本文の対象17件のうち **open な15件を close しきる**（#140 / #168 は 2026-08-05 時点で既に closed）。
**2026-08-06 時点の残りは 8件**: #141 / #155 / #157 / #171 / #172 / #173 / #176 / #177。
⚠ `gh issue list` の全体には **#147 / #156 も出るが、どちらも #180 の対象ではない**（数え間違いの元）。
あわせて、**#179 の design-review で出たまま open な Issue に追跡されていない2件（周5-b / 周7）をこのループで回収する。**

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（周によって `src/renderer/` 中心 / `src/main/` 中心が入れ替わる） |
| ブランチ | 周ごとに `main` から生やす（**スタック PR を作らない**） |
| 関連PR | 周ごとに追記 |

### ⛔ このループの掟

- **周の中で新規 GitHub Issue を立てない。** 見つけたものは `known-issues.md` に書き、切り出したくなったら**周を1つ増やす**
- 人力でしか進められないものは [#195](https://github.com/i-iwnl/ai-terminal/issues/195) に追記し、**元 Issue を not planned で閉じる**。#195 自体は閉じない
- マージ後は必ず `git checkout main && git pull` してから次の周へ

---

## 2. 完成条件

### 引き継いだ2周（#179 の design-review 由来。**Issue 本文には無い**）

- [x] **周5-b**: メニュー項目のホバー / フォーカスが見える
      （材料は `.claude/workspace/issue-179/known-issues.md` の **11番「周5-b」**。**ここには転記しない**）
- [x] **周7**: アクティブタブが可視域へスクロールする（前半 = PR #205 / 後半 = design-review で「何もしない」に決定）
      （材料は同 **11番「周7」**）

### #180 本体（周1〜9。完了条件の詳細は Issue 本文の表が正）

- [x] 周1 検査を足す（#139 / #146）— ⚠ **#146 は E2E ではなく unit で固定**（`describeSpawnError` は node-pty の性質で E2E から到達できない）
- [x] ~~周2 状態語の正を1箇所に集める（#140）~~ — **周ごと畳んだ**。実コードでは `tabExitCopy.ts` が唯一の正で、
      `TabBar.tsx` / `paneHeader.ts` の両方が参照し `test/unit/tab-exit-copy.test.ts` が固定済みだった
- [x] 周3 メニューの振り分けと dev/prod 差の記録（#152 / #145）— ⚠ **#152 も E2E ではなく unit で固定**
      （`getFocusedWindow()` はハーネスの非表示化により E2E で常に `null`。`known-issues.md` 6番）
- [x] 周4 設定ウィンドウ（#153 = PR #211 / #149 = PR #213・#214・#215）— **両方 close 済み**
- [x] 周5 読み上げラベル（#150 = PR #217）— **design-review は回さなかった**（画素が1つも動かないため。理由は worklog）
- [ ] 周6 gemini のセッション ID（#155）— ⛔ **一度「実装しない」で閉じたが、結論が誤りだったので再オープン**。
      「走行中セッションは一覧に出ない」は**空セッションでの観測を一般化した誤り**で、
      **会話済みなら走行中でも出る**（PR #221 で全面訂正）。**周 6' として実装し直す**
- [ ] 周7' 文書の追従と再発の仕組み（#157 / #177）
- [ ] 周8 UI の文言・レイアウト（#172 / #168(closed) / #173 / #176 / #171。**design-review 必須**）
- [ ] 周9 lint の網を広げる（#141。**単独 PR**）
- [ ] **周10（周6 で増やした）** 会話0往復の gemini セッションが、gemini の起動で消える件（`known-issues.md` 12番。**P2**）
- [ ] 各周で `make check` / `make e2e` / `make e2e-lint`（`FAIL=0`）が green

> **周番号の衝突に注意。** 引き継ぎの「周5-b / 周7」は #179 の番号、#180 本体の「周1〜9」は Issue 本文の番号。
> worklog では**必ず `#179 由来の周7` / `#180 周7'` のように出自を書く。**

---

## 3. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 引き継ぎ2周 + #180 周1・周3〜周5 完了（周2 は畳んだ）。**周6 は結論が誤りで #155 を再オープン**。次は周 6'（#155 の実装） |
| 実装 | PR #202〜#221 マージ済み。**#180 の対象で open は 8件**（#141 / **#155** / #157 / #171 / #172 / #173 / #176 / #177） |
| 検証 | マージ済み15本（#202〜#217）すべて `make e2e` green |

| 周 | 対象 | 状態 |
|---|---|---|
| 引き継ぎ 5-b | メニューのホバー / フォーカス | **完了**（PR 1 = #202 / PR 2 = #203 / PR 3） |
| 引き継ぎ 7 | アクティブタブの可視域スクロール | **完了**（前半 = PR #205 / 後半 = 3人が独立に「何もしない」） |
| #180 周1 | 検査を足す（#139 / #146） | **完了** |
| #180 周2 | 状態語「終了」の一元化（#140） | **畳んだ**（実コードで解決済みだった） |
| #180 周3 | メニューの振り分けと dev/prod 差（#152 / #145） | **完了**（PR 1 = #208 / PR 2 = #209） |
| #180 周4-a | 設定ウィンドウの位置と高さ（#153） | **完了**（PR #211）。⭐ この周は **E2E で本当に検証できた** |
| #180 周4-b | 支援技術の検知の実効表示（#149） | **完了**（PR #213 / #214 / #215）。⭐ design-review で案の中核が **5箇所**覆った |
| #180 周5 | 端末の入力欄の名前（#150） | **完了**（PR #217） |
| #180 周6 | gemini のセッション ID（#155） | ⛔ **結論が誤り。#155 を再オープンした**（PR #220 -> 訂正 PR #221）。⭐ 教訓は「**前提を作る操作そのものを検証する**」 |
| #180 周 6' | #155 の実装 | 未着手。**次はここ** |
| #180 周7'〜9 | Issue 本文の表を参照 | 未着手 |
| #180 周10 | 会話0往復セッションの消失（`known-issues.md` 12番） | 未着手。**周6 で増やした**（P2 へ降格） |

### 引き継ぎ周5-b の PR 分割（**画素が動く PR を1本だけにする**ため3本）

| PR | 内容 | 製品コード | 画像 | 状態 |
|---|---|---|---|---|
| 1 | 関門（`contrast.ts` の alpha ガード / `S40` に2件 / `S64` に3件） | **0行** | 0枚 | **完了**（#202） |
| 2 | 見た目（`line-height` / `:focus` 1本 + `onMouseMove` / `--surface-menu-active` / 白 2px inset / 枠から離す padding / `@media`） | 変える | **1枚**（S06） | **完了** |
| 3 | 挙動（`focusout` で閉じる / `tabIndex={-1}` / 外側クリックのフォーカス復帰） | 変える | 0枚 | **完了** |

⛔ **スタック PR にしない。** 3本とも `main` から生やし、順にマージする。

---

## 4. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | #180 周 6'（#155） | `--session-id` の実装。⛔ **`--resume` に UUID を渡さない**（数字始まりは index として解釈され既存ファイルを失う） |
| P1 | #180 周7'（#157 / #177） | 文書の追従と再発の仕組み |
| P2 | #180 周10（`known-issues.md` 12番） | 会話0往復のセッションが消える。**まず「復活するか」を測る**。復活するなら記録のみに降格 |
| P1 | #180 周8（#172 / #171 / #173 / #176。**design-review 必須**）/ 周9（#141。単独 PR） | 残りの周 |
| P2 | `known-issues.md` 5番（ウィンドウメニューのタブ一覧） | **`menu.ts` の面は周3 で空いた**。いま衝突相手がいない |
| P2 | `known-issues.md` 4番（ウィンドウ非アクティブ化） | Main の `win.on('blur')` を IPC で渡す。**Contract 変更なので `/electron-ipc`**。⚠ 6番の見立てを先に実測する |
