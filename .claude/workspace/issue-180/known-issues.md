# Known Issues

実装中に発見された未解決のバグ・先送りした課題。
詳細な観察記録は `worklog.md` を参照。

⛔ **このループの中で新規 GitHub Issue を立てない。** ここに書くだけにする。

---

## 1. #179 の design-review で出たまま、open な Issue に追跡されていない指摘

### 症状

#179（P2 の束ね）の周5（#170）で回した5ペルソナレビューが、その周のスコープ外の指摘を出した。
#179 は対象7件を全 close して閉じたため、**どこからも追跡されない状態で残った**。

### 原因（判明している場合）

「周の中で新規 Issue を立てない」という掟の副作用。**掟自体は正しい**（2026-08-04 に1日で 46 件作成 / 14 件 close の実績があり、これを止めるための掟）。
足りていないのは、**束ね Issue が閉じるときに known-issues の未対処項目を次の束ねへ引き渡す動線**。

### 影響範囲

- 材料の唯一の正: `.claude/workspace/issue-179/known-issues.md` の **11番**（⛔ **ここに転記しない**）
  - **周5-b**: メニュー項目のホバー / フォーカスが見えない（4項目・実測済み）
  - **周7**: アクティブタブが可視域へスクロールしない（3項目・実測済み）
  - **記録のみ**: 製品判断に寄るもの・予防（6項目）

### 対処方針

- [x] #180 のループの周として引き取る（reopen も新規起票もしない）
- [ ] 引き継ぎ周5-b を回す
- [ ] 引き継ぎ周7 を回す
- [ ] 「記録のみ」6項目は、この Issue を閉じるときに**次の束ねへ引き渡すか、#195 へ回すか、落とすかを決めて理由を書く**

### 優先度

P2（周5-b / 周7）・P3（記録のみ）

### ステータス

対処中（2026-08-05 に #180 へ引き取り）

---

## 2. 引き継ぎ周5-b の design-review で出た、この周のスコープ外の指摘

⛔ **この Issue のループの中で新規 GitHub Issue を立てない。** ここに書くだけにする。

### 次周の第一候補（ヘビーユーザーが「この案で唯一の手数削減項目」と判定）

| 指摘 | 根拠 | 指摘した人 |
|---|---|---|
| **メニュー項目にキー等価を併記する**（`⌘T` / `⇧⌘C` / `⇧⌘E`） | このメニューは**手数を1手も減らしていない**（3項目とも既存ショートカットが 100% 覆う）。**次の日にも残るのは「キーを覚えたこと」だけ**。幅は増えない（項目内寸 134px に対しラベル + キー等価で 77px、**57px 余る**）。⛔ **この周に入れなかった理由は「画像が変わるから」ではない**（`line-height` の是正でどうせ変わる）。**文字列の単一の正を `src/shared/` に新設し `menu.ts` と `TabBar.tsx` の両方から参照する設計変更を伴う**こと。入れるときは可視の span を `aria-hidden` にし `aria-keyshortcuts` を付ける（a11y） | ヘビー / macOS / IA / a11y |

### メニュー本体の残り（周を増やすときの材料）

| 指摘 | 根拠 | 指摘した人 |
|---|---|---|
| `--fs-sm`(12px) -> `--fs-md`(13px) | macOS のメニュー既定サイズで、**このアプリ自身の `body` も 13px**。メニューだけ本文より小さい理由が無い。**この周に入れないのは、面の色・行間・文字サイズの3つを同じ PR で動かすと画像が落ちたときの主語が3つになるから** | macOS / a11y |
| 型入力選択（`c` -> Claude）と `Home` / `End` | NSMenu の挙動 / APG の要求。⚠ **削減 0手**（3項目・既に modulo 循環しているので `End` = `ArrowUp` 1回） | macOS / ヘビー |
| `aria-label` / `title`「新しいタブを開く」が実結果と1手ずれる（押してもタブは増えない） | ⚠ **a11y は「違反している SC は無い」と判定**（`aria-haspopup="menu"` が補い、可視ラベルは `aria-hidden` なので 2.5.3 適用外）。⚠ **セレクタは4ファイル6箇所**（`screenshots.spec.ts:602,611,624` / `S95:124` / `S64:25` / `S06:30`） | macOS / IA |
| `.tab-bar__new` に `aria-expanded="true"` の視覚表現が無い（CSS 規則 0件） | 1.4.11 の「状態」 | a11y |
| 「+ ▾」を macOS 標準どおりタブバーの trailing 端へ固定する | Safari / Finder / Terminal.app / Xcode はタブ列に含めない。いまは押すたびに x 座標が動く。⚠ **`.tab-bar__drag-region` の幅計算と複数画像を巻き込む** | macOS |
| ネイティブ `Menu.popup()` へ移す | ⛔ **「Playwright から観測できない」は偽**（`S91` が `Menu.prototype.popup` を差し替えて検証済み。`scenarios.yml:1374` に「誤りだった」と明記）。**やらない正しい理由は3つ**: (a) `menu.ts:406` が避けている DIP とコンテンツ座標の換算が未解決 (b) `docs/images/S06-new-tab.png` が被写体を失う (c) `App.tsx:1098` の唯一の告知経路（`role="status"`）から外れる | macOS / a11y |

### メニューの外（ヘビーユーザーが実測した、毎日効くもの）

| 指摘 | 見積もり |
|---|---|
| **`newShellTab` / `splitActivePane` の cwd が最大2秒古い。** `newAgentTab`（`useTabs.ts:267-283`）だけが実プロセスへ問い合わせ直しており、⌘T（`useTabs.ts:244`）と ⌘D は `getSharedCwd()`（2秒ポーリング）を使う。**このリポジトリ自身が `useTabs.ts:271-274` で同じ事故を説明している** | **30〜50手/日** |
| **`rename-active-pane` に accelerator が無い**（`menu.ts:149` が `undefined`。`shortcuts.ts` に `'i'` は空き。AppAction は `ipc.ts:527` に既にある） | **12〜32手/日** |
| `⇧⌘T`（閉じたタブを開き直す）が未割り当て。⌘W の誤爆から復帰する手段が無い | 1〜3回/日 |
| コマンドパレットが無い / タブ横断のスクロールバック検索が無い | 乗り換えを妨げる2点 |

### ハーネス側の負債（保守が指摘）

| 指摘 | 内容 |
|---|---|
| `verify-css-substitution.mjs` の `stripRoot` が**先頭の `:root` しか剥がさない** | `css-tokens.test.ts` の `splitRoot` は**全部剥がす**。**2つのハーネスが `@media` 内の `:root` で食い違っている**。周5-b の PR 2 で `@media` にトークンを足すと、**トークン宣言の行が「値の変更」として差分に出る** |
| `grep -c "readme: true"` は **14** を返すが実体は **13** | `scenarios.yml:10` のコメント行を拾う。数えるなら `grep -c "^\s*readme: true"` |

### 優先度

P2（キー等価 / cwd の2秒古さ）・P3（その他）

### ステータス

未対処（**記録のみ。周を増やしたくなったらここから選ぶ**）

---

## 3. `npx agent-browser mouse move` では React の `onMouseMove` が発火しない（周5-b PR 2 で遭遇）

### 症状

実機確認（agent-browser + CDP）で `npx agent-browser mouse move <x> <y>` を使っても、
**`onMouseMove` のハンドラが動かない**。座標を少しずつ変えて複数回呼んでも同じ。

**`:hover` は正しく付く**（`document.querySelectorAll(':hover')` に対象要素が出る）ので、
「マウスが乗っていない」わけではない。

### 原因（判明している場合）

未特定。`Input.dispatchMouseEvent` の `mouseMoved` がホバー状態の更新には使われているのに、
React のルートに委譲されたリスナへは届いていないように見える。
**Playwright の `hover()` では発火する**（同じ CDP 経由なのに挙動が違う）。

### 影響範囲

- 実機確認の手順（`.claude/skills/e2e/operations/verify-on-device.md`）。**ポインタ由来の振る舞いは実機で直接作れない**
- ⚠ 「実機では動かない」と誤読しやすい。**周5-b で実際に誤読した**（さらに再ビルド漏れが重なって10分溶かした）

### 対処方針

- [x] 代わりに `dispatchEvent(new MouseEvent('mousemove', { bubbles: true, clientX, clientY }))` で確認した
- [ ] `verify-on-device.md` の「agent-browser でも届かないもの」に追記するか判断する（**周を増やすなら**）

### 優先度

P3

### ステータス

回避策あり（記録のみ）

---
