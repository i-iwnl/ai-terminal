# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-08-04 - ワークスペース作成と、着手前の実測（10件）

### 実施内容

- #159 の棚卸しで起票した Issue 群のうち P3 を束ねて **#161** を作成した（P1/P2 は #160）
- loop.md の「計画書の『現状はこうなっている』を実コードで測り直す」に従って実コードに当て直した。内訳:
  - **#145 / #146 / #149 / #150 / #152 / #153 / #155 / #157** — #159 の棚卸しで main = 61edbe5 に対して検証済み（そのとき起票したもの）。main は 4f6d57e（docs のみの差分）なので現行のまま有効
  - **#139 / #140** — #143 マージ後に測り直した
- 判定は**全件が有効**（既に解決していたものは0件）。**#139 の本文に誤りが1件**あり、Issue にコメントで訂正した
- 周1〜周7 を確定し、`overview.md` に完了条件を観測できる形で置いた

### 実測で分かったこと

**#139 の動機が誤っていた。** 「`--text-secondary` を `--surface-2` の上に載せた実測値が一度も取られていない」と書かれていたが、`e2e/specs/S40-contrast-contract.spec.ts` の `'非選択セグメントの文字（対トラック）': 6.69` が**まさに同じ色の組**を固定していた。`.sidebar__tabs button:not(.is-active)` は `background: transparent` なので、セレクタを見ただけでは `--surface-2` の上にあると分からない。`contrast.ts` の `effectiveBackground()` が親まで遡る実装を読んで初めて一致する。

**この Issue の価値は「未知の値を測る」ことではなく「`.pane-header` 固有の宣言が将来書き換わったときに検出できる」こと。** 動機を読み替えて着手する。数値（6.69 / 1.048）は独立計算で一致した。

**#140 の「3箇所」は正しかった。** `src/` の「終了」85ヒットのうち、状態を表す名詞ラベルはちょうど3件（残りは `終了する` / `〜を終了` / `終了しました（コード N）` など別の語）。

### 着手を助ける事実（周ごとに効く）

- **#139 は想定より安い。** S40 は既に `Meta+t` → `Meta+d` で分割状態を作っており `.pane-header` は DOM に存在する。`mainTargets` に1〜2エントリ足すだけ。ただし **`expected` にも足さないと `Object.keys` の突き合わせで落ちる**
- **#152 のキーボード経路は無害。** `registerAccelerator: false` により設定ウィンドウの Esc / `Cmd+W` は `SettingsPanel.tsx` の window keydown が捌く。**再現するのはマウスでメニューをクリックした場合だけ**。`actionItem` の1箇所を直せば37項目すべてに効く
- **#153 のブロッカーは消えている。** `src/main/window-state.ts` は実装済みで、`settings-window.ts` が import していないだけ。保存対象は x / y / height の3つ（横幅は `minWidth === maxWidth === 520` で仕様として固定）
- **#155 は事前実測が関門。** `gemini --session-id <UUID>` で起動したセッションが `--list-sessions` の行末 `[UUID]` に同じ UUID で出るかを先に測る

### 設計判断

`architecture.md` の「設計判断履歴」が正。要点だけ:

- **P3 を #160 と分けた。** 合わせると周が20近くになる。衝突するのは #140 / #157 の2件だけで、順序制約として管理できる
- **#155 は測定が否定的なら実装しない。** 測る前に実装を始めると、成立しない設計に時間を使う
- **#157 を最後に置いた。** 5箇所のうち3箇所が #158（#160 の周7）と同じファイル

### 教訓

**1. 「測られていない」はセレクタでは証明できない。値の組で考える。**

#139 の誤りがまさにこれ。`.pane-header` で grep すれば S40 に0件だが、**同じ色の組は別のセレクタ名で既に測られていた**。`background: transparent` の要素は親の色を継ぐので、セレクタと実際に測っている色が一致しない。

**2. 束ねを分けるときは、衝突する箇所を先に数えて順序に落とす。**

#160 と #161 は独立に見えて、`TabBar.tsx` の `leaf.exit` 参照4行（#142 と #140）と `App.tsx` / `CloseTabConfirmDialog.tsx` のコメント（#158 と #157）で衝突する。**分けた時点で順序制約として `overview.md` に書いた**ので、片方を先に進めても事故らない。

### 次に再開するとき最初に読むべきこと

- **周1（#139 / #146）から始める。** どちらも検査を足すだけで、値と振る舞いを変えない
- **#139 は S40 の `mainTargets` と `expected` の両方に足す。** 背景（1.048）を測るなら **`against: '.terminal-stack'` が必須**（`against` 無しだと自分と比べて 1.0 になる。S40 に同じ罠が3回記録されている）
- **#139 の項目名は既存の慣例に揃える。** S40 は項目名に「塗り」「枠」「ドット」が含まれるかで 3.0 / 4.5 の閾値を振り分ける文字列ヒューリスティックを持っている
- **#146 は実現不可能な可能性がある。** node-pty の spawn 失敗は同期例外にならず、`withoutCli: true` でも `describeSpawnError` を踏めない（S11 の冒頭コメントが実測として記録）。**できなかったら、何を試して何が起きたかを `limitations.md` に書いて close してよい**
- **周2（#140）は #160 の周2（#142）が main に入るまで着手しない。** 待たずに始めると `TabBar.tsx` で衝突する
- 各 Issue に投稿した「着手前の実測」コメントが、その Issue の最新の現状認識。**Issue 本文より優先して読む**
