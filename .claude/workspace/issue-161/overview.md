# Issue #161 P3: 検査の追加・語の統一・設定ウィンドウ（10 件を統合） - Overview

> **Issue**: [#161 P3: 検査の追加・語の統一・設定ウィンドウ（10 件を統合）](https://github.com/i-iwnl/ai-terminal/issues/161)
>
> #159 の棚卸しで起票した Issue 群のうち、P3 を束ねて周で回す。#121 と同じ形で、#160（P1/P2）と対になる。
> 対象は10件（#139 #140 #145 #146 #149 #150 #152 #153 #155 #157）。
> #141 / #147 は `deferred` なので着手しない。
>
> 詳細は以下の3ドキュメントに分割:
>
> - `architecture.md` - 触る構造・Contract変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-08-04

---

## 1. ゴール

10件を周に分けて溶かしきる。#160 と違って**大半が独立していて衝突しない**ので、周は機能領域でまとめている。ただし**2件だけ #160 に順序で依存する**（#140 と #157）。

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（renderer 主体。#155 のみ main + shared、#152 は main） |
| ブランチ | 未作成（周ごとに main から生やす。**スタック PR を作らない**） |
| 関連PR | 未作成 |

---

## 2. 完成条件

- [ ] 対象10件（#139 #140 #145 #146 #149 #150 #152 #153 #155 #157）がすべて close できる状態になっている
- [ ] 各周で `make check` / `make e2e` / `make e2e-lint`（`FAIL=0`）が green
- [ ] UI を触った周（周4）は `make e2e-screenshots` を回し、差分を1枚ずつ説明できている
- [ ] 追加した関門が、対象の不具合で**実際に赤くなる**ことを周ごとに確認した記録が `worklog.md` にある
- [ ] #155 は測定結果（Gemini CLI のバージョンと日付つき）が記録されている。**実装しない判断になった場合も残す**
- [ ] 型チェック通過（`make check`）
- [ ] Lintチェック通過（`make check`）

---

## 3. 周の構成と完了条件

| 周 | 内容 | 対象 | 完了条件（観測できる形） | 状態 |
|---|---|---|---|---|
| 1 | 検査を足す（値・振る舞いを変えない） | #139 / #146 | S40 に `.pane-header` の2エントリが入り green。ハーネスから `describeSpawnError` 経由の `.notice-banner--error` を出せて S55 が green。`git diff` に振る舞いの変更が無い | 未着手 |
| 2 | 状態語の正を1箇所に集める | #140 | 「終了」の定数が1箇所にあり、`TabBar.tsx` 2箇所と `paneHeader.ts` 1箇所がそれを参照。3箇所が同じ語を使うことを unit が固定 | 未着手 |
| 3 | メニューの振り分けと dev/prod 差の記録 | #152 / #145 | 設定ウィンドウにフォーカスがある状態でメニューを発火させても本体のタブ数が増えない E2E が green。`limitations.md` に dev/prod の手順がある | 未着手 |
| 4 | 設定ウィンドウ（位置の復元と検知の表示） | #153 / #149 | 位置と高さが復元される（横幅は 520 固定のまま）。VoiceOver 検知時に設定へ表示が出る。既存の S31 / S70 が green | 未着手 |
| 5 | 読み上げラベル | #150 | textarea のアクセシブルネームが「ターミナル: \<名前\>」になり、名前の変更に追従することを S37 が検証 | 未着手 |
| 6 | gemini のセッション ID（**事前実測 → 判断**） | #155 | 測定結果が記録されている。肯定的なら `buildGeminiPlan()` が `agentSessionId` を返し unit が固定。否定的なら4箇所の誤った前提が実測日つきで是正されている | 未着手 |
| 7 | 実装に追い越された文書の追従 | #157 | 5箇所が実態に合っている。`lint-skills.sh` が通る | 未着手 |

### 見た目・文言を変える周（`/design-review` を差し込む）

**周4（#149 の設定ウィンドウ表示）**。チェックボックスの意味（設定値）と実効値の区別を壊さない表現が要るので、計画を確定させる前に通す。

### 順序の制約（動かさないこと）

1. **#140（周2）は #160 の #142 より後。** どちらも `TabBar.tsx` の同じ4行（`leaf.exit` の参照）に触り、#142 が `leaf` を `allExited` に置き換える。**#160 の周2 が main に入ってから着手する**
2. **#157（周7）は最後。** 直す対象のうち `App.tsx` / `CloseTabConfirmDialog.tsx` の3箇所は **#158（#160 の周7）と同じファイル**。#158 が先に入れば、その PR に畳める分がある
3. **#155（周6）は実装の前に測定を1回挟む。** 測定結果が否定的なら実装しないので、周の中身が測定結果で変わる

---

## 4. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 完了（10件を実コードに当て直し、周を確定した） |
| 実装 | 未着手 |
| 検証 | 未着手 |

---

## 5. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | 周1 の計画ゲートを通す | #139 は S40 の `mainTargets` と `expected` の**両方**に足す（`Object.keys` の突き合わせがある）。背景を測るなら `against: '.terminal-stack'` が必須 |
| P1 | 周1 の #146 を実装する | `LaunchOptions` に spawn 失敗モードを足す。**実現不可能と判断したら、何を試して何が起きたかを `limitations.md` に書いて close する** |
| P2 | #160 の周2（#142）が main に入るのを待ってから周2（#140）へ | 待たずに始めると `TabBar.tsx` で衝突する |
