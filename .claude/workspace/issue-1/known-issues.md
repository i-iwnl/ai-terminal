# Known Issues

実装中に発見された未解決のバグ・先送りした課題。
詳細な観察記録は `worklog.md` を参照。

---

## 1. Phase 1 の受け入れ基準が未検証

### 症状

`docs/PLAN.md` の Phase 1 受け入れ基準（vim / htop / 日本語 IME / コピペ / 色 / リサイズ）が、**一度も人間の目で確認されていない**。preload と PTY の不具合を直した直後の状態で、まだアプリを実際に触っていない。

### 原因

GUI 操作を伴うため自動確認ができず、確認の機会を作れていない。

### 影響範囲

- ターミナルとしての基本機能全般
- ここが崩れていると E2E を積んでも意味がない

### 対処方針

- [ ] `make dev` で起動し、`.claude/skills/terminal/operations/verify-terminal.md` のチェックリストを実施する
- [ ] 崩れがあれば E2E より先に直す

### 優先度

P0

### ステータス

未対処

---

## 2. 日本語 IME は自動テストで検証できない

### 症状

変換中の下線表示・確定前の挙動は OS レベルの入力イベントで、Playwright から再現できない。

### 原因

IME の変換処理はブラウザやアプリの外側（OS の入力メソッド）で行われるため。

### 影響範囲

- このアプリで最も壊れやすく、かつ最も重要な機能が自動検証の対象外になる

### 対処方針

- [x] 自動化の対象外と明記する（Issue #1 に記載済み）
- [ ] `.claude/skills/terminal/operations/verify-terminal.md` の手動チェックリストを唯一の検証手段として維持する
- [ ] E2E では「日本語を含む**出力**の文字幅」（S04）までを担保し、入力側は担保しないことを README にも明記する

### 優先度

P1

### ステータス

対処方針確定（自動化しないことを決定）

---

## 3. tmux 永続化は E2E の範囲外

### 症状

tmux でラップした場合、内側の `claude` が終了しても PTY の exit が発火しない。この挙動と、アプリ再起動を跨いだセッション復帰は E2E で検証できない。

### 原因

アプリのプロセスライフサイクルを跨ぐ検証になるため、単一の Playwright セッションでは表現できない。

### 影響範囲

- tmux 永続化まわり全般
- E2E では `useTmux: false` の固定設定で動かすため、この経路は一切通らない

### 対処方針

- [ ] 必要になったら別途シェルスクリプトでの検証を検討する
- [ ] E2E のフィクスチャでは `useTmux: false` を明示し、意図的に対象外にしていることをコメントで残す

### 優先度

P3

### ステータス

先送り

---

## 5. E2E が `--disable-gpu` 固定のため、WebGL レンダラ経路を一度も検証していない

### 症状

`make dev`（GPU 有効 = WebGL レンダラ）でターミナルが**真っ黒のまま何も描画されない**不具合があったが、E2E 22件は全て green のままだった。

原因は `@xterm/xterm/css/xterm.css` が未 import だったこと（2026-07-28 に修正済み）。canvas を配置するのは xterm.css の規則なので、CSS が無いと WebGL レンダラは何も表示できない。

### 原因

E2E ハーネスは全テストを `--disable-gpu` で起動する（`e2e/fixtures/harness.ts`）。これは「WebGL だと文字が canvas に描かれ DOM から読めない」ため意図的にそうしている。結果として **DOM レンダラ経路しか検証されていない**。

DOM レンダラは文字を実 DOM のテキストノードとして描くため、xterm.css が無くても表示されてしまう。つまりこの不具合は E2E の構造上、原理的に検出できなかった。

### 影響範囲

- ユーザーが実際に使う `make dev` / 本番起動は WebGL レンダラ。**E2E が緑でも実利用時に壊れている**状態がありうる
- 描画品質に関わる不具合全般（文字幅・色・カーソル・選択範囲）

### 対処方針

- [x] 原因となった xterm.css の未 import を修正する
- [ ] GPU 有効で起動し、canvas をスクリーンショットで検証するシナリオを1件追加する（`toHaveScreenshot` かピクセル非空判定）。DOM テキストは読めないので、既存シナリオの流用ではなく専用の判定が要る
- [ ] `harness.ts` の `--disable-gpu` に、この盲点を明記したコメントを追加する

### 優先度

P1

### ステータス

原因は修正済み。再発防止のシナリオ追加が未対応

---

## 4. macOS 通知は検証手段が無い

### 症状

タスク完了時の macOS 通知の発火を、Playwright から観測できない。

### 原因

OS の通知センターへの表示はアプリの DOM 外で起こるため。

### 影響範囲

- 完了通知（busy から idle への遷移検知）

### 対処方針

- [ ] Main プロセスのログ出力で発火のみを間接的に確認する案を検討する
- [ ] 通知の内容そのものは手動確認に回す

### 優先度

P3

### ステータス

先送り
