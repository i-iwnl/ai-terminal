# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-07-29 - デザイン案の作成・5ペルソナのレビュー・Phase 0 の完了

### 実施内容

- デザイン案を1本書き、`/design-review` の5ペルソナ（macOS デザイナー / IA / ヘビーユーザー /
  a11y / 保守担当）に並列レビューさせた
- 指摘を統合し、Issue #20（親）と #21〜#25（Phase 0）として起票した
- 提案トークンでそのまま描いた視覚モックアップを公開し、#20 にコメントで貼った
- Phase 0 の5件を PR #29 / #30 / #33 / #34 / #35 として実装した（積み重ね）

### 設計判断

- **レビューの価値は案を磨くことではなく、案の前提を壊すことにある。**
  実際、案そのものより先に「デザイン以前に壊れているもの」が5件見つかり、案の中核的な前提が2つ覆った
- Phase 0 を親 Issue のチェックリストにせず個別 Issue に切り出した。
  `enhancement` の中に不具合が埋もれると、個別に着手・クローズできない

### 教訓（該当する場合）

- **5人中5人が独立に指摘した項目は、案の欠陥ではなく前提の誤り。**
  状態色が意味と逆だった件は、コード・README・E2E キャプション・掲載画像の4段階に伝播していた
- **自分で書いた案の数字は疑う。** レビューで検算された結果、コントラスト比3件・
  E2E シナリオ数・`prefers-reduced-motion` の対象の有無が誤っていた
- **`main` の履歴がルートから書き換えられる事故があった**（PR #26 / #28 が自動で閉じた）。
  最初「git の挙動がおかしい」で済ませかけたのは落ち度。**ブランチを切ったら
  `git log --oneline -1` と `git status` の両方を必ず確認する**

### 次に再開するとき最初に読むべきこと

- Phase 0 は完了。次は Phase 1 の PR 1（既定値の共通化）

---

## 2026-07-29 - Phase 1 / PR 1: 既定値を src/shared へ寄せる

### 実施内容

- `src/shared/defaults.ts` を新設し、`DEFAULT_THEME` / `DEFAULT_WEBHOOK` / `DEFAULT_CONFIG` を移した
- `src/main/config.ts` の自前定義を削除し、shared を参照するようにした
- `src/renderer/src/lib/defaults.ts`（#25 で作った暫定の置き場）を削除し、
  `App.tsx` / `SettingsWindow.tsx` は `@shared/defaults` を直接見る形にした
- `config.ts` からの再エクスポートは作らず、テストも唯一の正から直接 import するようにした
- **`coerceConfig({})` が `DEFAULT_CONFIG` と一致することを単体テストで固定した**
- 検証: `make check`（unit 76）/ `make e2e`（38 passed）/ `make e2e-lint`（PASS=279 FAIL=0）
- **`screenReaderMode` を `coerceConfig` から外して、新テストが赤くなることを確認した**

### 設計判断

- **再エクスポートを作らない。** `config.ts` から `DEFAULT_CONFIG` を再エクスポートすると
  同じものへの経路が2本になる。テスト側の import を1行変えるほうが安い
- Renderer 側の `FALLBACK_CONFIG` という名前をやめて `DEFAULT_CONFIG` に統一した。
  「フォールバック」と「既定値」が別物に見えていたのが、そもそも二重化を許した一因

### 教訓（該当する場合）

- **型が守ってくれない二重化がある。** 設定項目を増やすとき、`AppConfig` と `DEFAULT_CONFIG` は
  型で守られるが、**`coerceConfig` への追加漏れは型では検出できない**
  （返り値の型は満たしたまま、その項目だけが常に undefined になる）。
  空オブジェクトを食わせて既定値と一致するかを見る1行で、そこを機械化できた
- **「見た目の変化ゼロ」は `git status docs/images/` で確認できる。**
  主張するだけでなく、画像に差分が出ていないことを実際に見た

### 次に再開するとき最初に読むべきこと

- **PR 1 は完了。** 次は Phase 1 の PR 2（`:root` にトークンを宣言だけ追加。使用箇所ゼロ、値は現行 hex と 1:1）
- **PR 2〜4 は画像0枚、PR 5 で12枚**。この境目を崩さないことが Phase 1 の設計そのもの
- トークンの値は Issue #20 本文の A-3（コントラスト実測表）が正。**surface-3 の上でも検証すること**
- 積んである PR は #29 -> #30 -> #33 -> #34 -> #35 -> 本PR の順。前段がマージされたら base を繰り上げる

---

## 2026-07-29 - Phase 1 / PR 2: トークンを宣言する

### 実施内容

- `styles.css` の先頭に `:root` のトークン群を追加した。**使用箇所ゼロ**（宣言だけ）
- 現行の色を棚卸しした結果 **33種類**あり、面だけで8種類の近いグレーが混在していた
- 値はすべて現行のリテラルと 1:1 にした。PR 5 で変える予定のものには、
  変更後の値と理由（コントラスト比）をコメントで併記した
- 4段に畳めない中間色には**移行用トークン**（`--surface-tabbar` 等）を用意し、
  PR 5 で削除する前提であることを明記した
- 単体テスト `test/unit/css-tokens.test.ts` を追加した
- 検証: `make check`（unit 79）/ `make e2e`（38 passed）/ `make e2e-lint`（PASS=279 FAIL=0）/ 画像 0枚

### 設計判断

- **移行用トークンを作ることを選んだ。** 面を最初から4段に畳むと、その時点で見た目が変わり
  PR 3 が「値据え置き」を名乗れなくなる。近い色をいったんトークン名として温存し、
  PR 5 でまとめて畳む
- **トークン名は「用途」ではなく「目標の段」で付けた**（`--surface-sidebar` ではなく `--surface-0`）。
  用途で名付けると PR 5 で名前ごと変わり、値の変更と改名が混ざる

### 教訓（該当する場合）

- **「値は現行と 1:1」は主張ではなく検査にできる。**
  宣言した色が CSS 本体で実際に使われている値かを機械で確認するテストを書いた。
  `--surface-0` の値を1文字変えて赤くなることを確認済み。33種類の近い色を目視で守るのは無理
- 同じ値に2つのトークンが付くと置換のときに揺れるので、**重複も検査した**。
  偶然一致している3組（`#232323` / `#333333` / `#666666`）は理由付きで許容リストに入れてある

### 次に再開するとき最初に読むべきこと

- **PR 2 は完了。** 次は PR 3（面と境界を変数へ置換。値据え置き・画像0枚）
- 置換したら `test/unit/css-tokens.test.ts` に**逆向きの検査**（`:root` の外に生の hex を残さない）を
  足すことを検討する。PR 4 が終わった時点が入れどき
- 画像が動くのは PR 5 だけ。**PR 3-4 で `git status docs/images/` に差分が出たら、それは置換ミス**

---

## 2026-07-29 - Phase 1 / PR 3: 面と境界を変数へ置換する

### 実施内容

- 面（background）と境界（border 系）、影を `var(--...)` へ置換した。**値は据え置き**
- 検索バーの浮いた面（`#2a2a2a`）が区切り線と同値だったため、`--surface-float` を追加した
  （用途が違うので別トークンにし、重複の許容リストに理由付きで入れた）
- 単体テストに2つ足した:
  - `var()` が参照しているトークンがすべて宣言されていること（**タイプミスは CSS では黙って無視される**）
  - 宣言した色が「未置換ならリテラルが実在／置換済みなら参照されている」こと（段階的な置換に対応）
- **`scripts/verify-css-substitution.mjs` と `make css-substitution-check` を新設した**
- 検証: `make check`（unit 81）/ `make e2e`（38、S32 が1回 flaky）/ `make e2e-lint`（PASS=279）/ 画像0枚

### 設計判断

- **「値据え置き」を証明する仕組みを作った。** トークンを機械的に展開して HEAD の CSS と
  文字列比較する。結果は**完全一致**。目視でも E2E でも画像でも「色が少し違う」は検出できないので、
  これが唯一の担保になる
- 値を変える PR（PR 5）ではこのスクリプトは当然落ちる。**`make check` には入れず、独立した target にした**

### 教訓（該当する場合）

- **`var()` のタイプミスは CSS では黙って無視され、その宣言だけが効かなくなる。**
  ビルドも E2E も通ってしまう。`var(--surface-tabbar)` を `--surface-tabber` にして、
  2つのテストが赤くなることを確認した
- **検査は置換の進行に合わせて育てる必要がある。** PR 2 で書いた「宣言した色はリテラルとして
  実在する」は、置換した瞬間に全部落ちる。「未置換ならリテラル／置換済みなら参照」に
  書き換えて、段階的な移行のあいだも意味を持つ形にした
- **「見た目が変わっていない」は主張ではなく証明できる。** 画像の差分ゼロは弱い証拠
  （経過時間が毎回変わるので、そもそも画像は毎回バイト差が出る）。トークンを展開して
  元の CSS と一致することを見るのが本当の担保

### 次に再開するとき最初に読むべきこと

- **PR 3 は完了。** 次は PR 4（文字色・サイズ・余白に加え、**アクセント `#4c8bf5` と状態色**も）
- PR 4 の後、`test/unit/css-tokens.test.ts` の「未参照のトークンが無い」検査を強められる
- **PR 4 が終わるまでは `make css-substitution-check` が必ず通ること。** PR 5 で初めて落ちる
- 画像が動くのは PR 5 だけ

---

## 2026-07-29 - Phase 1 / PR 4: 文字色・状態色・アクセント・サイズ・余白を変数へ置換する

ブランチ: `refactor/css-tokens-type-and-space`（`main` = `6a2fa78` から分岐）

### 実施内容

- **`styles.css` 本体から色のリテラルが完全に消えた**（148行を置換）。文字色9種・アクセント・
  状態色5種・通知色・フォントサイズ6段・行間・余白（4px グリッド）・角丸を `var(--...)` へ。**値は据え置き**
- 置換は手作業ではなく使い捨てスクリプトで機械的に適用し、差分を目視で検分した
  （`:root` とコメントには触らない・寸法は対象外、という規則をコードで表現した）
- `make css-substitution-check` が **PASS**（HEAD と完全一致 = 見た目の変化ゼロ）
- 単体テストを「置換が完了した後の形」に強めた:
  - 旧「未置換ならリテラル／置換済みなら参照」を廃し、**未参照の色トークンはゼロ**を要求
  - **本体に色のリテラルが1つも残っていない**を追加（トークンを迂回した直書きの再混入を止める）
  - 色以外の未参照トークン（`--lh-tight` / `--sp-6`）は**理由付きの許容リスト**に載せた。黙って増やせない
- `scripts/verify-css-substitution.mjs` が比較前にコメントを落とすようにした
- ルート `CLAUDE.md` に `make css-substitution-check` と「置換と値の変更を混ぜない」規約を追記した
  （PR 3 で target を作ったのに唯一の正へ書いていなかった）
- 検証: `make check`（unit 100）/ `make e2e`（39 全緑・flaky 0）/ `make e2e-lint`（PASS=286 / FAIL=0）/
  `lint-skills`（PASS=87 / FAIL=0）/ 画像0枚

### 設計判断

- **4px グリッドに乗らない余白（2 / 3 / 6 / 10 / 14 / 18 / 20px）と `border-radius: 4px` は
  リテラルのまま残した。** グリッドへ畳むのは値の変更で見た目が動く。置換の PR に混ぜると
  「画像が変わった理由を分離できない」という Phase 1 の前提そのものを壊す
- **寸法（幅・高さ・線幅）は余白トークンの対象にしない。** `width: 8px` のドットと
  `gap: 8px` の余白が同じトークンを共有すると、PR 5 で尺を動かしたときにドットまで太る
- **`--lh-tight` / `--sp-6` は使用箇所ゼロのまま残した。** 尺の段を飛ばさないため。
  ただし「宣言だけあって使われない」を放置しないよう、テストの許容リストに理由込みで固定した
- **検証スクリプトからコメントを除外した。** 置換すると「既定色 #666 のまま」のような
  コメントが嘘になるが、直すと比較が落ちるため直せない、という板挟みが起きていた。
  コメントは描画に影響しないので落として良い

### 教訓

- **「コメントを比較対象から外す」は検査を弱めるので、弱まっていないことを実際に確かめた。**
  宣言を丸ごとコメントアウトする改変（`/* padding: ... */`）を注入したところ、
  宣言の行そのものが消えるため従来どおり検出できた。**確かめずに除外していたら、
  「消したのに気づかない」穴を自分で開けていた**
- **今周は負の対照を5件まとめて流した**（値の変更・宣言のコメントアウト・未参照トークン・
  リテラル直書き・`var()` のタイプミス）。全件で赤を確認してから緑を報告している
- **検査は置換の進行に合わせて「強める」だけでなく「捨てる」必要がある。** PR 3 の
  「未置換ならリテラル／置換済みなら参照」は、置換完了後は**常に真になる無意味な検査**に変わる。
  残しておくと、通っていることが何も保証しないテストが1本増える

### 次に再開するとき最初に読むべきこと

1. ブランチ `refactor/css-tokens-type-and-space` に**未コミットの変更**がある（commit / push / PR は
   ユーザーの明示指示待ち）。触ったのは `styles.css` / `test/unit/css-tokens.test.ts` /
   `scripts/verify-css-substitution.mjs` / `CLAUDE.md` の4ファイル
2. **Phase 1 の置換は PR 4 で完了した。次は PR 5 = 値の変更**（コントラスト是正・非テキスト 3:1・
   `prefers-contrast`）。**値の根拠は Issue #20 の A-3 の実測表が唯一の正**
3. **PR 5 では `make css-substitution-check` が落ちるのが正しい。** 落ちたことを不具合と読み違えない。
   代わりに PR 5 では**画像12枚の撮り直し**が関門になる
4. PR 5 は見た目が動くので、**計画を確定させる前に `/design-review` を通す**（loop.md の規定）
5. コントラストは**最も明るい面（設定ウィンドウ `--surface-3`）の上でも**測ること。暗い面だけで通しても意味がない

---

## 2026-07-29 - Phase 1 / PR 5 の計画: 5ペルソナレビューで前提が4つ覆った

**実装は1行も行っていない。計画ゲートで止まり、案を書き直した周。**

### 実施内容

- PR 5（値の変更）の案を書き、`/design-review` の5ペルソナ全員に並列レビューさせた
- **案の中核的な前提が4つ覆ったため、1 PR の計画を 7 PR に作り替えた**
- 改訂版の案: `<scratchpad>/pr5-proposal-revised.md`（初版は `pr5-proposal.md`。**編集せず別ファイルにして、何が覆ったかを残した**）
- レビュー結果と訂正を Issue #20 にコメントした

### 覆った前提（詳細は Issue #20 のコメント）

1. **`make e2e` はこの周の関門として何も守っていない。** 全39本を読んで確認したところ落ちる spec は **0本**。
   `--text-tertiary` を #000000 にしても緑になる。さらに **CI に `npm run unit` が入っていない**ので、
   PR 4 で入れた検査は PR 上で1度も走っていなかった
2. **画像を撮る前に別の修正が要る。** `e2e/fixtures/harness.ts:139-150` が `@shared/defaults` を import せず
   自前の `DEFAULT_CONFIG` を持つため、既定テーマを変えても E2E に届かず、**12枚全部に4pxの帯が写る**
3. **5人全員が、この周で作り込む退行を指摘した。** 選択中タブの塗り 1.15 -> 1.08、文字 2.85 -> 1.91。
   案が自ら「見えない」と診断した箇所を、案が悪化させていた
4. **`--surface-1` を動かす必然性が無かった。** 据え置けば分離は 1.07 -> 1.11 と**むしろ良く**、
   A-2 の帯・`defaults.ts` の変更・既存 `config.json` の移行（4人が指摘）が丸ごと不要になる

### 設計判断

- **面の値で3案が対立し、ヘビーユーザー案（`--surface-1` 据え置き）と a11y 案（`--surface-3` は #282828 まで）を採った。**
  macOS デザイナーの「サイドバーを #242424 へ上げる」は却下したが、
  **却下理由を `design-rules.md` に残す**ことを条件にした（記録しないと次のレビューで同じ指摘が出る）
- **画像は最後の1 PR でまとめて撮る。** `docs/images` は 776KB / 12枚あり、各 PR で撮ると履歴が膨らむ
- **`make css-substitution-check` は凍結せず、落ちた出力を成果物として使う。**
  `-`/`+` の行差分は12枚の PNG より精度の高いレビュー資料になる

### 教訓

- **手で書いたコントラストの表は、2周続けて誤った。** 初版の診断4件が誤りで、
  原因は「現状」の欄に案の値を貼っていたこと。**さらに Issue #20 の A-3 自体が誤っていた**（1.38 -> 実測 1.50）。
  A-3 は「初版は3件とも誤っていた」と書いて再計算した表である。
  **2周続けて同じ失敗をしたので、表を人間が書き写す運用そのものをやめ、契約テストに移す**（PR 5-0）
- **レビューの価値は案を磨くことではなく、案の前提を壊すことにある、が今回も再現した。**
  5人が返した中で最も重かったのは配色の good/bad ではなく「**その良し悪しを判定する関門が存在しない**」だった
- **サブエージェントの指摘は必ず自分で検算する。** 4件の数値誤りは a11y と保守担当が独立に同じ結論を出したが、
  こちらでも再計算し、さらに**計算関数自体を既知値（白/黒=21.00、#777 on 白=4.48）で検証**してから受け入れた
- **提案を別々に読むと気づけない組み合わせがある。** 提案 B（色を畳む）と提案 E（サイズを畳む）は
  単独では妥当だが、メモ一覧では**同じ周で色と大きさの両方を潰す**ため、2行が完全に同化する（IA が発見）

### 次に再開するとき最初に読むべきこと

1. **改訂版の案 `<scratchpad>/pr5-proposal-revised.md` が計画の正**（scratchpad はセッション固有なので、
   失われていたら Issue #20 の 2026-07-29 のレビュー結果コメントが同内容を持っている）
2. **次は PR 5-0（関門を作る周）。値を1つも変えない。** 中身は5つ:
   - 既定色の正を1本化（`src/main/index.ts:42` / `src/main/settings-window.ts:40` /
     `e2e/fixtures/harness.ts:139-150` が `@shared/defaults` を見る）
   - コントラスト契約の E2E（S40）。**最初は現状値を literal で持つ characterization 形式**にし、
     値を変える PR の diff に `3.51 -> 4.67` が現れるようにする
   - CI に `npm run unit` を足す（1行）
   - `test/unit/css-tokens.test.ts` の `intentionalDuplicates` を自己検査化する
     （現在は `continue` でスキップするだけなので、**古い名前が残っても通ってしまう**）
   - `make css-substitution-check` の `BASE_REV` 既定が `HEAD` である罠を塞ぐ
     （**コミット後にローカルで叩くと自分自身と比較して PASS する**）
3. **PR 5-0 は PR #44（`refactor/css-tokens-type-and-space`）の上に積む。** #44 は未マージ
4. 値を変える PR は 5-1 から 5-6。**画像は 5-7 でまとめて12枚**
5. **選択中タブの下線化は次周の必須項目**。この周は「悪化させない」だけで、根本解決していない

---

## 2026-07-29 - Phase 1 / PR 5-0: 値を変える前に、値を守る関門を作る

ブランチ: `chore/issue-20-pr5-0-guardrails`（`main` = `0adfa22`、PR #44 マージ後から分岐）

### 実施内容

**値は1つも変えていない。** レビューで判明した「関門が存在しない」を先に塞ぐ周。

- **既定色の正を1本にした。** `src/shared/defaults.ts` に `SURFACE`（面4色）を新設し、
  `src/main/index.ts` / `src/main/settings-window.ts` のハードコードされた `backgroundColor` と、
  `e2e/fixtures/harness.ts` のローカル theme をここから引くようにした
- **S40（コントラスト契約）を追加した。** 実際に描かれている色から比を測り、**数値そのものを固定する**
  characterization テスト。本体ウィンドウ8項目 + 設定ウィンドウ4項目
- **CI に `npm run unit` を足した。** PR 4 で入れた検査が PR 上で1度も走っていなかった
- `test/unit/css-tokens.test.ts` に、**CSS の `--surface-*` と TS の `SURFACE` の一致**を突き合わせる検査を追加
- 重複の許容リストを**自己検査化**した（衝突が解消した名前が残っていると落ちる）
- `verify-css-substitution.mjs` の `BASE_REV` 既定を `HEAD` から `origin/main` に変えた
- 検証: `make check`（unit 103）/ `make e2e`（**40 全緑**）/ `make e2e-lint`（PASS=293 FAIL=0）/ 画像0枚

### 設計判断

- **S40 の期待値は「あるべき値」ではなく「いまそうなっている値」。** 多くは WCAG を満たしていないが、
  満たしていないことを承知で固定する。値を変える PR では、この期待値の diff に
  `3.93 -> 5.68` のような変化が現れる。**12枚の PNG を目で見るより、この数行のほうがレビューできる**
- **測れなかった項目を「合格」にしない。** 測定結果のキー集合と期待値のキー集合が一致することを先に検査する。
  セレクタが変わって要素が見つからなくても静かに素通りする、という壊れ方を防ぐ
  （実際、最初の実行で「非選択タブ」が測れていないことをこの検査が捕まえた。起動直後はタブが1枚しかない）
- CSS 変数は Main から読めないので、面の色が `styles.css` と `defaults.ts` の2箇所に存在するのは
  **構造的に解消できない**。統一を諦めて、一致を単体テストで突き合わせる方針にした

### 教訓

- **S40 は書いた直後に、紙の上の表の誤りを2件見つけた。** どちらも「計算」ではなく
  **「背景をどれと見なすか」**の取り違えだった:
  - 設定の入力欄の枠は、入力欄自身の塗り（#1c1c1c、1.50）ではなく
    **囲んでいる面**（#232323、**1.38**）との比が実際の見え方を決める。
    **つまり Issue #20 の A-3 の数値 1.38 は正しく、こちらが投稿した「1.50 が正しい」という訂正が誤りだった**
  - 検索欄の枠が乗っているのはターミナル背景ではなく**検索バーの浮いた面**で、
    1.71 ではなく **1.47**。実際はさらに見えていない
- **紙の上で色を突き合わせる限り、この取り違えは無くならない。** 背景をどれと見なすかは DOM の構造で決まる。
  3周目でようやく原因が分かった（1周目: A-3、2周目: PR 5 初版、3周目: この訂正）
- **負の対照で「他の spec では捕まらない」ことまで見た。** `--text-tertiary` を #000000 にすると
  **S40 だけが落ち、S12（色を見ている数少ない spec）は通る**。
  「関門が無い」という主張が、実際にそのとおりであることを確認してから塞いだ

### 次に再開するとき最初に読むべきこと

1. **ここから先は値を変える周（5-1 〜 5-6）。落とすべきものが2つある**:
   - `make css-substitution-check REV=origin/main` は**落ちるのが正しい**。
     出力の `-` / `+` を PR 本文に貼り、「表に無い `+` 行が1本でもあれば意図しない変更」を受け入れ基準にする
   - **S40 の期待値の更新が作業の一部**。更新は推測しない（spec が実測値をログに出す）
2. 値の根拠は **S40 の実測を正とする**。Issue 本文の A-3 は設計意図の記録として残すが、
   **値の根拠としては使わない**（2周続けて誤っており、原因は対象の選び方だった）
3. 確定した値は Issue #20 の「5ペルソナレビュー結果」コメントの表。
   **`--surface-1` は #1e1e1e 据え置き**（動かす必然性が無く、動かすと帯・移行・4ファイルの同時変更を背負う）
4. **画像は 5-7 でまとめて12枚**。5-1 〜 5-6 の間は古いままで構わない（`lint-e2e.mjs` は存在しか見ない）
5. 選択中タブの下線化は**次周の必須項目**。この周は悪化を避けるだけ

---

## 2026-07-29 - Phase 1 / PR 5-1 〜 5-7: 値を変えきる（Phase 1 完了）

PR #46 / #47 / #48 / #49 / #50 / #51 / #52。**1周ごとに独立してマージした。**

### 実施内容

| 周 | 内容 | 主な結果 |
|---|---|---|
| 5-1 | フォントサイズ 6段 -> 4段 | 9px を廃止（macOS 最小推奨 11pt を大きく下回る） |
| 5-2 | 面 10トークン -> 5トークン | サイドバー #141414 / 設定面 #282828。**`--surface-1` は据え置き** |
| 5-3 | 文字色 9種類 -> 4段 + 端末 | **全段が4面すべてで 4.5:1**（最悪 4.86） |
| 5-4 | 枠 4種類 -> 1種類 | 1.30〜1.51 -> **3.15〜3.94**。アクセント・状態・エラー帯も |
| 5-5 | フォーカスリング | `outline: none` だけあって `:focus` が無い箇所が2つあった |
| 5-6 | `prefers-contrast: more` + S41 | 弱い文字・区切り線・選択中タブを高コントラスト時に上げる |
| 5-7 | 画像12枚 + README | 撮り直しは最後に1回だけ |

**残った未達は選択中タブの塗り（1.23）の1件だけ。** 塗りでは解けないので次周で下線を足す。

### 設計判断

- **レビュアーの対案を3件、再計算のうえ採らなかった。** いずれも「対案が前提にしていた別の値を、
  こちらが変更していた」ことが原因:
  - 選択中タブを `--surface-3` に畳む（5人全員の対案）-> 対案の 1.22 は `--surface-1` を
    #1b1b1b に動かす前提。据え置きに変えると **1.13 = 現状より悪化**するので、
    `--surface-tab-active` を残して 1.23 にした
  - 枠を #747474 に -> 残した選択中タブの面の上で **2.91** と 3:1 を割る。#7a7a7a にした
  - サイドバーを #242424 に明るくする（macOS ペルソナ）-> 原則3「ターミナルが主役」を優先して却下。
    **意図的な HIG 逸脱として記録する**ことを条件にした
- **画像は最後の1周でまとめて撮った。** `docs/images` は 776KB / 12枚あり、
  各 PR で撮ると binary の履歴が5倍に膨らむ。`lint-e2e.mjs` は存在しか見ないので、
  途中の周で画像が古いままでも関門は落ちない

### 教訓

- **S40 は、書いた直後から3回、手書きの数値の誤りを捕まえた。**
  設定入力欄の枠（1.50 ではなく 1.38。**A-3 が正しく、こちらの訂正が誤りだった**）、
  検索欄の枠（1.71 ではなく 1.47）、フォーカスリング（4.67 ではなく 4.29）。
  **3件とも「計算」ではなく「背景をどれと見なすか」の取り違え。**
  紙の上で色を突き合わせる限り無くならないので、値の根拠を実測に移したのは正しかった
- **測定の順序が結果を変えることがある。** フォーカスを当ててから枠を測ったら、
  `:focus` が `border-color` をアクセントに変えるせいで「通常の枠」ではない値が出た。
  **枠を測り終えてからフォーカスする**順序に直した
- **`:root` は1つではない。** `@media (prefers-contrast: more)` の中の `:root` を
  「本体に色を直書きした」と誤判定して単体テストが落ちた。あれは宣言なので検査から外す。
  ただし**外したことで検査が弱まっていないこと**を、本物のリテラルを別の場所に注入して確認した
- **レビューの対案には「その時点の他の値」という前提が埋まっている。** 5人全員が同じ対案を
  出していても、こちらが別の値を変えれば成り立たなくなる。**多数決をそのまま実装しない。**

### 次に再開するとき最初に読むべきこと

1. **Phase 1 は完了。次は Phase 2。** 入口は `overview.md` の「直近の次アクション」
2. **最優先は選択中タブの下線**（Phase 1 でただ1つ閾値に届かなかった箇所）。
   `.sidebar__tabs button.is-active` に `border-bottom: 2px solid var(--accent)` の前例がある
3. **配色の値を触るときは S40 の期待値を更新するのが作業の一部。** 推測しない（spec が実測をログに出す）。
   値を変える PR では `make css-substitution-check REV=origin/main` が落ちるのが正しく、
   その `-` / `+` 出力を PR 本文に貼って「表に無い行が1本でもあれば意図しない変更」を基準にする
4. **Issue #20 本文の A-3 は値の根拠に使わない**（2周続けて誤った）。設計意図の記録としてのみ残す
5. 未着手で送ったもの: `selectionBackground` #264f78（対背景 2.03）/ ターゲットサイズ 2.5.8（8要素）/
   キーボード到達性（`<div onClick>` でタブに Tab で到達できない）/ `font-weight` による階層 /
   `Cmd+B`（**別 Issue に切る**）

---

## 2026-07-29 - PR 6: Target Size 24x24 の一括適用（Phase 2 の1本目）

前のエントリで「#58 と独立に今すぐ着手できるもののうち最小」と判定した PR 6 を実施した。
**#58（cwd）は同じ日に別 PR（#72）で解決済み**なので、Phase 2 の詰まりは解けている。

### 実施内容

- `:root` に `--target-size-min: 24px` を追加
- 6種類のボタンに `position: relative` + `::before` の透明な当たり判定（`max(100%, var(--target-size-min))`）を敷いた
  - タブの閉じる / 通知バナーの閉じる / メモの閉じる / 履歴の「メモ」「編集」/ 履歴のプロバイダ切替・更新 / 検索の「前」「次」「x」
- E2E に S44 を追加。**実際にクリックが当たる範囲**を測る（`getComputedStyle(el, '::before')` で寸法を取り、その四隅を `elementFromPoint` で叩いてボタンに解決することまで確認する）

### 設計判断

- **`min-width` / `min-height` を直接足さず、`::before` の絶対配置にした。** 前者だとタブバーの高さ 36px や履歴行の詰まり方が動く。**見た目を変えずに当たり判定だけ広げる**のが PR 6 の趣旨
- **`max(100%, 24px)` にして、既に 24px 以上ある辺は広げない。** これにより履歴の「メモ」「編集」のような**隣接するボタン同士の当たり判定が横方向に重ならない**（幅はもともと 24px 以上あるので、伸びるのは高さだけ）
- **Issue #20 が挙げた7種類のうち「設定の閉じるボタン」は対象から外した。** #25（PR #35）で設定が独立ウィンドウになった際に画面上のボタンごと消えており、いまは Escape / Cmd+W / OS のクローズボタンしかない。**Issue 本文の調査が古くなっている**

### 教訓

- **調査時点の行番号と対象は、9本 PR を挟むと古くなる。** #20 本文の「7種類」は、うち1つが別 PR で消滅していた。**一括適用の PR は、着手時に対象を数え直す**こと（今回は 7 -> 6）
- **`boundingBox()` は擬似要素を見ない。** 当たり判定を擬似要素で作る以上、`boundingBox()` で測るテストは**常に green になる無意味な検査**になっていた。「実際にクリックが当たるか」を `elementFromPoint` まで見て初めて関門になる
- **当たり判定を広げると、隣が「別のボタン」でなくても影響が出る。** タブの閉じるボタンは隣にボタンが無いが、**タブそのものがクリック対象**（選択）。広がった当たり判定はタブの余白側に張り出すため、**そこを押すと「選択」ではなく「閉じる」になる**。`known-issues.md` の 4 に記録した

### 次に再開するとき最初に読むべきこと

1. **進捗の唯一の正は Issue #20 本文の表。** PR 6 の行を「完了」に更新すること（このエントリの時点では PR 作成まで）
2. **#58 は解決済み**（PR #72）。**PR 8 / 10 / 13 の前提がそろった**ので、Phase 2 の残りはどこからでも着手できる
3. `known-issues.md` の 4（タブの閉じるボタンの当たり判定がタブ選択領域に張り出す）を、PR 8 以降で**タブバーを触るときに一緒に判断する**

---

<!-- 以降、作業のたびにセクションを追記 -->

## 2026-07-29 - 残タスクの棚卸しと、#20 をクローズできるかの判断

実装は無し。**「#20 の残りを明らかにして、不要ならクローズする」**という依頼への調査周。

### 実施内容

- Issue #20 本文の PR 分割表（PR 6〜20）を、**実コードに当てて1本ずつ未着手かどうか確認した**
- 結果を `overview.md` の「Phase 2 以降」に表として引き写した（裏取りの grep 結果つき）
- **#20 はクローズしない**と判断。ユーザーが「開いたまま維持」を選択
- 未起票のまま放置されていた cwd の問題を **[#58](https://github.com/i-iwnl/ai-terminal/issues/58) として起票**（bug / P1）

### 判明したこと

| 区分 | 状態 |
|---|---|
| Phase 0（P0-a〜e） | 完了（#21〜#25 すべて CLOSED / PR #29,30,33,34,35） |
| Phase 1（PR 1〜5-7） | 完了（PR #36,37,38,44,45〜52） |
| PR 7 | Phase 0 の #25 / PR #35 で**前倒し完了済み**。Phase 2 の残りから除いてよい |
| PR 6, 8〜20 | **14 本すべて未着手。着手痕跡ゼロ** |

Phase 1 完了時点の宿題も未消化のまま残っている:
選択中タブの下線（`styles.css:313` に `border-bottom` が無い）/ 上端の帯の段差（40px と 36px）/ 実機確認3件。

### 設計判断

- **#20 は親 Issue として開いたまま維持する。** Phase 2 を 14 本の個別 Issue に割る案・5本程度に束ねる案も
  提示したが、**Issue を増やす手間に対して、いま得られるものが「一覧性」しかない**。
  Phase 0 を #21〜#25 に割ったのは「5件が独立に着手可能で、依存が無かった」からで、
  Phase 2 は依存グラフ（表の「依存」列）が密なので同じ理由が成り立たない
- **#58 を #20 の子 Issue にはしなかった。** これは UI の問題ではなく cwd の追跡モデルの問題で、
  #20 が閉じても独立して残る。#20 本文も「範囲外だが先に決着が要るもの」として区別していた

### 教訓

- **「範囲外だが先に決着が要る」と本文に書いただけでは、起票されないまま埋もれる。**
  #20 本文にこの節が書かれたのは Phase 0 の前で、Phase 0 と Phase 1 を丸ごと走り切っても
  起票されていなかった。**次の Phase の前提になっているものは、書いた周のうちに Issue にする**
- **完了済みの PR が「残りの表」に混ざったままだと、残量を過大に見積もる。**
  PR 7 は Phase 0 で前倒し完了していたのに、Phase 2 の表に残っていた。
  **前倒しで実装したら、元の表の行にも完了を書き戻す**

### 次に再開するとき最初に読むべきこと

1. **Phase 2 の入口は #58（cwd）。** PR 8 / 10 / 13 の3本が #58 の決着待ちなので、
   ここを先に通さないと Phase 2 の半分が着手できない
2. **#58 と独立に今すぐ着手できるのは PR 6 / 11 / 14 / 15 / 16 / 17 / 18 / 19 / 20**。
   このうち **PR 6（Target Size 24x24）が最小**（+25行・画像0枚・依存は完了済みの PR 5 のみ）
3. **Phase 1 の宿題を先に片付けてもよい。** 最優先は選択中タブの下線
   （Phase 1 でただ1つ 3:1 に届かなかった箇所。塗り 1.23）。
   `.sidebar__tabs button.is-active` に `border-bottom: 2px solid var(--accent)` の前例がある
4. 配色を触るなら **S40 の期待値の更新が作業の一部**（推測しない。spec が実測をログに出す）。
   `make css-substitution-check REV=origin/main` は落ちるのが正しい

## 2026-07-29 - Issue #20 本文の分割表にステータス列を足す

追記。前のエントリの棚卸し結果を、**Issue 本文の表そのものに反映**した。

### 実施内容

- Issue #20 本文の「Phase / PR 分割」表に **ステータス列**を挿入（26 行すべて）。
  完了行には対応する Issue 番号と PR 番号を書いた
- 表の直後に「進捗サマリ」と「Phase 1 の積み残し」の2節を追加
- **`overview.md` から同じ表を削除した。** ステータスが Issue 本文に載った時点で二重化になる

### 設計判断

- **ステータスは Issue 本文に置き、ワークスペースには置かない。** CLAUDE.md の
  「Issue 本文をコピーせず、リンクと要約に留める」に従う。
  前のエントリで overview.md に作った表は、Issue 本文に載せた瞬間に古くなる側になった
- **P0-e は「完了」とした。** 表の P0-e（`:focus-visible` 一括 + コントラスト是正3件）は
  独立 PR にならず、Phase 1 の PR #49（枠）と #50（フォーカスリング）へ吸収された。
  上部の Phase 0 一覧の 0-1〜0-5（#21〜#25）とは**行の対応がずれている**ので注意:
  一覧の 0-5（#25 設定ウィンドウ）は表では **PR 7**

### 教訓

- **表の行番号と、そこから切り出した Issue 番号は 1:1 にならない。**
  #20 は Phase 0 を #21〜#25 に切り出したが、表の P0-a〜P0-e と #21〜#25 の並び順が違ううえ、
  P0-e だけ Issue にならず Phase 1 に吸収された。**対応をどこかに書かないと復元できない**ので、
  ステータス列に Issue 番号と PR 番号の両方を書いた
- **順番どおりに進まない計画表は、進捗を持たないと嘘をつく。** PR 7 は Phase 0 で前倒し完了
  していたのに表では未完に見え、残量を 15 本と数えていた（正しくは 14 本）

### 次に再開するとき最初に読むべきこと

1. **進捗は Issue #20 本文の表が唯一の正。** overview.md には要点しか無い
2. 着手順の推奨は前のエントリと同じ: #58（cwd）-> PR 6（最小）-> 選択中タブの下線

## 2026-07-30 - PR 8: タスク行の再設計（語を左・グループ見出し・待たせている時間・ソート）

Phase 2 の2本目（26本中13本目）。PR #82。

### 実施内容

- 状態語を `.task-item__name` の先頭（名前より左）へ移動。走査は左から右
- `groupTasksForDisplay` / `formatGroupHeading`（`src/shared/agent-status.ts`）を新設し、
  「あなたの番 2件」のようなグループ見出しで区切る。並びは あなたの番 -> 作業中 -> 不明 に固定
- `AgentTask.yourTurnSince`（`src/shared/ipc.ts`）を追加。`poller.ts` が busy -> 非busy の
  遷移を検知した時刻を保持し、`formatWaitingSince` で「3分0秒待たせています」と出す
- 遷移検知を `src/main/agents/yourTurnSince.ts` の純粋関数 `computeYourTurnSince` に切り出し、
  `test/unit/your-turn-since.test.ts` で7件固定
- README の該当節にグループ化・並び順・待たせている時間の説明を追加。S12 を撮り直した

### 設計判断

- **未知の status は「あなたの番」に混ぜず、3つ目のグループとして末尾に置いた。**
  CLI が `waiting_for_input` のような値を返し始めても、「あなたの番」の件数に紛れ込ませない
  （誤って人間を急かさない）。鉄則5
- **`yourTurnSince` は永続化しない。** Main の再起動で失われ、そのときは
  セッション起動からの通算（`formatElapsed`）へ縮退する。無い情報を「待たせている時間」として捏造しない
- **`updateYourTurnSince` は `notifyOnIdle` に関係なく常に動かした。**
  待たせている時間の表示は通知設定とは独立した機能
- **unknown への遷移では記録を消さない。** 不明値を挟んだだけで、待たせ続けている可能性のほうが高い

### 教訓（レビューで差し戻した3件）

- **見えない線を「区切り」として足してしまった。** `.task-group + .task-group` に
  `border-top: 1px solid var(--border-subtle)` が入っていたが、サイドバーの面は
  `--surface-0` (#141414) で**コントラスト比 1.31 = 見えない**。しかもコメント自身が
  「線は増やさない」と書き、引用元の `.memo-panel__section`（余白だけで区切る）からも外れていた。
  **`--border-subtle` は「区切りを見せる」用途に使えない**（#56 の design-review が
  `.tab-bar` の下線を証拠に既に指摘していたのと同じ罠を踏んだ）
- **順序を変える変更は、順序に依存した検査を全数掃き出してから入れる。**
  S40 の順序依存セレクタは直したのに S39:34（`.task-item__name` の**先頭行**で
  `demo-project` を引く）を見落とし、`make e2e` で実際に落ちた。
  1箇所直した時点で「同種を他に探す」を必ず挟む
- **両端の純粋関数だけテストしても、真ん中が無テストなら機能は一度も実行されていない。**
  並び（`groupTasksForDisplay`）と文言（`formatWaitingSince`）にはテストがあったが、
  その間の遷移検知にはテストが無く、**E2E の固定フィクスチャは遷移を一度も起こさない**ため、
  PR 8 の目玉である「待たせている時間」はどのテストからも実行されていなかった。
  純粋関数に切り出して7件固定した

### 次に再開するとき最初に読むべきこと

1. **進捗の唯一の正は Issue #20 本文の表。** PR 8 の行を「完了 PR #82」に更新済み
2. **S12 の画像には依然「75時間0分」が写っている。** 固定 `agents.json` は
   busy -> idle の遷移を起こさないので `yourTurnSince` が常に undefined になり、
   README で配布する画像は**新機能が効いていない側**を示し続ける。
   ハーネスに「ポーリング間で内容が変わる `agents.json`」を足す必要があり、
   PR 8 の範囲を超えるため `known-issues.md` に記録した。
   **同種の動的フィクスチャは #56 の PR 7（`pty:resize` の回数カウント）でも要求されている**
3. 次に着手できるのは PR 9（`<li onClick>` の解体の残り + `role="tablist"` + `<main>` + 見出し。
   PR 8 の下流）/ PR 10（タブバーのマーカー + `basename(cwd)`）/ PR 11 / 12 など
4. **`make css-substitution-check` は落ちるのが正しい**（値を意図的に変える PR のため）

## 2026-07-30 - PR 9: タブとサイドバー行のキーボード到達性

Phase 2 の3本目（26本中14本目）。PR #86（当初 #85 として 0-c にスタックしたが、base ブランチを削除付きでマージしたため GitHub に自動クローズされ、base を main にして立て直した）。

### 実施内容

- `TabBar.tsx` に `role="tablist"` / `role="tab"` + roving tabindex。矢印キー / Home / End
- `HistoryList.tsx` / `MemoPanel.tsx` の `<li onClick>` を解体（#64 / PR #80 の流儀に合わせた）
- 履歴一覧に `<h2>` 見出し
- `role="tab"` の `aria-controls` と `TerminalPane` の `role="tabpanel"` を配線。
  id の生成規則は `tabs/tabAriaIds.ts` に閉じ込めた
- 新規 E2E S51 / S52 / S53。既存 spec 7本のセレクタを追従修正

### 設計判断

- **manual activation を選んだ**（矢印キーはフォーカスだけを動かし、選択は Enter / Space）。
  理由は下の教訓を参照。WAI-ARIA APG も「activate でフォーカスが移る場合は manual」と定めている
- **閉じるボタンは `tabIndex={-1}`**。代わりに `role="tab"` にフォーカスした状態の
  Delete / Backspace で閉じられるようにした（APG 推奨）。`tabIndex={-1}` にするだけだと、
  **変更前より能力が減る**（変更前はタブ本体に到達できない代わりに閉じるボタンには届いていた）
- **履歴行は `<button>` の入れ子にできない。** 行の中に「メモ」「編集」という別の
  インタラクティブ要素があるため、resume 部分だけを `<button>` にして兄弟に並べた。
  TaskList には無かった制約
- `.sidebar__tabs`（タスク / 履歴 / メモ）の tablist 化はスコープ外にした。
  既に個別に `<button>` で到達でき、3停止点 -> 1停止点への変更は実挙動の変更になる
- roving tabindex の `tabIndex=0` は、タブリストを離れて戻ったとき**選択中のタブに戻す**。
  「最後に矢印で見ていたタブ」を覚えるより驚きが少ない

### 教訓

- **`tabindex` を書かなかった要素は 0 になる。「書いていない = 停止点でない」ではない。**
  閉じるボタンに `tabIndex` を明示しておらず、Tab の停止点が「1 + タブ枚数」になっていた。
  roving tabindex を入れた目的そのものが未達だった。**E2E を書いていたのに実行していなかったので
  気づけなかった**（ワーカーには並列衝突を避けるため `make e2e` を禁じていた。
  この制約を課すなら、統合時にオーケストレーターが必ず回すこと）
- **ARIA のパターンは、周囲の既存挙動と噛み合って初めて機能する。** automatic activation
  （矢印 = 移動 + 選択）は APG にある正当なパターンだが、このアプリでは
  `TerminalPane.tsx` が `activeTabId` の変化のたびにフォーカスを端末へ移し、
  xterm が Tab を食うため、**矢印キーでの移動が実質1ホップに制限されていた**。
  3つ左のタブには永久に到達できず、**タブ切り替えは既に `Cmd+1-9` があるので、
  この PR が新しく与える能力が実質ゼロだった**。E2E は通っていたので検出されない
- **削除の理由が変更で無効になったコードは、消したままにすると理由を再発見できない。**
  ワーカーは「フォーカスを戻す `tabButtonRefs` は `TerminalPane` とのレースに必ず負ける」
  として削除したが、それは automatic activation を前提にした判断だった。
  manual にするとレース自体が起きないので復活させた

### 次に再開するとき最初に読むべきこと

1. **進捗の唯一の正は Issue #20 本文の表。** PR 9 の行を「完了 PR #86」に更新済み
2. 次に着手できるのは PR 10（タブバーのマーカー + タブタイトル `basename(cwd)` + 閉じるボタンの位置）。
   **PR 10 はタブバーを触るので、`known-issues.md` の 4（閉じるボタンの当たり判定が
   タブ選択領域に約 6.5px 張り出す）をそこで判断すること**。3案が記録してある
3. `known-issues.md` の 5（S12 の画像が「待たせている時間」を示せない）は #83 として起票済み
4. **`.sidebar__tabs` の tablist 化は未着手**。PR 9 のスコープ外とした

### スタック PR の運用でつまずいた点（次回のため）

**base ブランチを `gh pr merge --delete-branch` でマージすると、そこにスタックしていた子 PR は
GitHub に自動クローズされ、しかも再オープンできない**（base ブランチが存在しない PR は
`reopenPullRequest` が失敗する）。子 PR を立て直すしかない。

**スタックしている間は base 側を `--delete-branch` 無しでマージし、子の base を
main に付け替えてからブランチを消すこと。**
