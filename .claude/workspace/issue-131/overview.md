# Issue #131 タブに名前を持たせ、ペイン移動でタブの見出しとウィンドウ名が動かないようにする - Overview

> **Issue**: [#131](https://github.com/i-iwnl/ai-terminal/issues/131)
>
> `TabState` に `title?: string` を持たせ、タブバーの見出し・プロバイダ色・ツールチップ・
> ウィンドウタイトルをそこから引く。無ければ**木の先頭 leaf**から導出する。
> Issue #130 のレビューで 3 対 2 に割れた論点に、第三の案 (d) で決着を付ける。
>
> 詳細は `architecture.md` / `worklog.md` / `known-issues.md`。
>
> **最終更新**: 2026-08-04

---

## 1. ゴール

「タブの見出しがどのペインから借りられているか」を、**`tabDisplayTitle()` という純粋関数1本**に集約する。
タブバー・ウィンドウタイトル・プロバイダ色・ツールチップが全部そこを通る状態にして、
`Cmd+]` でタブの見た目が1つも動かないようにする。

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（renderer のみ。main は変更なし） |
| ブランチ | 未作成 |
| 関連PR | 未作成 |

---

## 2. 完成条件

Issue 本文と1:1。

- [x] `TabState.title?: string` が増え、`tabDisplayTitle(tab)` が `tabPane.ts` の純粋関数として存在する
- [x] `tab.title` が無いタブの見出しが、現状と1文字も変わらない
- [x] 分割中に `Cmd+]` を押しても、タブの見出し・上端の色・ツールチップ・ウィンドウタイトルが1つも変わらない
- [x] タブをダブルクリックして名前を付けると `tab.title` が設定され、**ペインの名前は変わらない**
- [x] 先頭ペインを閉じても、`tab.title` があればタブの見出しが変わらない
- [x] `tab.title` を空文字で確定すると導出に戻る
- [x] 「表示 > ペイン名を変更...」は従来どおりアクティブなペインだけを変える（S85 が green のまま）
- [x] 導出が `test/unit/tab-pane.test.ts` で検証されている
- [x] E2E で「`Cmd+]` でタブの見出しが動かない」が検証されている（**先に赤くなることを確認する**）
- [x] README がタブ名とペイン名の書き分けを説明している
- [x] 型チェック・Lint 通過（`make check`）
- [x] `make e2e` / `make e2e-lint` が通る

---

## 3. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 完了（Issue #130 の5ペルソナレビューの結果を材料に、ユーザーが (d) を選択） |
| 実装 | **完了** |
| 検証 | **完了**（`make check` unit 489 / `make e2e` 95 passed / `e2e-lint` FAIL=0 / 画素検査 全枚数 差0） |

---

## 4. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | commit / push / PR | **ユーザーの明示指示待ち**（CLAUDE.md の Git 操作規約） |
| P1 | `issue-130/known-issues.md` の X1〜X11 を起票する | X8 だけがこの Issue として起票済み。残り10件が未起票 |
| P2 | タブの終了バッジの意味を `every` に寄せる | `issue-56/design-review.md:81` が確定させた仕様と実装がずれたまま。**識別を代表 leaf へ移した今、状態だけがアクティブ leaf 依存で残っていて非対称が見えやすい** |
