# Issue #179 P2: 配色の追従漏れ・レンダラ耐障害性・掲載画像の関門（7件を統合・新規起票を止めるループ） - Overview

> **Issue**: [#179 P2: 配色の追従漏れ・レンダラ耐障害性・掲載画像の関門（7件を統合・新規起票を止めるループ）](https://github.com/i-iwnl/ai-terminal/issues/179)
>
> #159 / #160 の棚卸しで残った P2 を7件束ねて周で回す束ね Issue。7件中4件が「関門が構造的に届いていない」ことに帰着するため、周1 を関門づくりに専念させる。
> **このループの中で新規 GitHub Issue を立てない**（周の中で見つけたものは `known-issues.md` に書き、切り出したくなったらこの Issue の周を増やす）。
>
> 詳細は以下の3ドキュメントに分割:
>
> - `architecture.md` - 触る構造・Contract変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-08-05

---

## 1. ゴール

対象7件（#165 / #169 / #164 / #166 / #167 / #170 / #175）を**7周**で溶かし切り、すべて close する。
「対処方針が決まった」では閉じない。**周1 は値・振る舞いを1つも変えず、関門だけを作る。**

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（renderer + e2e / scripts） |
| ブランチ | 周ごとに `main` から生やす（**スタック PR を作らない**） |
| 関連PR | #183 / #184 / #185 / #186 / #188 / #189 / #191 / #192 / #193 / #198 / #199 |

---

## 2. 完成条件

対象7件がすべて close されていること。周ごとの観測できる完了条件:

- [x] **周1（#165 前半 / #169）** — 完了。S40 / S41 がプロバイダ色3本を `--surface-tab-active` の上でも測り、3:1 を要求すると**実際に赤くなる**ことを実測記録（`2.40` を Received）。`S56-split-pane.png` の SKIP が外れ `check3 ... 最大差 0` が出る（PASS=39 / FAIL=0）。`src/` は1行も触っていない
- [x] **周2（#164）** — 完了。`.tab-bar__tab.is-exited:not(.is-active)` が詳細度 (0,3,0) で `.is-active` と競合しなくなり、**値を1つも変えずに 4.47 -> 13.58**。バッジは `color` の宣言を落として継承させた。実機で選択中/非選択/ホバーの3状態を確認
- [x] **周2.5（#165 後半）** — 完了（#165 close）。PR #186（`@media` の死角）/ #188（`outline-offset: -4px` で帯とリングを引き離し、両立不能を解消）/ #189（輝度を散らして 3:1 と帯どうしの識別を**同時に**改善）。面との比 2.40/2.00/2.26 -> **4.24/3.13/3.66**、帯どうし 1.20/1.06/1.13 -> **1.35/1.16/1.17**。画像0枚
- [x] **周3（#166）** — 完了（#166 close）。PR #191（判定 `tabExitState()`）/ #192（**正常終了から赤を外す。色の値は1本も変えていない**）/ #193（語を `終了済み` / `異常終了` に分け、ペインの読み上げと `announce` も揃える）。`exit` を1回打ってもタブは赤くならず `終了済み` と出る。画像0枚
- [x] **周4-0（S79 の既存の赤）** — 完了（PR #198）。**環境要因**と判定（同一コミットで翌日 11/11 green）。S79 は OS 側（`win.isFullScreen()`）とアプリ側（`.app` のクラス）を分けて待ち、**どちらで止まったかをメッセージに出す**。両方のメッセージが出ることを実測。再現条件の特定は #195 の A-5
- [x] **周4（#167）** — 完了（PR #199・#167 close）。`onContextLoss` -> `dispose()` で DOM レンダラへ。**観測手段は在った**（`WEBGL_lose_context`）ので `limitations.md` 送りにはしていない。関門 S94 を新設。実機で「喪失後に `date` を2回打って両方描画される」まで確認
- [x] **周5（#170）** — 完了。**撮影レーン固有ではなく実機の不具合**だった（`.tab-bar__tabs` の `overflow-x: auto` がメニューを切っていた。実測 9点中1点）。原因は3つあり、それぞれ別の壊し方で赤くなることを確認。関門 S95 を新設。画像2枚（S01 / S06）
- [ ] **周6（#175）** — `Cmd+F` を開くと `term.getSelection()` が空でなければ検索欄に入る。**新しいキーは足さない**。E2E が固定する
- [ ] **周5-b（周5 の design-review 由来）** — メニュー項目のホバー / フォーカスが 1.08 で見えない。`:focus-visible` はマウス起点の `.focus()` で発火しない（2.4.7）
- [ ] **周7（同上）** — アクティブタブの `scrollIntoView`（約110手/日）
- [x] 型チェック通過（`make check`）
- [x] Lintチェック通過（`make check`）

### 順序の制約（動かさない）

1. 周1 -> 周2（測れないまま `@media` を足すと直ったことを確認できない）
2. 周2（#164）-> 周3（#166）（逆順だと #164 の結合状態規則を #166 が踏み潰す）
3. 周1（#169）-> ペインヘッダに触るすべての周

### このループの対象外

- **人が実施**: #148（VoiceOver）/ #151（OS 通知クリック）/ #154（tmux resume）。完了条件に含めない
- **据え置き**: #156（`deferred`）

---

## 3. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 進行中（周1・周2・周2.5・周3・周4-0・周4・周5 完了） |
| 実装 | 進行中（#169 / #164 / #165 / #166 / #167 / #170 が close。**残り #175 のみ**） |
| 検証 | 全周で完了（実機確認を含む）。**S79 の赤は周4-0 で解消**（環境要因と判定し、赤の主語を分けた） |

| 周 | 対象 | 状態 |
|---|---|---|
| 1 | #165 前半 / #169 | **完了**（PR #183） |
| 2 | #164 | **完了**（PR #184 / #185） |
| 2.5 | #165 後半 | **完了**（#186 / #188 / #189） |
| 3 | #166 | **完了**（#191 / #192 / #193） |
| 4-0 | S79 の既存の赤 | **完了**（PR #198。環境要因と判定） |
| 4 | #167 | **完了**（PR #199） |
| 5 | #170 | **完了**（design-review で案の中核が3箇所で覆った） |
| 6 | #175 | 次はここ |
| 5-b | メニューのホバー / フォーカスの可視性 | 未着手（周5 の design-review 由来） |
| 7 | アクティブタブの scrollIntoView | 未着手（同上） |

---

## 4. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | 周6（#175）`Cmd+F` で選択を検索欄へ | **これで #179 の対象7件が全部 close** できる。新しいキーは足さない |
| P1 | 周5-b | メニュー項目のホバー / フォーカスが**メニュー面に対して 1.08**。しかも `:focus-visible` はマウス起点の `.focus()` で発火しない。**見えるようにした結果、初めて画面に出た問題**（`known-issues.md` 11番） |
| P1 | 周7 | アクティブタブの `scrollIntoView`。**約110手/日**。⚠ `S54` が緑なのは根拠にならない（同 11番） |
| P2 | 人が実機で | #195 の A-5（S79 が落ちる日の OS 条件）/ B-1（`zsh` の終了コードの誤爆をどう扱うか） |

**`make e2e` はもう赤くない**（周4-0 で切り分け済み）。次の周で赤が出たら、それは自分が壊したものとして扱ってよい。
