# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-08-04 - ワークスペース作成と周1 の計画

### 実施内容

- `gh issue view 179` と対象7件（#165 / #169 / #164 / #166 / #167 / #170 / #175）の状態を確認。**7件すべて OPEN**
- `origin/main` は `2f82675`（#174 の修正が PR #182 として squash マージ済み）。ローカルの `fix/178-terminal-links-open-in-browser` に残る `e69406d` は**その重複**なので、周1 は `origin/main` から新しくブランチを生やす
- loop.md の計画ゲート「計画書の前提を実コードで測り直す」に従い、2軸を並列調査した
  - 軸A: `e2e/fixtures/contrast.ts` / S40 / S41 / `styles.css` のトークンとセレクタ
  - 軸B: `scripts/verify-screenshots.mjs` / `e2e/screenshots.spec.ts` / `e2e/fixtures/harness.ts` / ペインヘッダ

### 設計判断

- **#165 の対処方針が実コードとずれていた**: `ContrastTarget` には `against?: string` と `againstColor?: string` が**既にある**。
  ハーネスの API 追加は不要で、欠けているのは S40 の計測対象だけ -> `known-issues.md` 1番に記録
- **プロバイダ色は `againstColor: '--surface-tab-active'` で測る**: アクティブなタブは同時に1つしか作れないので、
  3本を1回のセットアップで測るにはトークン値と比べるのが素直。`@media` はトークンを差し替えるので高コントラスト側も同じ書き方で追従する
- **S56 の非決定性は `PROMPT_EOL_MARK=''` で潰す**: SKIP 理由に「zsh の部分行マーカー（反転表示の `%`）」と実測記録がある。
  待ち合わせを増やしても「出るときは出る」ので消えない。**そもそも出さない設定にする**

### 実コードで確認できた事実（周1 の根拠）

| 事実 | 出典 |
|---|---|
| プロバイダ色は `border-top: 2px solid var(--tab-provider-*)` の3セレクタだけで使われる | `styles.css` `.tab-bar__tab--shell/--claude/--gemini` |
| S40 はその3本を `against` 無しで登録している -> `measureContrast` の border 分岐で**親 `.tab-bar`（`--surface-1`）**と比べられる | `contrast.ts` の `item.property.startsWith('border') && el.parentElement ? el.parentElement : el` |
| `@media (prefers-contrast: more)` は8トークンを上書きするが、**`--tab-provider-*` は1本も入っていない**。`--surface-tab-active` は `#2e2e2e` -> `#525252` に上がる | `styles.css` の `@media` ブロック |
| `.tab-bar__tab.is-active`（`--text-bright`）と `.tab-bar__tab.is-exited`（`--status-exited`）は**詳細度が同じ (0,2,0)** で `.is-exited` が後勝ち | `styles.css` の宣言順 |
| `KNOWN_NONDETERMINISTIC` の中身は `S56-split-pane.png` の**1件だけ**。SKIP されるのは check3 のみで check1 / check2 は走る | `scripts/verify-screenshots.mjs` |
| S56 の撮影は**アクティブ側のペインのプロンプトしか待っていない**（2枚目は `toHaveCount(2)` と `.pane-header` 非空だけ） | `e2e/screenshots.spec.ts` の `screenshots S56 分割表示` |
| ハーネスの `.zshrc` は `PROMPT='%1~ %# '` / `RPROMPT=''` を書くが `PROMPT_EOL_MARK` には触れていない | `e2e/fixtures/harness.ts` `launchApp` |
| `make e2e-screenshots` は `docs/images/` に、`make e2e` は `e2e/.screenshots-out` に書く。check の `--dir` 既定は後者 | `Makefile` |

### 教訓（該当する場合）

- **「関門を作る口が無い」と書かれていても、原典を開くと既にあることがある。** #165 は口の追加を求めていたが、
  `against` / `againstColor` は実装済みで、S40 がそれを**使っていなかった**だけだった。
  loop.md が言う「行番号ではなく関数名・セレクタで特定する」を守ったことで、周1 のスコープが実際に縮んだ

### 次に再開するとき最初に読むべきこと

- **周1 は未着手。** `origin/main` から `test/179-contrast-and-screenshot-gates` を切るところから始める
- 周1 でやること（値・振る舞いを1つも変えない）:
  1. S40 に `--surface-tab-active` を背景としたプロバイダ色3本を足し、**赤くなることを実測して worklog に記録する**
  2. S41 に高コントラスト側の同じ3本を足す（`emulateMedia({ contrast: 'more' })` は S41 にしかない）
  3. `harness.ts` の `.zshrc` に `PROMPT_EOL_MARK=''` を足す
  4. `verify-screenshots.mjs` の `KNOWN_NONDETERMINISTIC` から `S56-split-pane.png` を外し、`make e2e` を**3回**回して check3 が安定して通ることを確認する
  5. 効かなければフォールバック（ヘッダを含む決定的な別カットを1枚足す）に切り替える
- **検証順序の注意**: `make e2e-screenshots-check` の比較元は `e2e/.screenshots-out` なので、**`make e2e` を先に回す**（`known-issues.md` 2番）
- 周2 以降の順序制約は `overview.md` の「順序の制約」を見る。動かさない

---

## 2026-08-04 - 周1: 関門を先に作る（#165 前半 / #169）

### 実施内容

- ブランチ `test/179-contrast-and-screenshot-gates` を `origin/main`（`2f82675`）から作成
- **#165 前半**: S40 にプロバイダ色3本 × `--surface-tab-active` の計測を追加。S41 に高コントラスト側を追加
- **#169**: S56 の撮影前の待ち合わせを強くし、`verify-screenshots.mjs` の `KNOWN_NONDETERMINISTIC` を空にした
- **製品コード（`src/`）は1行も触っていない。** 触ったのは `e2e/` 3ファイルと `scripts/` 1ファイルのみ

### 関門が実際に赤くなることの確認（周1 の本体）

**#165 の関門。** 一時的に `toBeGreaterThanOrEqual(3.0)` に差し替えて実行し、赤くなることを確認した:

```
Error: シェルタブの色相の枠（選択中タブ上） が高コントラストで 3:1 を満たしていない
Expected: >= 3
Received: 2.4048299191697136
```

実測値は Issue #165 の表と**小数第2位まで一致**した:

| トークン | 対 `--surface-1` | 対 `--surface-tab-active`（#2e2e2e） | 高コントラスト（#525252） |
|---|---|---|---|
| `--tab-provider-shell` | 5.13 | **4.18** | **2.40** |
| `--tab-provider-claude` | 4.27 | **3.48** | **2.00** |
| `--tab-provider-gemini` | 4.82 | **3.93** | **2.26** |

**「コントラストを上げる」が、この3本のコントラストを下げている**（4.18 -> 2.40）。
`--surface-tab-active` だけ明るくして前景を数え直していないため。

**#169 の関門。** SKIP を外した結果、`make e2e-screenshots-check` の出力から `[SKIP]` が
消え、`check3: S56-split-pane.png の画素が一致する（最大差 0）` が出るようになった（PASS=39 / FAIL=0）。

### 設計判断

- **プロバイダ色3本は S41 の既存 `targets` に入れず、別バッチ + characterization にした。**
  既存ループは全項目に `high > normal` を要求するが、この3本は現時点で**下がる**。
  同じループに入れると周1 で赤くなり `make e2e` を緑に保てない（CLAUDE.md「赤いまま push しない」）。
  代わりに **`PROVIDER_HIGH_NOW` で現在値を固定し、3:1 を満たした瞬間に赤くなる番人**を付けた。
  S40 の `staleFail` と同じ作法で、**周2 で直したときに閾値 assert への切り替えを強制する**
- **S56 の待ち合わせは先頭行の「完全一致」にした。** 部分一致で書いた最初の版は、
  `clear` がエコーされた `demo-project % clear` の状態でも素通りした

### 教訓（該当する場合）

- **⚠ 除外リストに書かれた「非決定の原因」が誤っていた。** `KNOWN_NONDETERMINISTIC` は
  「zsh が部分行マーカーを出す。**実際の端末内容**」と記録しており、これは
  「待っても無駄」と読める。立案時はそれを信じて `.zshrc` に `PROMPT_EOL_MARK=''` を
  足す計画にしたが、**8回撮って比べたら効かなかった**（2種類のまま）。
  正体は素朴な競合で、**待てば決まった**（待ち合わせを足すと 8枚が1種類）。
  しかもその画像は**コミット済みの1枚と1画素も違わなかった** = 画像の差し替えすら不要だった。
  **規約は「原因を実測してから書け」までしか要求しておらず、「なぜ待っても無駄なのか」は
  検証されないまま除外が永続化していた**（`known-issues.md` 3番に記録）
- **自分の計画も同じ壊れ方をする。** 上の `PROMPT_EOL_MARK` 案は `architecture.md` の
  設計判断履歴に「確実」と書いて記録済みだった。**実測が反証したので、その行を
  取り消し線付きで残して差し替えた。** loop.md の「ずれていたら、着手する前に記録を訂正する」は
  Issue 本文だけでなく**自分が数十分前に書いた設計判断にも適用される**
- **「赤くなるか確かめた」の反証テスト自体が空振りすることがある。** S56 の待ち合わせが
  効いているかを確かめるため「`clear` を打つ版」に戻して回したが**緑だった**。
  強い assert が `clear` の完了を待ち切ってしまうためで、この実験は何も分離していない。
  分離できたのは**正規化を外して8回撮り、画像が何種類になるかを数えたとき**だった。
  **assert の成否ではなく、成果物（画像）のばらつきを数えるほうが決定的**

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 38 ファイル / 555 テスト green |
| `make e2e` | **101 passed / 3 flaky**（S47・S60・S90。いずれも `launchApp` 内で、単独再実行は3件とも green。撮影を16回回した直後でマシンが重く、loop.md の「まず負荷を疑う」に当たる。触ったファイルとは無関係） |
| `make e2e-lint` | PASS=743 / **FAIL=0** |
| `make e2e-screenshots-check` | PASS=39 / **FAIL=0**（13枚 × 3 check。`[SKIP]` は0件） |
| 実機確認 | **この周は該当なし。`src/` を1行も触っておらず、画面に出る変化が無い**（`git diff --name-only` は `e2e/` 3件と `scripts/` 1件のみ）。省いたことをここに明記する |

`docs/images/` は**1枚も変更していない**。撮り直した13枚のうち11枚は画素差ゼロのバイト差、
S18 は最大チャンネル差1（許容ノイズ `MAX_CHANNEL_DELTA=2` の範囲内・超過画素0）だったので、
CLAUDE.md の規定どおりコミットに含めず戻した。

### 次に再開するとき最初に読むべきこと

- **周1 は実装・検証とも完了。commit / push / PR はユーザーの指示待ち**（ルート CLAUDE.md）
- ブランチは `test/179-contrast-and-screenshot-gates`（`origin/main` から。スタックしない）
- **周2 に入る前に、この周の PR を main にマージすること。** 周2 は #165 後半（`@media` に
  プロバイダ色3本を追随させる）と #164（`.is-active.is-exited` の結合状態規則）で、
  **周1 の関門が main に入っていないと「直った」ことを確認できない**
- 周2 で必ず触る3箇所（周1 が仕込んだ番人）:
  1. `S41` の `PROVIDER_HIGH_NOW` — 固定をやめ `toBeGreaterThanOrEqual(3.0)` と `high > normal` へ
  2. `S40` の `'終了したタブの文字（選択中・対 --surface-tab-active）'` 4.47 と
     `'終了バッジの文字（選択中）'` 4.47 — #164 を直すと `--text-bright` に戻るので値が動く。
     `wcag: 'fail'` の札も `staleFail` が更新を強制する
  3. `S41` 末尾の `exitedNormal[...] toBeCloseTo(4.47, 1)` 2行 — 同上
- **#164 は値を1つも変えずに直す**（`.tab-bar__tab.is-active.is-exited` を詳細度 (0,3,0) で足すだけ）。
  現状は `.is-active` と `.is-exited` が同じ (0,2,0) で、後に宣言されている `.is-exited` が後勝ちしている

---

## 2026-08-04 - 周2: design-review で案が覆り、関門づくり + #164 に組み替えた

### 実施内容

- 周2 は `styles.css` を触るので `/design-review`（5ペルソナ並列）を計画確定前に通した。
  **#179 本文は「design-review を差し込むのは周3」と書いていたが、起動条件の判定は
  design-review 側の SKILL.md が唯一の正**（loop.md がそう定めている）
- **提案 A（#165 後半）が5人中4人から独立に否定され、却下した**
- 周2 を2本の PR に組み替えた: PR #184（関門の訂正・拡充）-> PR #185（#164）

### 提案 A が却下された理由（実測）

案 = プロバイダ色3本の色相・彩度を保ち、明度だけ上げて対 `#525252` で 3:1 を取る。

| 測る対象 | 現状 | 案 | 向き |
|---|---|---|---|
| 面（`#525252`）との比 | 2.40 / 2.00 / 2.26 | 3.05 / 3.08 / 3.06 | 改善（狙いどおり） |
| **フォーカスリング（白）との比** | 3.25 / 3.90 / 3.46 | **2.54 / 2.56 / 2.55** | **3:1 割れ** |
| **帯どうしの相互比** | 1.20 / 1.06 / 1.13 | **1.01 / 1.00 / 1.01** | **等輝度＝識別不能** |

**両立不能が証明された**: 対 `#525252` で 3:1 には `L >= 0.3531`、対 `#ffffff` で 3:1 には `L <= 0.3000`。
区間が交わらないので **24bit の全色に解が無い**。`border-top: 2px` の真下に
`outline-offset: -2px` の白いフォーカスリングが**隙間 0px で接する**ため。

### 実機確認（agent-browser + CDP）

**E2E は `--disable-gpu` で DOM レンダラを使うが、実機は WebGL（canvas）。** ターミナルの文字は
DOM に無いので `.xterm-rows` が読めない。`agent-browser type` も xterm には届かず、
**1文字ずつ `press` して初めて入った**（`press Enter` だけは効いていた）。

| 確認したこと | 結果 |
|---|---|
| 選択中かつ終了のタブ（class） | `tab-bar__tab tab-bar__tab--shell is-active is-exited` |
| そのタブの文字色 | `rgb(255,255,255)` = `--text-bright`（対 `#2e2e2e` で 13.58） |
| バッジ「終了」の文字色 | `rgb(255,255,255)` — **宣言を落として継承させた結果、タイトルと一致** |
| 閉じるボタン `x` の色 | `rgb(255,255,255)` — レビューが指摘した4つ目の `color: inherit` の波及先 |
| 四角（`--state-slot--exited`） | `rgb(212,123,123)` = `#d47b7b` **据え置き**。終了を運ぶ色として残る |
| **ホバー中**（E2E では作れない） | 文字・バッジ・`x` とも白のまま。閉じるボタンは `opacity: 1 / display: block` |
| **非選択に戻したとき**（2枚目のタブを開く） | 文字・バッジとも `rgb(212,123,123)` に戻る。**選択状態で切り替わることを実機で確認** |

### 設計判断

- **#164 は `.is-active.is-exited { color: var(--text-bright) }` を足す形にしなかった。**
  それだと `--text-bright` が2箇所に散り、`.is-active` の色を将来変えたときに結合状態の規則だけが
  古い色を握って残る（= #164 が直そうとしている壊れ方の再生産）。
  代わりに `.tab-bar__tab.is-exited:not(.is-active)` へ倒し、`--text-bright` の出現は1箇所のまま
- **バッジはセレクタを足さず、`color` の宣言を落とした。** タブの色を継承するので、
  タイトルとバッジが**永久にずれない**。規則が1本増えるどころか1本減る
- **S41 の `exitedTargets` ループを「前景が動くもの / 天井に張り付いたもの」で分けた。**
  白は輝度の天井なので、面が明るくなれば比は必ず下がる（13.58 -> 7.81）。
  **一律に緩めると、まだ追随が要る項目（四角）の番人まで消える**ので分けた

### 教訓（該当する場合）

- **⚠ 周1 が S41 に残した是正指示が誤っていた。** 「`high > normal` の assert に切り替えよ」と
  書いたが、`providerTargets` は**面自体が動く**ので normal と high は別の面の上の値。
  大小比較に意味がない。**`high > normal` が不変条件として意味を持つのは、面が動かない対象だけ。**
  4人が独立に指摘した。**番人のメッセージも、書いた時点の思い込みごと固定されて次の人を誤らせる**
- **周1 が数えた「番人3箇所」は4箇所だった。** `S41` の `exitedHigh > exitedNormal` ループが漏れており、
  #164 を入れた瞬間に赤くなった（実際に赤くなるところまで観測した）
- **`color: inherit` の波及先を2箇所と数えたが実際は4箇所**（`.tab-bar__close` と `.tab-bar__title-input`）。
  `design-rules.md` は Issue #134 の教訓としてこの数え漏れ自体を記録しており、**同じ表を再度踏んだ**
- **ΔE の数値を、測っている座標系を書かずに引用した。** 却下事例の 5.8 は**1型色覚変換後**の値で、
  案が並べた 5.2 は**生の sRGB**。3人が指摘した。**ΔE は「生 / 変換後」を必ず列に書く**
- **「1本の PR にまとめる」根拠が事実として成り立っていなかった。** 「同じ期待値表を触る」と書いたが、
  #165 後半と #164 が触る行は S40 / S41 で**1行も重ならない**（100行以上離れた別ブロック）

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 555 テスト green |
| `make e2e` | 98 passed / 6〜8 flaky（毎回顔ぶれが変わり、いずれも `launchApp` 内で retry 済み。**長時間 Electron を回し続けた負荷**。loop.md「まず負荷を疑う」に当たる） |
| `make e2e-lint` | FAIL=0 |
| `make e2e-screenshots-check` | FAIL=0。**`docs/images/` は1枚も変わらない**（`screenshots.spec.ts` に `exit` 0件 / `emulateMedia` 0件 = 撮影レーンは終了タブにも高コントラストにも構造的に到達できない） |
| `make css-substitution-check` | **意図どおり落ちた**（CLAUDE.md「値を意図的に変えるときだけ落ちてよい」）。出力は `.is-exited` -> `.is-exited:not(.is-active)` と `color: #d47b7b` が 2箇所 -> 1箇所 の3行だけで、**意図しない置き換えが無いことの証明になっている** |
| 実機確認 | 上の表のとおり実施（ホバー中・非選択に戻したときを含む） |

### 次に再開するとき最初に読むべきこと

- **周2 は完了（PR #184 / #185）。#164 は close、#165 は後半が残るので open のまま**
- **次は #165 後半。周を1つ増やして扱う**（`known-issues.md` 4番に設計の制約を全部書いた）。
  **色を明るくするだけでは解けない**ので、輝度以外の軸を使う案から始める:
  - 帯の水平位置をプロバイダごとに変える（`border-top` を `linear-gradient` の `background-image` に）
  - 実線 / 破線で分ける（`repeating-linear-gradient`）
  - フォーカスリングの `outline-offset` を `-4px` にして帯とリングの間に塗りを挟む（1セレクタ2行）
  いずれも**追加ピクセル0・新トークン0**。`/design-review` を計画確定前にもう一度通すこと
- **その周が触る関門**: `S41` の `PROVIDER_HIGH_NOW` と `ADJACENT_HIGH_NOW`。
  後者はフォーカスリング側に 3:1 の閾値 assert が入っているので、**色を動かすと必ず赤くなる**
- 周3（#166）以降の順序制約は `overview.md` を見る。**#164 -> #166 の順序は守られた**

---

## 2026-08-05 - 周2.5: #165 後半の設計と、@media の死角を塞ぐ（PR #186）

### 実施内容

- 周2.5 の案（P: フォーカスリングを引き離す / Q: 線種で識別 / Q': 位置で識別 / R: 値を上げる）を書き、
  **3ペルソナ（macOS / a11y / 保守）でレビューした**（周2 の5人と違い、争点が構造とハーネスに寄っていたため3人に絞った）
- レビューで**案の中核がまた覆った**ので、実装は PR 1 だけに留め、残りを外部記憶へ落とした
- **PR #186（マージ済み）**: `css-tokens.test.ts` の `splitRoot` が `@media` 内の規則を素通りする穴を塞いだ

### レビューで覆った点（3人一致）

1. **「P + Q が入っても既存の関門は全部緑のまま」は誤り。** `make css-substitution-check` は
   行単位のテキスト比較なので必ず落ちるし、`make e2e-screenshots-check` は Q で **S09 / S19 の2枚**が落ちる
   （claude / gemini タブが写る画像はこの2枚だけ）。**「構造だけ・値なし・全緑」という周2.5 の売りが成立しない**
2. **`dotted` は R を打ち消す。** インク被覆 50% で帯の実効輝度が落ち、**色を 3:1 に上げても実効 2.00**。
   点線のまま実効 3:1 には `#cfcfcf` 相当が要る。**形の軸を選ぶときは、その形がインクを何%残すかを先に測る**
3. **P には下辺の副作用がある**（案が書いていない）。選択線とリング下辺が接して白 4px になる
4. **幾何の関門は「色の比」ではなく「隙間の px」を測る。** `ADJACENT_HIGH_NOW` は幾何に対して盲目で、
   **P を入れても数値が1桁も動かない**

### 実装した PR #186 の中身

`splitRoot` がブロックの終わりを `indexOf('\n}')` で探しており、`@media` 内の
インデントされた `:root`（`\n  }` で閉じる）を飛び越して **`@media` 自身の閉じ括弧**に当たっていた。
そのため **`@media` 内の `:root` より後ろの規則が `rest` から丸ごと消え、リテラル検査を素通りしていた**。

**穴が開いていても現状は全部緑になる**（`@media` 内にセレクタ規則が1本も無いため）。
そして #165 後半は高コントラスト側に規則を足す周なので、まさにここを踏む。

**旧実装に差し戻して、新しい検査だけが落ちることを確認した**（1 failed / 26 passed -> 27 passed）。

### 教訓（該当する場合）

- **⚠ `make e2e` の省略可否を、コミットメッセージに書いてから判定した。** 「`test/unit/` だけだから不要」と
  書いたが、CLAUDE.md の判定コマンドを回すと **`test/unit/` は必須側**だった
  （免除は `.claude/` `docs/` `README.md` `CLAUDE.md` のみ）。回して訂正した。
  **「関係なさそう」で省かない、はコミットメッセージを書く手にも適用される**
- **形の軸は「色と独立」ではない。** 破線・点線はインク被覆を下げるので**輝度の軸を食う**。
  「色以外の軸を使えば色の制約から自由になる」という直感が外れる実例
- **レビューを3人に絞った判断は妥当だった**（争点が構造・ハーネスに寄っていた）。ただし
  **run-review.md は5体を既定としている**ので、絞るなら理由を書くこと（ここに書いた）

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 559 テスト green |
| `make e2e` | 99 passed / 5 flaky（すべて `launchApp` 内で retry 済み） |
| `make e2e-lint` | FAIL=0 |

### 次に再開するとき最初に読むべきこと

- **`known-issues.md` の 6番を最初に読む。** 周2.5 の残り（PR 2 / PR 3）の作り方が、
  3人のレビュアーが指定した具体的な手順つきで全部書いてある
- **PR 2 = P（`outline-offset: -4px`）+ 幾何の関門。** 純粋関数 `tabs/tabBandGeometry.ts` +
  `test/unit/tab-band-geometry.test.ts` + E2E（S51 の手順で本物の `:focus-visible` を作る）+
  S78 に「選択中 × フォーカス時の下辺」。**値は変えない・画像0枚**
  - ⛔ `expect(getComputedStyle(btn).outlineOffset).toBe('-4px')` と書かない（宣言の言い換えになる）
  - **`ADJACENT_HIGH_NOW` のフォーカスリング 3:1 assert を gap に条件づけ、逆向きの番人を付ける**
  - **実機確認が必須**（リング右端と `x` の距離が 3px -> 1px。E2E も撮影レーンもフォーカスを作らない）
- **PR 3 = 識別軸（太さ 2/3/4px）+ R（色を 3:1 へ）を `@media` の中だけで。** 線種は使わない（`dotted` は R を打ち消す）
- **周3（#166）以降は手つかず。** `overview.md` の周表と順序制約を見る

---

## 2026-08-05 - 周2.5 完了: #165 後半を3本の PR で閉じた

### 実施内容

- **PR #186** — `css-tokens.test.ts` の `splitRoot` が `@media` 内の規則を素通りする穴を塞いだ
- **PR #188** — `outline-offset: -4px` で帯とフォーカスリングを 2px 引き離し、**両立不能の制約そのものを外した**。幾何を純粋関数 `tabs/tabBandGeometry.ts` に閉じ、E2E は実測値を流し込むだけにした
- **PR #189** — `@media` にプロバイダ色3本を足し、**輝度を意図的に散らした**。#165 close

### 数字（すべて実測）

| | 対 #525252（面） | 帯どうしの相互比 | 対 #1e1e1e |
|---|---|---|---|
| 着手前 | 2.40 / 2.00 / 2.26 | 1.20 / 1.06 / 1.13 | 5.13 / 4.27 / 4.82 |
| **完了後** | **4.24 / 3.13 / 3.66** | **1.35 / 1.16 / 1.17** | **9.04 / 6.68 / 7.81** |
| 却下した「最小値」案 | 3.05 / 3.08 / 3.06 | **1.01 / 1.00 / 1.01** | — |

**すべての指標が着手前より改善した。** 画像は1枚も変わっていない。

### 設計判断

- **識別軸を「形」ではなく「散らした輝度」で持たせた。** 線種は**インク被覆が下がって帯の実効輝度を
  落とす**ので色を上げる効果を打ち消す（点線は被覆 50% で、3:1 に上げても実効 2.00）。
  太さはタブごとに content-box が変わって**タイトルが 1px 縦にずれる**。
  輝度を散らせば帯どうしの比は 1.06〜1.20 -> 1.16〜1.35 と**むしろ改善する**ので、それで足りると判断した
- **役目を終えた番人は外した。** 周1 が置いた `nowPassing` は「3:1 を満たしたら赤くなって
  閾値 assert への切り替えを強制する」ためのもので、実際にそう働いた。
  **残すと常に空配列を返すだけの assert になり、今度はそれが腐る**
- **帯どうしの比に下限（1.06）を置いた。** これが #165 後半の本命の関門で、
  「面との 3:1 だけを見て最小値を選ぶ」案を確実に落とす

### 教訓（該当する場合）

- **「色以外の軸を使えば色の制約から自由になる」は成り立たない。** 破線・点線はインクを間引くので
  輝度の軸を食う。**形の軸を選ぶときは、その形がインクを何%残すかを先に測る**
- **詰んでいたのは色ではなく幾何だった。** 「両立不能」を証明したところで手が止まりかけたが、
  制約の片方（リングとの隣接）は**幾何を変えれば消える**ものだった。
  **証明した不可能性が、前提ごと動かせないとは限らない**
- **⚠ `make e2e` の省略可否を判定コマンドを回す前にコミットメッセージへ書いた**（#186）。
  `test/unit/` は必須側。回して訂正した

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 571 テスト green |
| `make e2e` | 100 passed / flaky はすべて `launchApp` 内で retry 済み |
| `make e2e-lint` | FAIL=0 |
| `make e2e-screenshots-check` | FAIL=0。**画像0枚**（変更が `@media` の中だけで、撮影レーンに `emulateMedia` が0件なので構造的に到達不能） |
| `make css-substitution-check` | 意図どおり落ちる。**出力はトークン3行の追加のみ** |
| 実機確認 | PR #188 で実施（`outline-offset` -4px / 隙間 2px / リング白 / `x` が欠けていないことを画素走査で確認）。**PR #189 は `@media` の中だけなので CDP では到達できず、代わりに S40 と画素検査13枚で「既定側が1ミリも動いていない」ことを機械で担保した** |

### 関門が赤くなることの確認

- PR #188: `outline-offset` を -2px に戻す -> 「隙間が 2px を下回った / Expected >= 2 / Received 0」
- PR #189: 色を足す前 -> 「Expected 2.4 / Received 4.237」（周1 の番人が予告どおり発火）

### 次に再開するとき最初に読むべきこと

- **周3（#166）が次。** 終了表示を severity で分ける（正常終了を赤にしない・語も分ける）。
  **`/design-review` を計画確定前に通す周**（語を変えるため）
- ⛔ **#164 の結合状態規則（`.tab-bar__tab.is-exited:not(.is-active)`）を踏み潰さないこと。**
  順序制約「周2 -> 周3」はこのために置かれている
- **`known-issues.md` 7番は人にしかできない**: macOS の「コントラストを上げる」が実際に
  `@media` に到達するかは一度も観測されていない。**偽なら高コントラスト側の作業
  （トークン11本ぶん）が誰にも届いていない**
- 残りは #166 / #167 / #170 / #175 の4件

---

<!-- 以降、作業のたびにセクションを追記 -->

## 2026-08-05 - 周3: タブの終了表示を severity で分ける（#166・PR #191 / #192 / #193）

### 実施内容

- `/design-review` を**計画確定前に**5ペルソナで通した。**案の中核が3箇所で覆った**ので、初版を編集せず改訂版を別に書いた
- **PR #191** — 判定 `tabExitState()` を切り出す（画面 0px）
- **PR #192** — **#166 の本体。** 正常終了から赤を外し、赤を異常終了だけに絞る
- **PR #193** — 語を `終了済み` / `異常終了` に分け、ペインの読み上げと `announce` も揃える

### 初版の案が誤っていた事実（レビューで検算されて覆った）

| 初版の記述 | 実際 | 指摘 |
|---|---|---|
| **「値を据え置けば S40 の 5.49 / 4.47 は動かない」** | **誤り。必ず動く。** S40 / S41 が測る終了マークは**どちらも `exit`（コード 0）で作った正常終了タブ**。`contrast.ts` は `querySelector`（最初の1件） | IA / 保守 |
| **「語を変えると `aria-label*="終了"` が壊れる」** | **逆。壊れない。** `終了済み` も `異常終了` も部分一致する。**壊れずに恒真化するほうが実害** | IA / a11y / 保守（3人） |
| 「終了を表示している層は4層」 | **6層。** `paneHeader.ts` の `paneAccessibleLabel` と `App.tsx` の `announce` が案の外だった | 4人 |
| 争点の自己弁護「原則2 に従えば色は手がかりに数えない」 | **循環論法。** 同じ論法で周2.5 の却下も覆せる | macOS / IA / a11y（3人が同じ訂正） |
| 「終了があなたの番を追い越さない ✓」 | **符号しか見ていない。** 差が 1.50 -> 1.06 に潰れる。**争点2 で自分が問題視した等輝度を、別のペアで自分が作っていた** | a11y / ヘビーユーザーが独立に実測 |
| 「新トークンに `--text-secondary` の値を使えないのは同値トークン禁止のため」 | **誤り。** `css-tokens.test.ts` の正規表現はリテラル `#hex` 行しか拾わず、`var()` 参照は検査対象外（前例が `styles.css` の `--pane-active-accent`）。**テストを避けるために色を決めていた** | a11y |

### 設計判断

- **正常終了は状態スロットを塗らない**（macOS ペルソナの対案 D-3 を採用）。
  「中立色の四角 / 赤い四角」の2色で表す初版の案は3人が独立に却下した:
  高コントラストで等輝度（1.03）、中立色は「あなたの番」の橙とも輝度が並ぶ（1.06）。
  **層を1つ減らすと、新トークン・`@media` の追随・コントラストの検算・関門の更新がすべて同時に消える。**
  結果、**色の値は1本も変えずに #166 が直った**（`--status-exited` も `#f0b8b8` もそのまま）
- **詳細度は (0,3,0) のまま。** `.is-exited-abnormal:not(.is-active)` に差し替えるだけ。
  初版が書いた (0,4,0) は不要で、説明も不正確だった（競合しないのは詳細度ではなく `:not(.is-active)` が排他だから）
- **`is-exited` は正常・異常の両方に付ける。** これで既存4関門のセレクタと #164 の結合状態規則が生きる
- **`severityForExit()` ではなく `isAbnormalExit()` を呼ぶ。** 前者の `'info' | 'error'` は
  通知バナーの表現であって事実ではない。タブが借りると、バナーの表現を増やしたときにタブの色が動く
- **可視の語は4文字固定。** 生値は `aria-label` と `title` だけ。`title` は
  プロバイダを**上書きせず連結**する（終了した瞬間にプロバイダの非色手がかりを失わせない）
- **順序を「語 -> 色」から「色 -> 語」に入れ替えた。** a11y / 保守は「等輝度を許容する前提が語だから語が先」と言ったが、
  **D-3 を採ったことで等輝度そのものが消滅した**ので、その依存関係の根拠が無くなった。
  run-review.md の「統合で1つでも値を変えたら、それに依存する対案を全部計算し直す」に従った

### 関門

- **S40 に2件を新設。** `正常終了したタブの文字（非選択）` 7.01 / `異常終了したタブの文字（非選択）` 5.49。
  **これが無いと、この Issue の中核を守るものが1つも無かった**（`.is-exited:not(.is-active)` に
  赤を書き戻しても全 spec が緑のままだった。`非選択タブの文字` は最初の1件 = 生きている1枚目を拾う）
- **S40 / S41 の fixture を `exit 7` へ。** 比の値は変わらない（同じ色・同じ面）
- **S87 にケース4。** 分割した片方を `exit 7`・片方を `exit`。`some` 集約と
  「終了2枚に対して四角は1つ」を**色を1つも読まずに枚数だけで**固定する
- **S87 の `[aria-label*="終了"]` を語ごとの一致へ。** 部分一致は語を分けても通るので恒真化する
- `test/unit/tab-exit-copy.test.ts`（14本）。4文字固定・`強制終了` 禁止・Label in Name

### 教訓（該当する場合）

- **⚠「値を据え置いたから関門も動かない」は、関門が何を測っているかを確認するまで言えない。**
  S40 は `querySelector`（最初の1件）で対象を選ぶので、**同じ色でも、その色が付く要素が変われば測定対象が変わる。**
  この誤認は案の PR 分割の根拠そのものだった
- **⚠ 部分一致で語を固定した関門は、語を細分化すると壊れずに恒真化する。** `[aria-label*="終了"]` は
  `終了済み` にも `異常終了` にも `強制終了` にも通る。**壊れないことが実害**で、
  「関門が赤くなるか」を確かめる習慣だけでは見つからない（そもそも赤くならないのだから）
- **状態を色で分ける前に「その状態にピクセルを割かない」を検討する。** 周2.5 は
  「詰んでいたのは色ではなく幾何だった」と書いたが、今回は**そもそも層が要らなかった**。
  塗らないと決めた瞬間に、等輝度の議論・新トークン・`@media` の追随・S40/S41 の期待値更新が全部消えた
- **等輝度を許容してよいのは、常時可視の非色手がかりがその層の外にあるとき。**
  「原則2 に従えば色は手がかりに数えない」だけでは、あらゆる等輝度を正当化できる（周2.5 の却下も覆せる）。
  分かれ目は「代替手がかりが**常時可視**か」で、ツールチップ（ホバー必須）は数えない
- **⚠ E2E の `.terminal-pane.is-active` は、タブが2枚以上あると複数一致する**
  （`visible` はタブ単位・`active` はペイン単位）。`:not(.terminal-pane--hidden)` と併用する
- **⚠ `Meta+d` の直後は、まだ分割前のペインが `.is-active` のまま。** プロンプト待ちが
  そちらで即座に通り、focus + type が分割前のペインに入って「打った文字がどこにも無い」ように見える。
  **可視ペインが2枚になるのを先に待つ**
- **⚠ `npx playwright test --config=e2e/screenshots.playwright.config.ts` は `docs/images/` に直接書く。**
  知らずに叩いて `S56-split-pane.png` をコミットしかけた（画素差ゼロのバイト差だったので戻した）。
  **判定レシピは、変わったと分かっている別画像で先に較正してから使った**

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 40 ファイル / **592 テスト green**（571 -> 592） |
| `make e2e` | **101 passed**。flaky は毎回顔ぶれが変わりマシン負荷 |
| `make e2e-lint` | PASS=743 / **FAIL=0** |
| `make e2e-screenshots-check` | PASS=39 / **FAIL=0**。**画像0枚**（13枚に終了したタブは1枚も写っていない） |
| `make css-substitution-check` | PR #192 で意図どおり落ちる（**出力は3行だけ**）。#193 は PASS |
| 実機確認 | PR #192 / #193 の両方で実施（下記） |
| **S79** | ⚠ **failed。ただし `main` に戻して再ビルドしても同じ箇所で落ちる既存の赤**（`known-issues.md` 8番） |

実機（agent-browser + CDP。WebGL レンダラ）:

| タブ | class | color | 四角 | バッジ | title |
|---|---|---|---|---|---|
| 生存（選択中） | `is-active` | `#ffffff` | 無し | 無し | `シェル` |
| **正常終了（非選択）** | `is-exited` | **`#a8a8a8`（生存と同じ。赤くない）** | **無し** | `終了済み` | `シェル・終了済み` |
| 異常終了（非選択） | `is-exited is-exited-abnormal` | `#d47b7b` | 1つ | `異常終了` | `シェル・異常終了（コード 7）` |

タブ幅は終了済み / 異常終了とも **133px で一致**（4文字固定）。
分割したペインの `aria-label` は `…、異常終了（コード 7）` で、**そのときタブ側には何も出ない**（`every` の門）。

### 関門が赤くなることの確認

- PR #191: `some` -> `every` -> unit 2本が落ちる
- PR #192: 実装を差し戻す -> `正常終了したタブの文字（非選択）` が `Expected 7.01 / Received 5.492…`。S87 も同時に落ちた
- PR #193: `exitWord()` を `'終了'` に戻す -> S87 の「読み上げ『終了済み』」が `Expected 1 / Received 0`、unit 8本

### 次に再開するとき最初に読むべきこと

- **周3（#166）は完了。#166 は close 済み。残りは #167 / #170 / #175 の3件で、次は周4（#167）**
- **`known-issues.md` の 8番〜10番がこの周で増えた。** とくに **9番（`zsh` の終了コードは
  直前のコマンドの終了ステータス）は、この周で入れた赤の意味に直接効く**。
  `grep` を空振りした直後に `exit` した shell タブが「異常終了」で赤くなる（実機で追試済み）。
  **この周では判定を変えなかった**（#166 が「`isAbnormalExit()` を使い回す。新しい判定を書かない」と
  明示指定していたため）。周を1つ増やすかはユーザー判断
- **8番（S79 の既存の赤）を先に見ること。** `make e2e` が赤いまま3周ぶんの PR を通しているので、
  次の周で「自分が壊した」と誤読しないように
- **`known-issues.md` 7番（macOS の「コントラストを上げる」）は依然として人にしかできない。** 未検証のまま

---

## 2026-08-05 - 周4-0: S79 の赤を切り分ける（環境要因と判定。関門の主語を分けた）

### 実施内容

`known-issues.md` 8番（`make e2e` の S79 が落ちたまま）を、周4 に入る前に片付けた。

| 測ったこと | 結果 |
|---|---|
| S79 単体（`npx playwright test S79`） | **green**（3.4s） |
| フル `make e2e` | **102 passed / 2 flaky**（S44・S60。どちらも `launchApp` の起動タイムアウト） |
| S79 単体 `--repeat-each=10` | **10/10 green**（41.2s） |

**同一コミット・同一マシンで 11/11 green。** 間の差分は `.claude/` と `docs/` だけで
`src/` は1行も動いていない（`git log` で確認）。2026-08-04 は `main` に戻して再ビルドしても
落ちていたので、**日付の側が変数**だった。**アプリの回帰ではない = 周は増やさない。**

### 設計判断

- **実害は「落ちること」ではなく「赤の主語が読めないこと」だと切り分けた。**
  2026-08-04 は関門が赤いまま PR を3本通し、`main` へ戻して再ビルドする切り分けまでやっている。
  再現条件を特定しても（それは人力）、次に落ちた日の読み違えは防げない
- **そこで S79 を OS 側とアプリ側に分けて待つようにした。**
  `win.setFullScreen(true)` は AppKit へ要求を出すだけで、Space を作るかは WindowServer の状態次第。
  遷移しなければ `enter-full-screen` は発火せず `.app` にクラスは永久に付かない。
  **その区間はこのリポジトリのコードではない**ので、`win.isFullScreen()` を先に待って主語を分ける
- ⛔ **`test.skip()` にはしなかった。** 環境要因でも赤は赤で出す。飛ばすと
  `enter-full-screen` の経路が壊れた日に誰も気づかない（**周3 で踏んだ「部分一致の関門が恒真化する」と同じ形**）
- **再現条件の特定は #195 の A-5 へ。** 別アプリをフルスクリーンにする・システム設定を切り替える、が要る
  = エージェントには不可能。**急がない**（次に環境要因で赤くなった日に見ればよい）と明記した

### 関門が赤くなることの確認（両方の主語で1回ずつ）

| 壊し方 | 出たメッセージ |
|---|---|
| `BrowserWindow.prototype.setFullScreen` を no-op に差し替える | **「macOS がフルスクリーン遷移を行わなかった（win.isFullScreen() が false のまま）。アプリの回帰ではなく環境要因」** |
| `win.on('enter-full-screen')` の送信を落として**再ビルド** | **「Main は遷移したのに Renderer に届いていない（IpcEvent.fullScreenChanged の経路）= アプリの回帰」** |

**2つ目は `npm run build` を挟んで実測した**（`out/` を見るので、`src/` を壊しただけでは緑のまま通る）。
壊した実装は両方とも戻し、戻したあと S79 が green に返ることまで確認した。

### 教訓（該当する場合）

- **⚠「`main` でも落ちる」は「アプリの回帰ではない」までしか言わない。** 環境要因だと決めるには
  **同じコミットが緑になるところ**まで見る必要がある。周3 は前者で止まっていたので、
  「既存の赤」というラベルのまま丸1日残った
- **関門が環境に触るなら、環境の区間とアプリの区間を別々に待つ。** 1本の assert で両方を跨ぐと、
  落ちたときに主語が読めず、**切り分けのコストが毎回かかる**（この Issue では2日ぶん払った）
- **落ちない不具合の再現を追うより、落ちたときに主語が出るようにするほうが安い。**
  再現条件は人力でしか特定できず、しかも特定しても読み違えは減らない

### おまけで見つけた、関門の判定コマンド自体の壊れ方（loop.md を直した）

**`make e2e` の省略可否を決める判定コマンドが、この環境では必ず「省略可」を返していた。**

`loop.md` に書いてあったのは `grep -qvE ... && echo 必須 || echo 省略可` で、
`e2e/specs/` を2ファイル触ったこのブランチで叩いたら **「e2e 省略可」**と出た。切り分けた結果:

```
printf 'x\ny\n' | grep -qv 'x'          -> 1   # Claude Code のシェルは grep が ugrep のラッパ関数
printf 'x\ny\n' | command grep -qv 'x'  -> 0   # BSD grep
printf 'x\ny\n' | grep -cv 'x'          -> 1   # ugrep でも -cv は正しい（-q との併用だけが壊れる）
```

**外れ方が「関門を飛ばす側」に倒れる。** `loop.md` を、終了ステータスに頼らず
**該当ファイル名をそのまま出す**形（`command grep -vE`）に直した。**判定の正は必ずコマンド**という
規約自体は変えていない。壊れていたのは規約ではなく道具のほう。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 40 ファイル / **592 テスト green** |
| `make e2e` | **102 passed / 2 flaky**（S79 を含めて green。切り分けで先に実施） |
| `make e2e-lint` | PASS=743 / **FAIL=0** |
| `lint-skills.sh` | PASS=93 / **FAIL=0**（`limitations.md` が 533行になり上限超過 -> 分割） |
| 撮影 | **不要**（`e2e/specs/` と `.claude/` しか触っていない。UI・CSS・DOM 構造は 0行） |
| 実機確認 | **不要**（画面に出るものを1つも変えていない。変えたのは spec の待ち方と文書だけ） |

**`limitations.md` を2つに割った。** S79 の節を足して 533行（上限 500）を超えたため、
「spec を書くときに繰り返し踏んだ落とし穴」を `reference/spec-writing-traps.md` へ H2 境界で分離
（147行）。分割前後の全行を正規化して突き合わせ、**落ちた行が無いことを機械で確認**した。
`SKILL.md` の索引と `run-e2e.md` の参照先も更新済み。

### 次に再開するとき最初に読むべきこと

- **次は周4（#167）。** `WebglAddon` の `onContextLoss` で DOM レンダラへ落とす
- **観測手段はある見込み。** `WEBGL_lose_context` 拡張（`canvas.getContext('webgl2').getExtension('WEBGL_lose_context').loseContext()`）で
  コンテキスト喪失を意図的に起こせる。**S23 が GPU 有効で画素を数えている**ので、
  そのレーンを流用して「喪失後に DOM レンダラ（`.xterm-rows`）で文字が出ている」ことを見られるはず。
  **無理だと分かったら理由を `limitations.md` に書く**（#167 の完了条件に明記されている）
- **`make e2e` はもう赤くない。** 次の周で赤が出たら、それは自分が壊したものとして扱ってよい

---

## 2026-08-05 - 周4: WebGL コンテキストを失っても文字を消さない（#167）

### 実施内容

- `useTerminal.ts` の `WebglAddon` に `onContextLoss` を足し、喪失時に `dispose()` して DOM レンダラへ落とす
- **E2E S94 を新設**（GPU 有効レーン。`WEBGL_lose_context` 拡張で意図的に失わせる）
- `/terminal` の `xterm-setup.md` に「起動後にコンテキストを失っても DOM レンダラへ落とす」節を追加

**#167 の完了条件にあった「観測手段が無ければ理由を limitations.md に書く」は不要になった。**
観測手段はあった（下記）。**書かずに済ませたのではなく、在ることを実測して確かめた。**

### 設計判断

- **`onContextLoss` は `loadAddon` の前に登録する**（xterm.js のドキュメントどおりの順序）
- **WebGL へ戻す試みはしない。** 文字が出続けることが最優先で、復帰の再試行は
  「白いキャンバスのまま待つ」時間を作る側に働く。**アドオン自身が3秒待って
  `webglcontextrestored` を見ている**ので、そこで戻らなければ諦めてよい
- **本番の失われ方（コンテキスト上限超過）は E2E で作らない。** ペインを16枚以上開く必要があり重い。
  `WEBGL_lose_context` は Chromium が同じ `webglcontextlost` を発火させる標準拡張で、
  **アドオンから見た経路は本番と同じ**

### 関門

- **S94 新設。** 前提（WebGL で動いている・`.xterm-rows` が**無い**）を先に固定してから失わせ、
  **`.xterm-rows` に文字が戻ってくること**を見る
- ⛔ **canvas が消えたことだけを見ない。** 見るのは文字が DOM に戻ること。canvas の有無は手段でしかない
- **プロンプトがバッファに入るまで S23 と同じ画素ポーリングで待つ。** 待たずに失わせると、
  「文字が戻ってこない」のか「そもそも出ていない」のかを分けられない

### 関門が赤くなることの確認

`onContextLoss` を入れる前（`git stash` して**再ビルド**）:

```
Error: WebGL コンテキストを失ったあと、文字が DOM に戻ってきていない
       = 晴眼の利用者にだけ画面が消えている（onContextLoss を購読していない）
Expected pattern: /[$%#>]/ / Error: element(s) not found
```

### 教訓（該当する場合）

- **⚠ `.xterm-screen` の canvas の1枚目は WebGL のものではない。**
  `.xterm-link-layer`（2D コンテキスト）が**先に**並んでいて、`getContext('webgl2')` が null を返す。
  最初この形で書いて「WebGL コンテキストを取得できない」で空振りした。
  **WebGL コンテキストを持つ1枚を探し当てる**
- **⚠ `.xterm-rows` は DOM レンダラの持ち物で、WebGL レンダラでは DOM に存在しない。**
  「行が在ること」を待とうとして 15 秒溶かした。**この非存在こそが「戻ってきた」と言えることの前提**なので、
  待つのではなく `toHaveCount(0)` で固定した
- **`console.log('webglcontextlost event received')` はアドオン自身が出している。**
  ログの出所を自分のコードだと誤認しないこと

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 40 ファイル / **592 テスト green** |
| `make e2e-lint` | PASS=751 / **FAIL=0**（S94 の追加ぶん 743 -> 751） |
| `lint-skills.sh` | PASS=93 / **FAIL=0** |
| `make e2e` | （このエントリの下で実施） |
| 撮影 | **不要**（UI・CSS・DOM 構造は0行。変えたのは喪失時の挙動だけ） |
| **実機確認** | **実施した（下記）。この周では E2E より実機のほうが本番に近い** |

実機（agent-browser + CDP。**本番と同じ WebGL レンダラ**）:

| 段階 | canvas | `.xterm-rows` | 内容 |
|---|---|---|---|
| 喪失前 | 2枚（`.xterm-link-layer` と WebGL） | **0個** | 画面は canvas に描かれている |
| 喪失後（5秒待つ） | **0枚** | **1個** | `yoshinaga@MacBook-Pro ai-terminal %` |
| 喪失後に `date` を2回打つ | 0枚 | 1個 | **両方とも実行され、DOM レンダラで描画された**（12:06:07 / 12:06:28） |

**落ちたあとも入力・実行・描画が普通に続く**ところまで見た。

### 次に再開するとき最初に読むべきこと

- **周4（#167）は完了。次は周5（#170）** — `docs/images/S06-new-tab.png` の「+ ▾」メニューが空箱
- **周5 の下見で分かっていること**: 画像を開くと、注釈3が囲む矩形の内側が一様な `#1e1e1e`。
  さらに**注釈1と2の吹き出しがタブバーそのものを覆っている**（3枚のタブが1枚も見えない）。
  #170 の本文は「メニューが空箱」しか言っていないが、**同じ画像の別の壊れ方も同時に見える**
- メニューは `document` の `mousedown` で閉じる（`TabBar.tsx`）。注釈レイヤーは
  `pointerEvents: 'none'` の `position: fixed` なのでクリックはしていない。**閉じている仮説の裏取りが要る**
- 残りは #170 / #175 の2件

---

## 2026-08-05 - 周5: 「+ ▾」メニューが実機で見えていなかった（#170・design-review で案の中核が3箇所で覆る）

### 実施内容

**#170 は「撮影レーン固有か実機でも起きるかの切り分け」が完了条件だった。答えは実機。**
しかも「画像が空箱」より重い症状で、**押すとタブバーの中身が画面外へ消えていた**。

- 実機（CDP）で切り分け -> **`.tab-bar__tabs`（`overflow-x: auto`）のクリップ**と特定
- **`/design-review` を計画確定前に5ペルソナで通した**。初版の中核が3箇所で覆ったので改訂版を別に書いた
- 関門 **S95 を新設**（3構成 × 3項目 × 3点 = 当たり判定で見る）
- 構造 + `right: 0` + `z-index: 21` + `no-drag` を実装。`S51` の入れ子 assert を削除
- `docs/images/` は **S01 と S06 の2枚**を撮り直した

### 初版の案が誤っていた事実（レビューで検算されて覆った）

| 初版の記述 | 実際 | 指摘した人 |
|---|---|---|
| 「`Cmd+T` / `Cmd+Shift+C` / **`Cmd+Shift+G`** があるので困らない」 | **Gemini は `Cmd+Shift+E`。`Cmd+Shift+G` は「前を検索」**で、しかも **#62 でわざわざ剥がした割り当て**。**直した不具合の側を現状として引用していた** | macOS / IA / ヘビー / a11y（**4人**） |
| 「CSS の変更はコメントのみ。メニューの指定はそのまま使える」 | **誤り。原因は3つあり、入れ子を直すと消えるのは1つだけ** | **4人** |
| 「壊れる spec は無い」 | **`S51:74-77` が確実に落ちる**（`.tab-bar__tabs > .tab-bar__new-wrapper > .tab-bar__new` を `toHaveCount(1)`） | ヘビー / a11y / 保守（**3人**） |
| 「撮り直しは1枚」 | **2枚**（`S01` の注釈2が `.tab-bar__tabs` の矩形を囲っているので右辺が 36px 動く） | 保守 |
| 関門「メニュー**中心**の `elementFromPoint`」 | **中心はちょうど2番目の項目に当たる**ので上下 1/3 のクリップを素通しする | **5人全員** |
| 関門「`scrollTop` が 0 のまま」 | **修正後は恒真化**し、**修正前でも `click()` 直後に読むと 0**（スクロールは `focus()` の effect で起きる） | ヘビー / a11y / 保守（**3人**） |
| 「実機でメニューは**1ピクセルも見えない**」 | **未証明だった。実測は 9点中1点が当たり判定に出ていた**（先頭項目の一部が帯の中に描かれている） | IA / a11y（2人が独立に幾何から） |

### 原因は3つあった

| # | 原因 | 直し方 |
|---|---|---|
| (a) | `.tab-bar__tabs` の `overflow-x: auto` -> **`overflow-y` も `auto` に計算される**（CSS Overflow 3） | 包含ブロックをスクロールコンテナの外へ |
| (b) | `z-index: 15` が `.notice-list`（**20**）に負ける。通知は自動消去も Escape も無い | 21 へ（モーダルの 30 未満） |
| (c) | `left: 0` + `min-width: 160px` で**ウィンドウ右端からはみ出す**。`body { overflow: hidden }` で黙って消える | **`right: 0`** |

**(c) は A を入れて初めて露出する。** タブが溢れると `.tab-bar__drag-region`（`flex: 1` / basis 0）の実幅が 0 になり、
「+」が設定ボタンに密着する。**ウィンドウ幅に依存せず必ず起きる。**
右端揃えなら**はみ出さないことを算数で言える**（アンカーの右端は最小でも 296px あり、160px は必ず内側）。**JS は要らない。**

### なぜ1年近く誰も気づかなかったか（**関門が4本あって、4本とも通っていた**）

| 関門 | なぜ通るか |
|---|---|
| `toBeVisible()`（S06 / S64） | **祖先のクリッピングを見ない** |
| 項目のクリック（S06 / S64 / S69 / S78） | Playwright が `scrollIntoViewIfNeeded()` を挟むので**切られていても押せる** |
| キーボード操作（S64） | 開いた瞬間に先頭項目へフォーカスが乗るので**見えないまま矢印 + Enter で通る** |
| 日常利用 | `Cmd+T` / `Cmd+Shift+C` / `Cmd+Shift+E` があるので、メニューを使わなくても困らない |

**「関門が無かった」のではなく「4本あって4本とも見えていないことを見ていなかった」。**
周3 の「部分一致の関門は壊れずに恒真化する」と同じ族で、**壊れないことが実害**。

### 関門が赤くなることの確認（原因ごとに1回ずつ）

| 壊し方 | 結果 |
|---|---|
| 実装前（入れ子がスクロールコンテナの中） | **「9点のうち 1点しか当たり判定に出ていない」**（当たったのは `.tab-bar__drag-region` / `.xterm-screen`） |
| `right: 0` を `left: 0` に戻す | **「タブ12枚 + 通知バナーあり: 9点のうち 6点」**（当たったのは `null（ビューポート外）` ×3） |
| `z-index: 21` を 15 に戻す | **「タブ12枚 + 通知バナーあり: 9点のうち 6点」**（当たったのは `.notice-banner--error`） |

**3つの修正がそれぞれ独立に効いていることを、別々の壊し方で確認した。**

### 実機確認（本番と同じ WebGL レンダラ）

| 構成 | 結果 |
|---|---|
| タブ1枚 | 3項目とも当たり判定に出る。メニュー矩形 226〜386 / `scrollTop` 0。**タブも消えない** |
| タブ12枚（`scrollWidth` 1080 / `clientWidth` 855 = 溢れている） | 3項目とも当たり判定に出る。**矩形 991〜1151 でウィンドウ（1200）の内側**。「+」も設定ボタンの隣に残る |

### 画像（撮り直し2枚。**13枚中11枚は画素差ゼロだったので戻した**）

| 画像 | 変わった理由 |
|---|---|
| `S06-new-tab.png` | **メニューが描画されるようになった**（bbox 260,0-740,210）。これがこの周の成果物そのもの |
| `S01-launch.png` | 注釈2（`.tab-bar__tabs` を囲む枠）の**右辺が 36px 縮んだ**（bbox 347,0-395,44）。「+」が矩形の外に出たため。**枠の意味はむしろ正確になった**（キャプションは「タブバー: シェルや AI のタブを切り替える」） |

### 教訓（該当する場合）

- **⚠ `overflow-x: auto` は `overflow-y` も `auto` にする**（CSS Overflow 3）。**横スクロールのつもりで縦に切る**
- **⚠ `toBeVisible()` は祖先のクリッピングを見ない。Playwright のクリックは要素へスクロールしてから押す。**
  この2つが揃うと、**「見えていない」を検出できない関門しか作れない**
- **⚠ 「見えない」を測るときは点ではなく複数点で。** 中心1点は、中央の項目に当たるので上下のクリップを素通しする
- **⚠ 「1ピクセルも見えない」を根拠なしに書かない。** 実測は 9点中1点で、**先頭項目の一部は描かれていた**。
  レビュー2人が幾何から独立に否定した。**症状の記述が違うと、関門の設計も変わる**
- **⚠ `npx electron .` は「プロセスは立つがウィンドウが出ない」状態に落ちることがある**（`known-issues.md` 12番）。
  最小の Electron アプリでも再現したので**起動経路の問題**。バイナリ直叩きに変えた
- **関門を先に赤くしてから実装する形は、原因が複数あるときに特に効く。** 3つの修正それぞれについて
  「これを戻すと何点になるか」を測れたので、**どれも要ることを証明できた**

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 592 テスト green |
| `make e2e-lint` | PASS=759 / **FAIL=0**（S95 のぶん 751 -> 759） |
| `lint-skills.sh` | PASS=93 / FAIL=0 |
| `make e2e` | （このエントリの下で実施） |
| `make e2e-screenshots` | **13枚中、画素が変わったのは2枚**（S01 / S06）。残り11枚はバイト差のみなので戻した |
| 実機確認 | 3構成で実施（上表） |

### 次に再開するとき最初に読むべきこと

- **周5 は完了。残りは #175（周6）だけ。** #170 は close する
- **周5-b を追加した**（`known-issues.md` 11番）。**#170 の目的を満たしきるために要る**:
  メニュー項目のホバー / フォーカス面が **1.08 で、しかも暗くなる**。さらに **`:focus-visible` は
  マウス起点の `.focus()` で発火しない**ので、**マウスで開くとどの項目が選ばれているか見えない**（2.4.7）。
  **見えるようにした結果、初めて画面に出てくる問題**
- **周7 を追加した**（同 11番）。アクティブタブの `scrollIntoView`。ヘビーユーザーの実測で **約110手/日**。
  ⚠ **`S54` が12枚で緑なのは、この不具合が無いことの根拠にならない**（キーボード経路は `focus()` の副作用で
  スクロールしている。壊れているのは `Cmd+J` / `Cmd+E` / `Cmd+1-9` のような **DOM フォーカスを動かさない経路**）
- 残りの周: **周6（#175 Cmd+F）-> 周5-b -> 周7**。#179 を close できるのは #175 まで終えた時点

---

## 2026-08-05 - 周6: Cmd+F が選択範囲を検索欄へ引き継ぐ（#175）

### 実施内容

- 判定を純粋関数 `searchSeed.ts` に切り出し、`test/unit/search-seed.test.ts`（7本）で固定
- `onSearchVisibilityChange(visible, seed?)` に `seed` を足し、開くときだけ選択から引き継ぐ
- **E2E S96 を新設**（3段。うち1段は書き直した。下記）
- README のショートカット表と「キーの選び方」の節を更新

**`/design-review` は起動していない。** 起動条件（`styles.css` / 文言・ラベル・空状態 / サイドバー・タブバー・
通知・設定の構造 / 状態の見せ方）のいずれにも当たらない。**検索欄に入る値が変わるだけ**で、画面の構造も語も動かない。

### 設計判断

- **引き継がない = 検索欄を触らない**（空にするのではない）。端末の行は右端まで空白で埋まっているので、
  **行末の余白を撫でただけで空白の塊が取れる**。それで上書きすると
  **`Cmd+F` が「前回の語をもう一度探す」用途に使えなくなる**
- **複数行の選択は引き継がない。** `@xterm/addon-search` は行をまたげないので、入れても必ず0件になり、
  しかも**引き継がなければ使えたはずの前回の語が消える**
- **前後の空白は落とし、中の空白は残す**（`git status` のような語句をそのまま探せる）
- **`searchTermRef` も同時に更新する。** `Cmd+G` は入力欄ではなくこの ref を見るので、
  ここを忘れると「検索欄には語が出ているのに `Cmd+G` は前回の語を探す」ズレができる
- **自動で検索は実行しない。** 既存の「検索語が見えていない状態から検索しない」方針と揃える。
  引き継いだ語は**見えている**ので、Enter は利用者が押す（#175 の「2手」と一致）

### 関門が赤くなることの確認（2つの向きで）

| 壊し方 | 出たメッセージ |
|---|---|
| 引き継ぎを無効化（`seedSearchFromSelection` が常に undefined） | **「選択した語が検索欄に入っていない（Cmd+E を奪ったままの代替が働いていない）」** |
| 「選択が無ければ空にする」に変える | **「選択が無いのに検索欄が書き換わった」** |

### ⚠ 自分で書いた関門が、書いた直後に恒真だった（書き直した）

初版の S96 の3段目は「選択を解いて開き直すと **`S96-SEED-ME` が残っている**」を見ていた。
**これは何も区別していない** — 選択が解けていなければ、同じ語がもう一度引き継がれて**同じ値になる**。

`spec-writing-traps.md` の「測る対象そのものが違う」と同じ形。
**選択からは絶対に出てこない語（`S96-TYPED-BY-HAND`）を手で入れてから測る**ように直した。
初版に付けていた `window.getSelection()` の待ちも外した（**WebGL レンダラでは常に空**なので、
DOM レンダラでたまたま通っていただけの、意味を持たない前提だった）。

### 検証

| 関門 | 結果 |
|---|---|
| `make check` | 41 ファイル / **599 テスト green**（592 -> 599） |
| `make e2e-lint` | PASS=767 / **FAIL=0** |
| `make e2e` | （このエントリの下で実施） |
| 撮影 | **不要**（検索欄の値が変わるだけ。UI・CSS・DOM 構造は0行） |
| 実機確認 | 実施（下記） |

実機（agent-browser + CDP。**本番と同じ WebGL レンダラ**）:

| 操作 | 結果 |
|---|---|
| `echo S96LIVE` の出力行をドラッグ選択 -> `Cmd+F` | 検索欄に **`S96LIVE`** が入る |
| 手で `ZZZ-MANUAL` を入れて閉じ、別の場所をクリックして選択を解く -> `Cmd+F` | **`ZZZ-MANUAL` のまま**（空にならない・上書きされない） |

⚠ **WebGL レンダラでは `.xterm-rows` が無い**ので、E2E と同じやり方（行の矩形を掴む）は使えない。
座標でドラッグしたが、**`mouse down` -> 1回 `move` -> `up` では選択されなかった**。
**間に複数回 `move` を挟む**と選択できる（xterm は mousemove を見ている）。

### 次に再開するとき最初に読むべきこと

- **#179 の対象7件はこれで全部 close。#179 を閉じてよい**
- 続きは **周5-b**（メニュー項目のホバー / フォーカスが 1.08。`:focus-visible` がマウスで発火しない）と
  **周7**（アクティブタブの `scrollIntoView`。約110手/日）。どちらも `known-issues.md` 11番に材料がある
- そのあとは **#180（P3・15件）** へ
