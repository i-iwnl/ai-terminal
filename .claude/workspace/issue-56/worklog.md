# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-07-29 - ワークスペース作成と計画

### 実施内容

- Issue #56 を起票し、ワークスペースを作成した
- 現状のタブモデル（`useTabs.ts` の `TabState[]` フラット配列）と、
  `App.tsx` が `.terminal-stack` に全ペインを `position: absolute` で重ね置きしている構造を確認した

### 設計判断

- `architecture.md` の「設計判断履歴」を参照（二分木モデル / #55 を先行させる順序）

### 教訓（該当する場合）

- まだ実装していないので無し

### 次に再開するとき最初に読むべきこと

- **この Issue はまだ着手していない。** 先に `.claude/workspace/issue-55/` を完了させる
- 着手時は `overview.md` の「直近の次アクション」の P1（`/design-review`）から。
  **`styles.css` を触る周なので、計画を確定させる前にペルソナレビューを通すのが loop の規約**

---

## 2026-07-29 - design-review（5ペルソナ）と案の改訂

### 実施内容

- 初版の案を書き（`split-view-proposal-v1.md`。スクラッチパッド、リポジトリには入れていない）、
  5ペルソナのサブエージェントを並列で起動した
- 5体すべてが返答。指摘を人数で並べ替え、改訂版を `design-review.md` として作成した
- `architecture.md` の「技術的制約」「Contract 変更」「設計判断履歴」を改訂結果に合わせた
- `known-issues.md` に、分割とは独立の負債10件を記録した

### 5人全員が独立に指摘した4件（= 案の前提が壊れていた）

| # | 内容 |
|---|---|
| U1 | **`Cmd+Option+矢印` は現状のコードでは絶対に発火しない。** `shortcuts.ts:15` が altKey を無条件に弾き、`test/unit/renderer-lib.test.ts:36-39` がそれを不変条件として固定している |
| U2 | 「S40 の期待値更新」は誤り。S40 の測定対象14件に `.terminal-pane` 系は1つも無く、**既存の期待値は1行も動かない**。必要なのは行の追加 |
| U3 | `ratio` の 0.1〜0.9 クランプは幅の下限にならない。既定 1200px で 0.1 = **5桁**。しかも fontSize を追跡しないので**文字を大きくした人だけが壊れる** |
| U4 | 疑問6は「タブを前に出す」では解けない。対象ペインが既に見えている場合 `setActiveTabId` は no-op で、**通知をクリックしても画面が1px も動かず壊れて見える** |

### 案の前提そのものを壊した指摘（a11y、単独だが致命的）

**`visibility: hidden` はアクセシビリティツリーからも部分木を除去している。**
初版はこれを「`ResizeObserver` が壊れるから維持する」とだけ書いたが、副作用の半分しか見ていなかった。
xterm は `screenReaderMode` 有効時に `aria-live="assertive"` の live region を作るので、
今は「露出している live region は常に1個」が**偶然**成立している。分割はこの遮断を無効化し、
assertive が N 個同時に露出する。**2ペインで claude と gemini を並走させると VoiceOver は文を最後まで喋れなくなり、
ユーザー自身のタイピングのエコーまで潰れる。**

### 矛盾した指摘と、どちらを採ったか

`design-review.md` の「1. 矛盾した指摘と、どちらを採ったか」が唯一の正。要点だけ:

- **非アクティブペインの減光**: 4対1で「減光しない」。ただし「現行最大のアクセントの10.7倍の面積の青を視野中心に8時間点灯させる」という指摘は数値として正しいので、**線を 2px -> 1px に落として吸収**した
- **`Cmd+Shift+W`**: macOS ペルソナは賛成、ヘビーユーザーは反対。**反対を採った**（macOS 全域で「ウィンドウを閉じる」。衝突しなくても、そう学習している指が N 本のエージェントを殺す。被害が非対称）
- **細すぎるペインの拒否**: 3対1で拒否。ただし両者の計算は矛盾しておらず、**閾値を「実セル幅 x 20桁」で持てば両方成立する**（13px では当たらず 26px では当たる）

### 設計判断

- **全員一致の対案をそのまま実装しなかった。** 5人中4人が「スプリッタを `--border-control` にせよ」と書いたが、
  a11y だけが「その結果 `--accent` との比が 1.56 になる」を計算していた。しかも
  **`#1e1e1e` と `#5b9cff` の両方に対して 3:1 を満たすグレーは 256段に1つも存在しない**。
  多数決の結論は採り、`prefers-contrast: more` でアクティブ線を `--focus-ring` に切り替えることで補った
- **周の切り方を 4周 -> 10 PR に組み替えた。** うち PR 0-a / 0-b / 0-c / 1 は
  「案の前の別作業」で、分割が1行も入らなくても単独で意味がある（3人以上が指摘した項目は
  案の中ではなく案の前に切り出す、という規約に従った）
- **PR 3（木を導入するが常に leaf 1枚。分割はできない）を新設した。**
  E2E の diff が0行であることがレビューの根拠になる。CSS の「置換と値の変更を混ぜない」と同じ理屈で、
  分割が壊れたとき「モデル移行のバグか、分割の実装のバグか」を分離できる

### 教訓

- **自分が直前に書いたドキュメントと、自分が書いた案が矛盾していた。** `architecture.md` に
  「fit は ResizeObserver が見ているので個別の呼び出しは要らない」と書いた3時間後に、
  案には「ドラッグ中は fit を呼ばない」と書いていた。**実装者は architecture.md を読み、ゲートを入れないほうを採る。**
  レビューが指摘するまで気づかなかった
- **「呼ばない」と「発火しない」は違う。** 提案 D の「ドラッグ中は `fit()` を呼ばない」は、
  自分の手で呼ばないという意味しか持たない。`ResizeObserver` は Grid の比率が変われば勝手に発火する
- **`design-rules.md` に書いてある規約を引用しても、適用を誤ることがある。**
  「装飾の線は 1.4.11 の対象外」は規約どおりだが、**スプリッタは装飾ではなくコントロール**だった。
  規約の引用は、その規約が当てはまるかの判断を代替しない
- **議題の引き継ぎが1周で落ちた。** Issue #55 の known-issues 3 と issue-56 の overview P1 の
  両方に「ドロップハイライトを design-review に載せる」と書いていたのに、案に1行も書かなかった。
  **書いた本人が忘れる**ので、案を書くときに前周の known-issues を開く手順が要る

### 次に再開するとき最初に読むべきこと

1. **改訂版の案は `.claude/workspace/issue-56/design-review.md`。** 初版はスクラッチパッドにしかなく、
   セッションが切れると消える（意図的。何が覆ったかは本 worklog の上の表に残してある）
2. **実装はまだ1行も始めていない。** 次は PR 0-a（`screenReaderMode` をアクティブ限定に）から。
   **これは分割が1行も入らなくても単独で意味があり、今日は実質 no-op だが不変条件がテストで固定される**
3. **`known-issues.md` の 1（独立の負債10件）の起票可否を、ユーザーに確認する必要がある。**
   とくに 1-2（tmux の `-A` が効かず、`Cmd+W` した claude にアプリから二度と戻れない）は
   分割で `Cmd+W` の頻度が上がるので、分割より先に手当てすべきかの判断が要る
4. **10 PR は多いので、ユーザーがスコープを縮める判断をする可能性がある。** その場合も
   PR 0-a / 0-b / 1 は落とさないこと（落とすと分割の実装中に必ず踏む）

---

## 2026-07-29 - PR 0-a: screenReaderMode をアクティブなペインだけに

導入計画の1本目。**分割のコードは1行も書いていない。**

### 実施内容

- `App.tsx` の `screenReaderMode` を、アクティブなタブのときだけ true にした
- S37 に「**タブが2枚あっても `.xterm-accessibility` はちょうど1個**」「タブを切り替えても1個のまま」の検査を足した
- `scenarios.yml` の S37 の note に、この不変条件を足した理由を追記した
- `useTerminal.ts` は無変更。`options.screenReaderMode` の変化を `term.options` へ反映する仕組みが既にあり、タブ切り替えで正しく付け替わることを新しい検査で確認した

### 設計判断

- **ゲートを `.map()` の中の prop 式に置いた**（`tab.id === tabsApi.activeTabId && (...)`）。同じ JSX の `active={tab.id === tabsApi.activeTabId}` と並ぶ形になり、近傍の書き方に一致する
- **S37 を拡張し、新しいシナリオは作らなかった。** `make e2e-lint` の check7 が「1 spec に `test()` は1つ」を検査しているため。既に起動しているアプリを使い回す形になった

### 教訓

- **「visibility: hidden だから DOM 上も1個になる」は誤り。** 計画時（design-review.md の PR 0-a の行）は「今日は実質 no-op」と書いており、私自身も**この検査は関門にならないかもしれない**と予想していた。実際に `App.tsx` を元に戻して測ったら、`.xterm-accessibility` は**2個**になり検査は赤くなった。
  理由は、`visibility: hidden` が消すのは**露出と描画**であって、**DOM ノードの生成ではない**から。live region は `TerminalPane` ごとの xterm インスタンスが自分の `screenReaderMode` オプションを見て作るので、CSS とは独立に増える。
  **つまりこの変更は no-op ではなく、既に実害の一歩手前まで来ていた**（VoiceOver から見て隠れていただけで、DOM には assertive な live region が タブ枚数ぶん生えていた）
- **予想をコメントに書いてから測ると、間違いがコードに残る。** ワーカーは最初「この検査では捕まえられないので visibility ベースで書き直す必要がある」という趣旨のコメントを spec に書いていた。実測後にその記述を訂正している。**測る前に書いた説明は、測ったあとに読み直す**

### 次に再開するとき最初に読むべきこと

1. **次は PR 0-b**（`useTerminal.ts` の `doFit` と `handle.fit` の1本化 + 同値の `pty.resize` を送らない）。関門は「挙動不変・E2E 全 green」
2. 進捗の表は `overview.md` の「導入計画（PR 0-a 〜 9）の進捗」に置いた。**内容の正は `design-review.md` の表**なので、そちらに状態を書き込まないこと（二重管理になる）
3. `known-issues.md` の 1 のうち **#60（tmux の `-A`）は解決済み**（PR #73）。`Cmd+W` の頻度が上がる前提の懸念が1つ減った
4. **PR 0-a で分かった事実を、分割の実装時に使うこと**: 非アクティブなペインを `visibility: hidden` 以外の方法で隠すと、この不変条件は別の壊れ方をする

---

## 2026-07-29 - 独立の負債10件のうち5件を返済した（分割の実装ではない）

`known-issues.md` の 1（design-review が分割とは無関係に見つけた負債10件）のうち、
**5件を分割の前に片付けた**。分割のコードは1行も書いていない。

### 実施内容

| # | Issue | PR | 内容 |
|---|---|---|---|
| 1-2 | #60 | #73 | resume でも tmux セッション名が安定。`Cmd+W` した claude に戻れる |
| 1-3 / 1-4 | #62 | #77 | `Cmd+Shift+G` を検索に返し、`Cmd+G`（次を検索）を足す |
| 1-1 | #61 | #78 | `TERM_PROGRAM` の素通しをやめ、嘘の `Restored session:` 行を止める |
| 1-10 | #63 | #79 | 入力欄の編集中はグローバルショートカットを先取りしない |
| 1-6 | #64 | #80 | タスク一覧の行をキーボードで押せるようにする |

### 設計判断

- **「分割後に顕在化」と評価していた 1-10（#63）を、分割の前に片付けた。** `Cmd+D` が
  分割に割り当たるのを待つまでもなく、**編集中の `Cmd+W` で編集対象のタブごと閉じる**
  という実害が既にあった。**「分割後に顕在化」は「今は無害」を意味しない**
- **1-8（#67 検索バーのはみ出し）は手を付けていない。** これは本当に分割が入って
  初めて幅が細くなるもので、いま直しても検証手段が無い

### 教訓

- **依存は逆向きにも効く。** #64 の E2E で「このアプリが起動したタスク行」を作るには
  `ownedByApp` が要るが、偽 CLI の固定 `agents.json` に対してそれを作る経路は
  通常存在しない。**唯一の抜け道が resume で、それは #60 の修正（PR #73）で
  `agentSessionId` が resume でも埋まるようになって初めて成立した**。
  分割の前段として片付けた負債が、別の負債のテストを可能にしていた
- **1つの周で5件を続けて直すと、後の周が前の周のテストを壊す。** #61 が #62 の
  S45 を落とした（表示が1行ずれた）。**壊れたのはテストのほうで、実装は正しかった**。
  実測してから直したので、実装を「修正」してしまう事故は避けられた

### 次に再開するとき最初に読むべきこと

1. **分割の実装は依然 PR 0-a しか終わっていない。** 次は PR 0-b（`doFit` と
   `handle.fit` の1本化）。`overview.md` の導入計画の表を見ること
2. `known-issues.md` の 1 の表に**状態列を足した**。残り5件（#65 / #66 / #67 / #68 と、
   1-8 は分割の周で踏む）
3. **スタックした PR 9本が未マージ**（#72 -> #73 -> #74 -> #75 -> #77 -> #78 -> #79 -> #80、
   および独立の #76）。分割の実装に入る前にマージ順を確認すること

---

<!-- 以降、作業のたびにセクションを追記 -->

## 2026-07-30 - PR 0-b: `doFit` と `handle.fit` の1本化 + 同値の `pty.resize` を送らない

導入計画の2本目（10本中2本目）。PR #81。分割表示のコードは1行も入っていない。

### 実施内容

- `useTerminal.ts` の `doFit`（マウント effect 内のローカル関数）と `handle.fit`（`handleRef` 内）を、
  フック本体スコープの単一の `fit` に統合。ResizeObserver・初回 `requestAnimationFrame`・
  `TerminalHandle.fit` の3経路がすべてこの1関数を指す
- 直前に送った cols/rows を `lastResizeRef`（`useRef`）に持ち、同値なら `pty.resize` を送らない
- 判定を `terminal/resizeGate.ts` の純粋関数 `shouldSendResize` に切り出し、
  `test/unit/resize-gate.test.ts` で5件固定

### 設計判断

- **直前値はインスタンススコープの `useRef` に置いた。** モジュールスコープの変数に置くと、
  ペインが複数になった瞬間に全ペインで1つの直前値を奪い合って壊れる。
  PR 7（スプリッタ）の関門「ドラッグ中は `pty:resize` が飛ばず `mouseup` 後に1回だけ」は
  この置き場所に乗っている
- 統合後の `fit` は refs しか読まないので、`handleRef` が初回レンダーの closure を
  保持し続けても stale にならない（`useImperativeHandle` の `handle` の同一性も安定）
- 2経路の差分は2つだけで、どちらも観測可能な挙動を変えずに寄せられた:
  null ガード（`handle.fit` 側に統一）と `ptyId` の参照タイミング（毎回 `optionsRef.current` を読む側に統一）

### 教訓

- **「挙動不変」は「送るシグナルが不変」を意味しない。** `TerminalPane.tsx:62` は
  タブがアクティブになるたび `handle.fit()` を呼び、これまでは**寸法が同じでも毎回
  `pty.resize` が飛んで子プロセスに SIGWINCH が届いていた**。vim / htop / tmux は
  SIGWINCH で再描画するので、タブ切り替え時の再描画がこれに暗黙に依存していた可能性がある。
  今回それが止まる。**寸法は不変・シグナルは減る**という差なので E2E では捕まらない
- design-review.md 記載の行番号（`doFit`(164-172) / `handle.fit`(205-217)）は実コードと
  ずれていた（着手時点で 197-206 / 242-254）。**行番号ではなく関数名で特定すること**

### 次に再開するとき最初に読むべきこと

1. **GUI 手動検証が未了。** vim / htop / tmux を開いてタブを往復し、
   同値 `pty.resize` の停止でタブ切り替え時の再描画が壊れていないことを目で確認する。
   E2E では絶対に捕まらない（`/terminal` skill の GUI 手動検証の手順に従う）
2. **次は PR 0-c**（`.notice-banner` に `role="alert"` + `.app` 直下に `role="status"` を1つ +
   ランドマーク `<main>` / `<nav>`。現状ランドマークは0件）
3. 進捗の表は `overview.md`。**内容の正は `design-review.md` の表**
4. `known-issues.md` の 1-8（#67 検索バーのはみ出し）は依然未着手。分割の周で踏んだ時点で該当 PR に含める

## 2026-07-30 - PR 0-c: ランドマークと live region

導入計画の3本目（10本中3本目）。PR #84。分割表示のコードは1行も入っていない。

### 実施内容

- `App.tsx` の `<div className="main">` を `<main className="main">` に
- `Sidebar.tsx` の `<aside className="sidebar">` を `<nav className="sidebar" aria-label="サイドバー">` に
- `.notice-banner` に `role="alert"`（`SettingsWindow.tsx` の前例に合わせた）
- `.app` 直下に `role="status"` の live region を1個。**PTY 終了の告知に紐づけた**
- 新規 E2E S48

### 設計判断

- **`role="status"` を空のプレースホルダにしなかった。** 「実際には更新されない live region」は
  ARIA の嘘そのもの。PTY の終了は現状 `TabBar` の終了バッジ（視覚のみ）でしか分からず、
  非視覚的な手がかりがゼロだったので、そこに紐づけた
- **S37 の不変条件とは競合しない。** S37 が測るのは xterm 内部の `.xterm-accessibility`
  （`aria-live="assertive"`、アクティブなタブ1個分）。新設の `role="status"` は暗黙 `polite` で、
  assertive の読み上げを割り込んで中断しない。design-review 0-4 が問題視した
  「assertive 同士が発話を潰し合う」事象とは別種
- サイドバーは `<nav>` にした。タスク / 履歴 / メモ はアプリの3つの主要な行き先で、
  補助的内容を示す `<aside>`（`complementary`）より実態に合う
- `.app-status` は `position: absolute` なので `.app` の flex レイアウトに影響しない。
  `styles.css` を触らずインライン style に留めた（この周の対象外なので）

### 教訓

- **計画書の「現状ランドマークは0件」は事実誤認だった。** `Sidebar.tsx` には初期コミットから
  `<aside>`（暗黙の `role="complementary"`）が存在していた。0-c の対応自体は変わらないが、
  **design-review.md の前提が実コードとずれていた2件目**（1件目は 0-b の行番号ずれ）。
  **計画書の「現状はこうなっている」は、着手時に必ず実コードで測り直すこと**

### 次に再開するとき最初に読むべきこと

1. **次は PR 1**（`shortcuts.ts:15` の altKey ガードを**矢印キーに限って**緩める +
   冒頭コメントと `test/unit/renderer-lib.test.ts:36-39` の不変条件を意図的に更新）。
   これで前提工事（0-a / 0-b / 0-c / 1）が終わり、**PR 2 から分割表示の本体**に入る
2. **PR 0-b の GUI 手動検証が依然として未了。** vim / htop / tmux でタブを往復すること
3. `role="alert"` は E2E で検証していない。`notice` を確実に出す手段が無く
   （node-pty が同期的に投げない）、S11 のコメントが同じ理由でタイミング依存だと記録している

## 2026-07-30 - PR 1: altKey ガードを矢印キーに限って緩める（前提工事の完了）

導入計画の4本目（10本中4本目）。PR #87。**これで前提工事（0-a / 0-b / 0-c / 1）が完了し、
次の PR 2 から分割表示の本体に入る。**

### 実施内容

- `shortcuts.ts` の `if (!e.metaKey || e.ctrlKey || e.altKey) return null;` を
  「矢印キーのときだけ altKey を許可する」形に緩めた
- 判定を `passesModifierGate`（エクスポートした純粋関数）に切り出した
- 冒頭コメントを改訂（従来は「altKey が同時に押されている場合は無視する」と言い切っていた）
- 単体テストを更新

### 設計判断

- **`AppAction` は1つも増やしていない。** ペイン間移動の割り当ては PR 4 以降の担当。
  この PR はガードを緩めるだけで、`Cmd+Option+矢印` は今も何も起きない
- 矢印キーだけを例外にする理由は、**macOS で Option+英数字が特殊文字の合成に使われる**
  （Option+e はアクセント記号の dead key）ため。矢印キーは Option と組み合わせても
  文字を生成しない
- `Cmd` 無しの `Option+矢印`（端末の単語単位移動）は横取りしない。
  `passesModifierGate` が `metaKey` を必須にしているため構造的に到達しないが、
  **単体テストでも固定した**

### 教訓

- **「テストがある」と「テストが変更を固定している」は別物。**
  最初の実装は `matchShortcut(Cmd+Option+矢印)` が `null` を返すことを assert していたが、
  **これはガードを緩める前も後も同じ結果**なので、`shortcuts.ts` の変更を丸ごと revert しても
  テストは green のままだった。テスト名は「ガードに弾かれない」と主張しているのに、
  それを1ミリも検証していなかった
- **観測可能な出力が変わらない変更は、判定を純粋関数に切り出して直接固定する。**
  このリポジトリで4例目（`shouldSendResize` / `computeYourTurnSince` / `rovingTabindex` /
  `passesModifierGate`）。**同じ形が繰り返し必要になるということは、これが既定の作法**
- **完了条件に「revert して赤くなることを実測して報告する」を入れると、この見落としは防げる。**
  実際、ワーカーは指示どおり revert して「新規テスト1件だけが赤くなる」ことを実測した
- 計画書の行番号がまた実コードとずれていた（`shortcuts.ts:15` -> 実際は `:72`、
  `renderer-lib.test.ts:36-39` -> 実際は `:106`）。**3件目**

### 次に再開するとき最初に読むべきこと

1. **前提工事は完了。次は PR 2**（`tabs/paneTree.ts` の純粋関数 + 単体テスト。
   **UI を1行も触らない**）。関門は `make check`
2. PR 2 の次は PR 3（**木を導入するが常に leaf 1枚。分割はできない**）。
   `styles.css` の diff 0行、**E2E の期待値 0行変更が関門**
3. **PR 0-b の GUI 手動検証が依然として未了**（vim / htop / tmux でタブを往復する）
4. 進捗の表は `overview.md`。**内容の正は `design-review.md` の表**

## 2026-07-31 - PR 2: paneTree.ts の純粋関数（分割表示の本体の1本目）

導入計画の5本目（12本中5本目）。**前提工事が終わり、ここから分割表示の本体に入る。**

### 実施内容

- `src/renderer/src/tabs/paneTree.ts`（新規）に木の表現と操作を純粋関数として実装
- `test/unit/pane-tree.test.ts`（新規）に43件

**UI は1行も触っていない。** `git status` に新規2ファイル以外が出ないことがこの周の関門で、維持した。

### 設計判断

- **`PanePath`（ルートからの子インデックス列）を追加した。** 計画書の型定義では split ノードが
  ID を持たないので、`ratio` 更新の対象を一意に指すにはこれ以外の手段が無い
- **木を畳む条件を「親を残った兄弟で置き換える」に一般化した。** 「兄弟が親を置き換える」と
  書くと leaf 限定に読めるが、兄弟が部分木でも同じ処理で成り立つ
- 4方向移動は端で留まり、次/前は折り返す。空間的な移動と巡回は別物
- 木がルートの leaf 1枚だけのときは `closePane` が `null` を返す。
  **タブ自体を閉じるかどうかは呼び出し側の判断**（`AppAction` の `close-pane` と
  `close-tab` の切り分けは PR 4 の担当）

### 教訓

- **計画書が明示的に扱った例だけを実装し、同じ理屈が当てはまる対称のケースを
  「対象外」と断定してしまう。** 初版は上下分割の `ratio` クランプを比率のみ（0.1〜0.9）にし、
  コメントに「行数の下限は非目標」と書いていた。**計画書の「## 5. 非目標」には4件しか
  挙がっておらず、行数の下限は含まれていない。**
  実質的にも U3 とまったく同じ壊れ方をする（既定 800px・26px フォントで ratio 0.1 は約2行。
  フォントを大きくしたユーザーだけが壊れる）。**幅と対称に実測ベースへ直した**
- **根拠のある断定かどうかは、原典を開けば機械的に確かめられる。**
  「非目標」と書かれていたら非目標リストを開いて数える

### 次に再開するとき最初に読むべきこと

1. **次は PR 3**（木を導入するが常に leaf 1枚。分割はできない）。
   関門は **`styles.css` の diff 0行、E2E の期待値 0行変更**。
   ここで「モデル移行のバグ」と「分割の実装のバグ」を分離できる状態を作る
2. `paneTree.ts` はまだ**どこからも import されていない**。PR 3 で初めて繋ぐ
3. `MIN_PANE_COLUMNS` / `MIN_PANE_ROWS` はどちらも 20。呼び出し側が
   `PaneCellMetrics`（実ピクセルと実セル寸法）を測って渡す契約

## 2026-07-31 - PR 3: 木を導入するが、常に leaf 1枚

導入計画の6本目（12本中6本目）。PR #97。**振る舞いを1つも変えない周。**

### 実施内容

- `TabState` を `tabs/tabPane.ts` へ移し、`{ id, layout: PaneNode, activePaneId, createdAt }` に変えた
- **PTY のメタ（`ptyId` / `kind` / `title` / `agentSessionId` / `cwd` / `exit`）を leaf へ移した**
- `paneTree.ts` に `getLeaf` / `resolveActiveLeaf` / `updateLeaf` を追加
- `useTabs.ts` / `App.tsx` / `TabBar.tsx` の参照を `tabLeaf(tab)` 経由に置き換えた
- 単体テストを 256 -> 271 件に

### 関門の結果

| 対象 | 結果 |
|---|---|
| `src/renderer/src/styles.css` の diff | **0行** |
| `e2e/` の diff（spec / scenarios.yml とも） | **0行** |
| `docs/images/` の diff | **0行** |
| `make e2e` | **53 passed / flaky 0** |
| `make css-substitution-check` | **PASS**（CSS を触っていないので通るのが正しい） |

**E2E を1行も直さずに全部通った。** モデル移行が振る舞いを変えていないことの証拠になる。

### 設計判断

- **`leaf.paneId` は `tab.id` と同じ文字列（= `ptyId`）を使い回した。** PR 3 は
  「1タブ = 1 leaf」を保証するので別の採番体系を作る必要が無い。純粋に内部の識別子なので
  PR 4 が自由に変えてよい
- **`TabState` を `useTabs.ts` から `tabs/tabPane.ts` へ出した。** `tabTitle.ts` と同じ理由で、
  `useTabs.ts` は `window.api` に依存するため `test/unit/` から import できない。
  window に触れないモジュールへ純粋なヘルパを置く
- `updateLeaf` のパッチ型から `kind` / `paneId` を除外した（`Partial<Omit<...>>`）。
  構造的に不正なパッチを型で弾く

### 教訓

- **「何も変わらないこと」を関門にすると、変わってしまったことが必ず表に出る。**
  この周は E2E の diff 0行が唯一のレビュー根拠で、実際に1行も直さずに 53 spec が通った。
  もし通らなければ「モデル移行が振る舞いを変えた」ことが即座に分かる設計だった
- `splitPane` は**どこからも呼ばれていない**（grep で確認）。分割の経路は PR 4 で初めて生える

### 次に再開するとき最初に読むべきこと

1. **次は PR 4**（分割を有効化）。`Cmd+D` / `Cmd+Shift+D` + **`AppAction` に `close-pane` を新設**
   （`close-tab` の意味は変えない）+ **`menu.ts` を同じ PR で** + `S36:71` 更新 + 新規 E2E。
   関門は「2つの `.xterm-screen` が同時に可視」「`.terminal-pane.is-active` で一意に引ける」
2. **PR 4 から実際に見た目が変わる。** ここまでの3周（PR 2 / 3 と前提工事）は1ピクセルも動いていない
3. `known-issues.md` の 1-8（#67 検索バーのはみ出し）は、**PR 4 で幅が細くなった時点で踏む**。
   踏んだら該当 PR に含めること

## 2026-07-31 - PR 4: 分割を有効化する（画面が初めて変わる周）

導入計画の7本目（12本中7本目）。PR #98。**ここまでの6本は1ピクセルも動いていなかった。**

### 実施内容

- `AppAction` に `split-pane` と `close-pane` を新設（**`close-tab` の意味は変えない**）
- `Cmd+D` / `Cmd+Shift+D` で分割、`Cmd+W` は `close-pane`（1枚ならタブが閉じる）
- `menu.ts` に分割の2項目。`タブを閉じる` はアクセラレータを失い、
  **`（N ペイン）` の動的ラベル**を持つ（`menu:pane-count` の IPC を新設）
- `PaneTreeView.tsx`（新規）が木を再帰的に DOM へ描く
- `TerminalPane` の `active` を `visible`（タブの可視性）と `active`（ペインのフォーカス）に分離
- `useTerminal` に `getCellMetrics()` を追加（実セル寸法を `clientWidth / cols` で逆算）
- **#67（検索バーのはみ出し）をこの周で踏んで直した**

### 関門の結果

| 関門 | 結果 |
|---|---|
| 2つの `.xterm-screen` が同時に可視 | **満たす**（S56） |
| `.terminal-pane.is-active` で一意に引ける | **満たす**（S56） |
| `.xterm-accessibility` は分割後も1個 | **満たす**（S37 拡張。**revert で赤くなることを実測**） |
| ペイン境界に `border` を使わない | **満たす**（`.pane-split` は flex のみ。装飾は PR 5 / 7） |

### 設計判断

- **`close-tab` の意味を変えず `close-pane` を新設した。** 型が変わらない意味変更は機械が守れない
- **分割の拒否は実セル寸法で判定する。** E2E も固定ピクセルを仮定せず、
  `Cmd+D` を繰り返して「ペインが増える」か「拒否の通知が出る」かをポーリングし、
  **両方が実際に起きたこと**を確認する形にした
- **ペイン間に装飾を1つも入れなかった。** アクティブ表現は PR 5、スプリッタは PR 7
- `role="tabpanel"` は**木のルートだけ**に置いた。leaf ごとに置くと
  1つのタブに複数の tabpanel ができて id が重複する

### この周で直した潜在バグ（木が2枚以上になって初めて壊れるもの）

- `closeTab` が**アクティブな leaf の PTY しか kill していなかった**（他のペインの PTY が漏れる）
- `findTabByPtyId` / `findTabByAgentSessionId` が**アクティブな leaf しか見ていなかった**
- `markExited` / `refreshTabCwd` / `handleExit` のタイトル引きが
  「終了した leaf は常にアクティブ」と仮定していた
- cwd ポーリングの「このタブはアクティブか」判定が**タブ ID と PTY ID を比較していた**

**いずれも PR 3 までは1タブ1 leaf なので絶対に露見しない。** 分割を有効にした瞬間に
終了バッジ・通知のタイトル・サイドバーの cwd が静かに間違う類のもの。

### 教訓

- **「1つしか無い」前提のコードは、2つ目ができた瞬間に静かに壊れる。**
  型は通り、テストも通り、1枚のときは正しく動く。**分割を有効にする周に、
  この種の潜在バグがまとめて表面化する**ことを見込んでおくべきだった
- 計画書の `S36:71` は実際には 73 行目にずれていた（**通算4件目**）

### 次に再開するとき最初に読むべきこと

1. **次は PR 5**（アクティブ表現 + ペインヘッダ + aria 名）。
   `.terminal-pane.is-active` の hook は既にあるので、**見た目を付けるだけ**。
   design-review の指定は「xterm の非フォーカス時カーソル」「アクセント線 **1px**
   （`box-shadow: inset`。**`border` は禁止**）」「ペインヘッダ 18px（分割中のみ）」の3層。
   S40 に**行を追加**する（既存の期待値は動かない）
2. **U4（タスク一覧 / 通知の2経路をペイン粒度へ）は PR 8 の担当**。この周では
   タブ粒度の引き直ししかしていない
3. `known-issues.md` の 1-8（#67）は**この周で解決した**

## 2026-07-31 - PR 5: アクティブ表現とペインヘッダ

導入計画の8本目（12本中8本目）。PR #99。

### 実施内容

- アクティブ表現の3層（提案 C'）: xterm の非フォーカス時カーソル / アクセント線 1px
  （`box-shadow: inset`。**`border` は使わない**）/ ペインヘッダの語
- `--pane-active-accent` を新設し、`prefers-contrast: more` で `--focus-ring` へ切り替え
- ペインヘッダ 18px（分割中のみ）。`zsh` / `claude (再開)` + cwd の basename
- `paneHeader.ts` の純粋関数 + 単体テスト、新規 E2E S57
- S40 / S41 に**行を追加**（既存の期待値は1行も動かしていない）

### 計画書の判断を実測で覆した

**design-review は「ヘッダを通常フローに入れてはいけない」と定めていた。**

> **幾何を分岐させない実装にすること**（1ペイン時と N ペイン時で `.terminal-pane__container` の
> 内寸が変わると、その差分が SIGWINCH として実行中の子プロセスに届く）

初版はこれに従って `position: absolute` の重ね置きにした。**その結果、不透明な背景が
ターミナルの内容に乗り、15px の行のうち14px が隠れた**（レビューで実測）。
分割中は各ペインの最初の行が読めない状態だった。

そこで `ipcMain.on('pty:resize', ...)` で発火回数を実測した。

| 実装 | `pty:resize` の回数（左右分割1回） |
|---|---|
| 重ね置き | **2** |
| 通常フロー | **2** |

**同じだった。** ヘッダが出入りするのは1ペイン <-> 2ペインの遷移のときだけで、
そのとき当該ペインは**どのみちリサイズされている**（幅が半分になる）。
ヘッダの高さぶんは、**既に飛んでいる resize の `rows` の値を変えるだけ**で、
イベントを1つも増やさない。

**計画書の懸念は当たらなかった。** 通常フローを採用し、隠れる内容をゼロにした。

### 教訓

- **「行数が変わらない」検査は「内容が隠れていない」ことを保証しない。**
  初版の S57 は行数の不変を確認して green だった。行数は変わらないまま、
  ヘッダが最初の行の上に乗っていた。**測る対象を間違えると、通っているテストが
  何も守らない**
- **計画書の禁止事項も、根拠が実測できるなら実測してから従う。**
  今回は禁止の理由（SIGWINCH が増える）が実測で否定された。
  **理由が成り立たない禁止は、守ると別の害を生む**
- **仕様の穴はワーカーの落ち度ではない。** design-review は「幾何を分岐させない」としか
  書いておらず「隠すな」とは書いていない。ワーカーは指示どおりに実装していた

### 次に再開するとき最初に読むべきこと

1. **次は PR 6**（ドロップ可能領域のハイライト。提案 H）。
   `box-shadow: inset 0 0 0 2px var(--accent)` の**4辺の枠**で、
   アクティブ線（上辺1本）と**形で分ける**。`dragleave` は子要素に入るたびに
   発火するのでカウンタで管理し、`drop` でも必ず消す
2. **`pty:resize` の回数を数える仕掛けは、この周で実際に動かした。**
   PR 7（スプリッタ）の関門がまさにこれなので、**手法は実証済み**
3. S40 の既存21件は引き続き1行も動いていない

## 2026-07-31 - PR 6: ドロップ可能領域のハイライト

導入計画の9本目（12本中9本目）。PR #100。

### 実施内容

- ドラッグ中、カーソル下のペインに `box-shadow: inset 0 0 0 2px var(--accent)` の**4辺の枠**
- `dragleave` を `dragCounterRef` のカウンタで管理。`drop` では無条件にゼロ化
- 新規 E2E S58

### 設計判断

- **アクティブ線（上辺1本）とドロップ枠（4辺）を形で分けた。** 同じアクセント色で
  辺の本数だけ違うのは `design-rules.md` で却下済みの「位置が同じで色だけ違う」と同型
- **結合状態（アクティブ かつ ドロップ先）の規則を別に用意した。**
  `box-shadow` は単一プロパティの上書きなので、これが無いと**ドロップ枠がアクティブ線を
  黙って消し、`prefers-contrast: more` での高コントラスト表示が失われる**

### 教訓

- **`dragleave` は子要素に入るたびに発火する。** 素朴に「`dragleave` で消す」と書くと、
  ペイン内でカーソルを動かしただけで枠が消える。素朴な実装に戻すと S58 が実際に赤くなることを実測した
- **単一プロパティで複数の状態を表すと、後から足した状態が前の状態を黙って消す。**
  `box-shadow` は上書きなので、結合状態を明示的に書かないと片方が消える。
  **CSS の詳細度で勝ったほうだけが残る**ことに気づかないと、高コントラストモードだけが壊れる

### 次に再開するとき最初に読むべきこと

1. **次は PR 7**（スプリッタ。ゴースト方式）。関門は「**ドラッグ中は `pty:resize` が飛ばず
   `mouseup` 後に1回だけ**」で、**計測手法は PR 5 の周で既に実証済み**
   （`electronApp.evaluate()` で `ipcMain.on` を足して数える）
2. 残りは PR 7 / 8 / 9 の3本
