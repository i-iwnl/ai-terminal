# Worklog

時系列の作業ログ。**設計判断の根拠**と**学んだ教訓**を中心に記録する。
コード差分は git log を参照、現状の構造は `architecture.md` を参照。

---

## 2026-07-29 - ワークスペース作成と計画

### 実施内容

- Issue #56 を起票し、ワークスペースを作成した
- 現状のタブモデル（`useTabs.ts` の `TabState[]` フラット配列）と、
  `App.tsx` が `.terminal-stack` に全ペインを `position: absolute` で重ね置きしている構造を確認した

### 設計判断

- `architecture.md` の「設計判断履歴」を参照（二分木モデル / #55 を先行させる順序）

### 教訓（該当する場合）

- まだ実装していないので無し

### 次に再開するとき最初に読むべきこと

- **この Issue はまだ着手していない。** 先に `.claude/workspace/issue-55/` を完了させる
- 着手時は `overview.md` の「直近の次アクション」の P1（`/design-review`）から。
  **`styles.css` を触る周なので、計画を確定させる前にペルソナレビューを通すのが loop の規約**

---

## 2026-07-29 - design-review（5ペルソナ）と案の改訂

### 実施内容

- 初版の案を書き（`split-view-proposal-v1.md`。スクラッチパッド、リポジトリには入れていない）、
  5ペルソナのサブエージェントを並列で起動した
- 5体すべてが返答。指摘を人数で並べ替え、改訂版を `design-review.md` として作成した
- `architecture.md` の「技術的制約」「Contract 変更」「設計判断履歴」を改訂結果に合わせた
- `known-issues.md` に、分割とは独立の負債10件を記録した

### 5人全員が独立に指摘した4件（= 案の前提が壊れていた）

| # | 内容 |
|---|---|
| U1 | **`Cmd+Option+矢印` は現状のコードでは絶対に発火しない。** `shortcuts.ts:15` が altKey を無条件に弾き、`test/unit/renderer-lib.test.ts:36-39` がそれを不変条件として固定している |
| U2 | 「S40 の期待値更新」は誤り。S40 の測定対象14件に `.terminal-pane` 系は1つも無く、**既存の期待値は1行も動かない**。必要なのは行の追加 |
| U3 | `ratio` の 0.1〜0.9 クランプは幅の下限にならない。既定 1200px で 0.1 = **5桁**。しかも fontSize を追跡しないので**文字を大きくした人だけが壊れる** |
| U4 | 疑問6は「タブを前に出す」では解けない。対象ペインが既に見えている場合 `setActiveTabId` は no-op で、**通知をクリックしても画面が1px も動かず壊れて見える** |

### 案の前提そのものを壊した指摘（a11y、単独だが致命的）

**`visibility: hidden` はアクセシビリティツリーからも部分木を除去している。**
初版はこれを「`ResizeObserver` が壊れるから維持する」とだけ書いたが、副作用の半分しか見ていなかった。
xterm は `screenReaderMode` 有効時に `aria-live="assertive"` の live region を作るので、
今は「露出している live region は常に1個」が**偶然**成立している。分割はこの遮断を無効化し、
assertive が N 個同時に露出する。**2ペインで claude と gemini を並走させると VoiceOver は文を最後まで喋れなくなり、
ユーザー自身のタイピングのエコーまで潰れる。**

### 矛盾した指摘と、どちらを採ったか

`design-review.md` の「1. 矛盾した指摘と、どちらを採ったか」が唯一の正。要点だけ:

- **非アクティブペインの減光**: 4対1で「減光しない」。ただし「現行最大のアクセントの10.7倍の面積の青を視野中心に8時間点灯させる」という指摘は数値として正しいので、**線を 2px -> 1px に落として吸収**した
- **`Cmd+Shift+W`**: macOS ペルソナは賛成、ヘビーユーザーは反対。**反対を採った**（macOS 全域で「ウィンドウを閉じる」。衝突しなくても、そう学習している指が N 本のエージェントを殺す。被害が非対称）
- **細すぎるペインの拒否**: 3対1で拒否。ただし両者の計算は矛盾しておらず、**閾値を「実セル幅 x 20桁」で持てば両方成立する**（13px では当たらず 26px では当たる）

### 設計判断

- **全員一致の対案をそのまま実装しなかった。** 5人中4人が「スプリッタを `--border-control` にせよ」と書いたが、
  a11y だけが「その結果 `--accent` との比が 1.56 になる」を計算していた。しかも
  **`#1e1e1e` と `#5b9cff` の両方に対して 3:1 を満たすグレーは 256段に1つも存在しない**。
  多数決の結論は採り、`prefers-contrast: more` でアクティブ線を `--focus-ring` に切り替えることで補った
- **周の切り方を 4周 -> 10 PR に組み替えた。** うち PR 0-a / 0-b / 0-c / 1 は
  「案の前の別作業」で、分割が1行も入らなくても単独で意味がある（3人以上が指摘した項目は
  案の中ではなく案の前に切り出す、という規約に従った）
- **PR 3（木を導入するが常に leaf 1枚。分割はできない）を新設した。**
  E2E の diff が0行であることがレビューの根拠になる。CSS の「置換と値の変更を混ぜない」と同じ理屈で、
  分割が壊れたとき「モデル移行のバグか、分割の実装のバグか」を分離できる

### 教訓

- **自分が直前に書いたドキュメントと、自分が書いた案が矛盾していた。** `architecture.md` に
  「fit は ResizeObserver が見ているので個別の呼び出しは要らない」と書いた3時間後に、
  案には「ドラッグ中は fit を呼ばない」と書いていた。**実装者は architecture.md を読み、ゲートを入れないほうを採る。**
  レビューが指摘するまで気づかなかった
- **「呼ばない」と「発火しない」は違う。** 提案 D の「ドラッグ中は `fit()` を呼ばない」は、
  自分の手で呼ばないという意味しか持たない。`ResizeObserver` は Grid の比率が変われば勝手に発火する
- **`design-rules.md` に書いてある規約を引用しても、適用を誤ることがある。**
  「装飾の線は 1.4.11 の対象外」は規約どおりだが、**スプリッタは装飾ではなくコントロール**だった。
  規約の引用は、その規約が当てはまるかの判断を代替しない
- **議題の引き継ぎが1周で落ちた。** Issue #55 の known-issues 3 と issue-56 の overview P1 の
  両方に「ドロップハイライトを design-review に載せる」と書いていたのに、案に1行も書かなかった。
  **書いた本人が忘れる**ので、案を書くときに前周の known-issues を開く手順が要る

### 次に再開するとき最初に読むべきこと

1. **改訂版の案は `.claude/workspace/issue-56/design-review.md`。** 初版はスクラッチパッドにしかなく、
   セッションが切れると消える（意図的。何が覆ったかは本 worklog の上の表に残してある）
2. **実装はまだ1行も始めていない。** 次は PR 0-a（`screenReaderMode` をアクティブ限定に）から。
   **これは分割が1行も入らなくても単独で意味があり、今日は実質 no-op だが不変条件がテストで固定される**
3. **`known-issues.md` の 1（独立の負債10件）の起票可否を、ユーザーに確認する必要がある。**
   とくに 1-2（tmux の `-A` が効かず、`Cmd+W` した claude にアプリから二度と戻れない）は
   分割で `Cmd+W` の頻度が上がるので、分割より先に手当てすべきかの判断が要る
4. **10 PR は多いので、ユーザーがスコープを縮める判断をする可能性がある。** その場合も
   PR 0-a / 0-b / 1 は落とさないこと（落とすと分割の実装中に必ず踏む）

---

## 2026-07-29 - PR 0-a: screenReaderMode をアクティブなペインだけに

導入計画の1本目。**分割のコードは1行も書いていない。**

### 実施内容

- `App.tsx` の `screenReaderMode` を、アクティブなタブのときだけ true にした
- S37 に「**タブが2枚あっても `.xterm-accessibility` はちょうど1個**」「タブを切り替えても1個のまま」の検査を足した
- `scenarios.yml` の S37 の note に、この不変条件を足した理由を追記した
- `useTerminal.ts` は無変更。`options.screenReaderMode` の変化を `term.options` へ反映する仕組みが既にあり、タブ切り替えで正しく付け替わることを新しい検査で確認した

### 設計判断

- **ゲートを `.map()` の中の prop 式に置いた**（`tab.id === tabsApi.activeTabId && (...)`）。同じ JSX の `active={tab.id === tabsApi.activeTabId}` と並ぶ形になり、近傍の書き方に一致する
- **S37 を拡張し、新しいシナリオは作らなかった。** `make e2e-lint` の check7 が「1 spec に `test()` は1つ」を検査しているため。既に起動しているアプリを使い回す形になった

### 教訓

- **「visibility: hidden だから DOM 上も1個になる」は誤り。** 計画時（design-review.md の PR 0-a の行）は「今日は実質 no-op」と書いており、私自身も**この検査は関門にならないかもしれない**と予想していた。実際に `App.tsx` を元に戻して測ったら、`.xterm-accessibility` は**2個**になり検査は赤くなった。
  理由は、`visibility: hidden` が消すのは**露出と描画**であって、**DOM ノードの生成ではない**から。live region は `TerminalPane` ごとの xterm インスタンスが自分の `screenReaderMode` オプションを見て作るので、CSS とは独立に増える。
  **つまりこの変更は no-op ではなく、既に実害の一歩手前まで来ていた**（VoiceOver から見て隠れていただけで、DOM には assertive な live region が タブ枚数ぶん生えていた）
- **予想をコメントに書いてから測ると、間違いがコードに残る。** ワーカーは最初「この検査では捕まえられないので visibility ベースで書き直す必要がある」という趣旨のコメントを spec に書いていた。実測後にその記述を訂正している。**測る前に書いた説明は、測ったあとに読み直す**

### 次に再開するとき最初に読むべきこと

1. **次は PR 0-b**（`useTerminal.ts` の `doFit` と `handle.fit` の1本化 + 同値の `pty.resize` を送らない）。関門は「挙動不変・E2E 全 green」
2. 進捗の表は `overview.md` の「導入計画（PR 0-a 〜 9）の進捗」に置いた。**内容の正は `design-review.md` の表**なので、そちらに状態を書き込まないこと（二重管理になる）
3. `known-issues.md` の 1 のうち **#60（tmux の `-A`）は解決済み**（PR #73）。`Cmd+W` の頻度が上がる前提の懸念が1つ減った
4. **PR 0-a で分かった事実を、分割の実装時に使うこと**: 非アクティブなペインを `visibility: hidden` 以外の方法で隠すと、この不変条件は別の壊れ方をする

---

## 2026-07-29 - 独立の負債10件のうち5件を返済した（分割の実装ではない）

`known-issues.md` の 1（design-review が分割とは無関係に見つけた負債10件）のうち、
**5件を分割の前に片付けた**。分割のコードは1行も書いていない。

### 実施内容

| # | Issue | PR | 内容 |
|---|---|---|---|
| 1-2 | #60 | #73 | resume でも tmux セッション名が安定。`Cmd+W` した claude に戻れる |
| 1-3 / 1-4 | #62 | #77 | `Cmd+Shift+G` を検索に返し、`Cmd+G`（次を検索）を足す |
| 1-1 | #61 | #78 | `TERM_PROGRAM` の素通しをやめ、嘘の `Restored session:` 行を止める |
| 1-10 | #63 | #79 | 入力欄の編集中はグローバルショートカットを先取りしない |
| 1-6 | #64 | #80 | タスク一覧の行をキーボードで押せるようにする |

### 設計判断

- **「分割後に顕在化」と評価していた 1-10（#63）を、分割の前に片付けた。** `Cmd+D` が
  分割に割り当たるのを待つまでもなく、**編集中の `Cmd+W` で編集対象のタブごと閉じる**
  という実害が既にあった。**「分割後に顕在化」は「今は無害」を意味しない**
- **1-8（#67 検索バーのはみ出し）は手を付けていない。** これは本当に分割が入って
  初めて幅が細くなるもので、いま直しても検証手段が無い

### 教訓

- **依存は逆向きにも効く。** #64 の E2E で「このアプリが起動したタスク行」を作るには
  `ownedByApp` が要るが、偽 CLI の固定 `agents.json` に対してそれを作る経路は
  通常存在しない。**唯一の抜け道が resume で、それは #60 の修正（PR #73）で
  `agentSessionId` が resume でも埋まるようになって初めて成立した**。
  分割の前段として片付けた負債が、別の負債のテストを可能にしていた
- **1つの周で5件を続けて直すと、後の周が前の周のテストを壊す。** #61 が #62 の
  S45 を落とした（表示が1行ずれた）。**壊れたのはテストのほうで、実装は正しかった**。
  実測してから直したので、実装を「修正」してしまう事故は避けられた

### 次に再開するとき最初に読むべきこと

1. **分割の実装は依然 PR 0-a しか終わっていない。** 次は PR 0-b（`doFit` と
   `handle.fit` の1本化）。`overview.md` の導入計画の表を見ること
2. `known-issues.md` の 1 の表に**状態列を足した**。残り5件（#65 / #66 / #67 / #68 と、
   1-8 は分割の周で踏む）
3. **スタックした PR 9本が未マージ**（#72 -> #73 -> #74 -> #75 -> #77 -> #78 -> #79 -> #80、
   および独立の #76）。分割の実装に入る前にマージ順を確認すること

---

<!-- 以降、作業のたびにセクションを追記 -->
