# Issue #56 ターミナルの分割表示（1タブ内に複数ペイン） - Overview

> **Issue**: [#56 ターミナルの分割表示（1タブ内に複数ペイン）](https://github.com/i-iwnl/ai-terminal/issues/56)
>
> 1つのタブを二分木で任意に分割し、複数のターミナルを同時に表示する（iTerm2 相当）。
> AI エージェントの並走がこのアプリの中心的なユースケースなのに、タブ切り替えでしか
> 行き来できないことが解決対象。
>
> 詳細は以下の3ドキュメントに分割:
>
> - `architecture.md` - 触る構造・Contract変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-07-29

---

## 1. ゴール

`useTabs.ts` の「タブ = PTY 1本」のフラットなモデルを「タブ = ペインの二分木」に置き換え、
分割・フォーカス・リサイズ・閉じるの4操作をキーボードとメニューから行えるようにする。

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（renderer。main はメニュー項目の追加のみ） |
| ブランチ | 未作成 |
| 関連PR | 未作成 |

---

## 2. 完成条件

- [ ] Cmd+D で現在のペインを左右に分割し、新しいシェルの PTY が右に立つ
- [ ] Cmd+Shift+D で上下に分割する
- [ ] 分割後のペインをさらに分割できる（二分木。深さに制限なし）
- [ ] アクティブなペインが視覚的に区別でき、キー入力・Cmd+F / Cmd+K がそのペインに向かう
- [ ] ペインをクリックするとアクティブが移る
- [ ] Cmd+W はアクティブペインを閉じる。タブ内の最後の1枚だったときだけタブを閉じる
- [ ] ペイン境界をドラッグしてサイズを変更でき、xterm の cols/rows と PTY の resize が追従する
- [ ] Cmd+Option+矢印でペイン間をフォーカス移動できる
- [ ] メニューに分割の項目があり、キーが表示されている
- [ ] レイアウトツリーの操作（分割・閉じる・隣接ペイン探索）が純粋関数として `test/unit/` で検証されている
- [ ] E2E で「分割すると2つのターミナルが同時に見える」「片方を閉じると残りが全面になる」が検証されている
- [ ] README とスクリーンショットが分割表示を反映している
- [ ] 型チェック通過（`make check`）
- [ ] Lintチェック通過（`make check`）

---

## 3. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 完了（`/design-review` を5ペルソナで実施し、案を改訂。`design-review.md` が正） |
| 実装 | 進行中（**PR 0-a のみ完了**。10 本中1本） |
| 検証 | PR 0-a の範囲のみ完了（S37 拡張） |

### 導入計画（PR 0-a 〜 9）の進捗

**全 12 本完了。** これまでコメント等で使っていた「10 本」は誤り（`0-a` / `0-b` / `0-c` を1本と数えていた）。design-review.md の表と Issue 本文の完了条件はどちらも 12 行ある。

進捗の詳細は `design-review.md` の「3. 改訂した導入計画」の表が正。ここには状態だけ書く。

| PR | 内容 | 状態 |
|---|---|---|
| 0-a | `screenReaderMode` をアクティブなペインだけに + S37 拡張 | **完了**（PR #74） |
| 0-b | `doFit` と `handle.fit` の1本化 + 同値の `pty.resize` を送らない | **完了**（PR #81） |
| 0-c | `role="alert"` / `role="status"` / ランドマーク | **完了**（PR #84） |
| 1 | `shortcuts.ts` の altKey ガードを矢印キーに限って緩める | **完了**（PR #87） |
| 2 | `tabs/paneTree.ts` の純粋関数 + 単体テスト（UI を1行も触らない） | **完了**（PR #95） |
| 3 | 木を導入するが常に leaf 1枚（振る舞いを変えない） | **完了**（PR #97） |
| 4 | 分割を有効化（`Cmd+D` / `Cmd+Shift+D` / `close-pane` / `menu.ts`） | **完了**（PR #98） |
| 5 | アクティブ表現 + ペインヘッダ + aria 名 | **完了**（PR #99） |
| 6 | ドロップ可能領域のハイライト | **完了**（PR #100） |
| 7 | スプリッタ（ゴースト方式）+ メニューでの比率調整 | **完了**（PR #102） |
| 8 | 最大化・ペイン移動・U4・閉じるときの確認 | **完了**（PR #103） |
| 9 | README + スクリーンショット + scenarios.yml | **完了**（PR #104） |

---

## 4. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | PR 0-b（`doFit` と `handle.fit` の1本化）に着手する | 分割が1行も入らなくても単独で意味がある。挙動不変・E2E 全 green が関門 |
| P1 | `known-issues.md` の 1（独立の負債10件）のうち #60 は解決済み | `Cmd+W` した claude に戻れない問題は PR #73 で解決。分割で `Cmd+W` の頻度が上がる前提が1つ解消した |
| P2 | `lib/paneTree.ts` の純粋関数と unit test（PR 2） | 分割 / 閉じる / 隣接探索。**モデルを先に固めてから UI を書く** |
