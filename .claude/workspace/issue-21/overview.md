# Issue #21 タスク一覧の状態色と文言が、実際の意味と逆になっている - Overview

> **Issue**: [#21 タスク一覧の状態色と文言が、実際の意味と逆になっている](https://github.com/i-iwnl/ai-terminal/issues/21)
>
> `claude agents --json` の `busy` はエージェント稼働中（人間は待たなくてよい）だが、
> 表示側は `busy` を橙＋グローで強調しており、**放っておいてよい行だけが光っている**。
> 誤りはコードだけでなく README・E2E キャプション・掲載スクリーンショットにも伝播している。
> 親 Issue [#20](https://github.com/i-iwnl/ai-terminal/issues/20) の Phase 0-a。
>
> 詳細は以下の3ドキュメントに分割:
>
> - `architecture.md` - 触る構造・Contract変更・設計判断
> - `worklog.md` - 時系列の作業ログ・次に再開するとき最初に読むべきこと
> - `known-issues.md` - 判明した問題・未解決事項・先送りしたもの
>
> **最終更新**: 2026-07-29

---

## 1. ゴール

タスク一覧が「いま自分の番であるセッション」を正しく強調する状態にする。あわせて CLI の生の英語（`busy` / `idle`）をラベルとして出すのをやめ、人間から見た意味に翻訳する。**見た目の再設計（グループ見出し・ソート・待たせている時間）は #20 の後続 PR の担当で、この周ではやらない。**

| カテゴリ | 対象 |
|---|---|
| 対象トラック | 単一（renderer + ドキュメント + E2E） |
| ブランチ | `fix/task-status-color-and-wording` |
| 関連PR | 未作成 |

---

## 2. 完成条件

- [x] 「あなたの番」の行が強調され、「作業中」の行が静かになっている（現状と逆転している）
- [x] 画面に出る状態ラベルが日本語に翻訳されている（`作業中` / `あなたの番` / `不明`）
- [x] `busy` / `idle` 以外の未知の値が「不明」として扱われ、**片側に誤訳されない**
- [x] 生の `status` 文字列が `.task-item__meta` に残っている（鉄則4/5）
- [x] `README.md` の説明が実装と一致している
- [x] `e2e/screenshots.spec.ts` のキャプションが実装と一致している
- [x] `docs/images/S12-task-list.png` を撮り直した
- [x] E2E `S12` が green（新しいクラス名・ラベルに追従済み）
- [x] 型チェック通過（`make check`）
- [x] Lintチェック通過（`make check`）

---

## 3. 現状進捗

| 項目 | 状態 |
|---|---|
| 設計 | 完了（#20 のペルソナレビューで確定） |
| 実装 | 完了 |
| 検証 | 完了 |

---

## 4. 直近の次アクション

| 優先度 | アクション | 詳細 |
|---|---|---|
| **P0** | PR を作成する | 実装・検証・文書更新は完了済み |
| P1 | #24 着手前に既知 status の定義を共通化するか決める | `known-issues.md` の1番。#24 で判定が3箇所目になる |
