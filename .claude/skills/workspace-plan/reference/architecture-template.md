# Architecture テンプレート

生成時は `{...}` をタスク固有の内容に置換する。タスクの規模に応じてセクションを省略してよい（単一トラックの小さい変更なら「2. Contract変更」以降を「なし」で1行にできる）。

---

```markdown
# Architecture

Issue #{Issue番号} における変更対象の構造。
設計判断の根拠は末尾の「設計判断履歴」を参照。

---

## 1. 対象トラック

{単一トラック / main（PTY・agents・history）+ renderer（React UI）の2トラック}

| コンポーネント | 変更内容 | 影響範囲 |
|---|---|---|
| {例: src/main/agents/} | {追加/変更/削除} | {影響を受ける他コンポーネント} |

---

## 2. Contract（src/shared/ipc.ts）変更

{IPCのチャンネル名・型を変更する場合のみ記載。変更しないなら「なし」と明記}

| チャンネル / 型 | 変更 | 内容 |
|---|---|---|
| {例: SpawnPtyRequest} | {ADD/ALTER} | {内容} |

---

## 3. 技術的制約・前提条件

- {制約1（例: PTY出力は加工しない、外部フォーマットのパース失敗でアプリを落とさない等、該当するルート CLAUDE.md の鉄則があれば引用）}

---

## 4. 設計判断履歴

| 日付 | 判断 | 根拠 | 代替案 |
|---|---|---|---|
| {日付} | {判断内容} | {根拠} | {検討した代替案} |
```
